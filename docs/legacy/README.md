# Legacy documentation (pre-v2 rewrite)

Everything in this directory describes the OLD three-binary system
(CLIENT + HOST + per-container LXC daemon, latch secrets, GHCR images)
that was replaced by the v2 rewrite in August 2026. Kept for archaeology
only — none of it matches the current system. Current docs live one level
up in docs/.
