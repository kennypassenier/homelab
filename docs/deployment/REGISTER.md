# Register — The Homelab Deployment Project

The mechanism behind scope constraint C6: **every finding, task and decision
of this project gets a number and lives here**, not in a conversation. A
session that lost its context resumes by reading this file. Updating a row is
part of doing the work, not paperwork afterwards.

↳ *C6 = the scope constraint requiring this project to survive a context
loss; see `SCOPE.md`.*

**Numbering.** `D#` decisions taken, `T#` tasks to do, `F#` findings (facts
discovered that change what we do), `B#` blockers waiting on someone else.
Numbers are permanent and are never reused, including after a row closes.

**Status.** `open` · `doing` · `done` · `parked` · `dropped`.

## Decisions (D)

| # | Decision | Where it landed | Date |
|---|---|---|---|
| D1 | Scope approved: goals G1-G8, non-goals N1-N5, constraints C1-C8 | `SCOPE.md` | 2026-08-30 |
| D2 | No-touch list narrowed to 100, 101, 102, 103 | `core/src/safety.rs`, pinning test | 2026-08-30 |
| D3 | VM 101 keeps its VM lifecycle untouchable; HA-internal config changes only with per-change permission | `SCOPE.md` C8 | 2026-08-30 |
| D4 | CT 107 is cleaned up; CT 111 is kept and integrated | `SCOPE.md` | 2026-08-30 |
| D5 | Scratch containers 190/191 cleaned up after coordinating with pipeline-v2 | `SCOPE.md` | 2026-08-30 |
| D6 | Project documents live in `docs/deployment/` in this repo | `SCOPE.md` | 2026-08-30 |
| D7 | Inter-service configuration uses LXC IPs, not public URLs | `SCOPE.md` C3 | 2026-08-30 |
| D8 | Almanac joins the scope; it already runs on CT 112 as an adopted native service | `SCOPE.md` G3 | 2026-08-30 |
| D9 | `no_touch` override removed from the live `/etc/homelab/host.toml`; the compiled list is the only list | host.toml (read back, daemon restarted) | 2026-08-30 |
| D10 | The three v1 stack files claiming live vmids are deleted | commit 35feb55 | 2026-08-30 |
| D11 | Emergency vzdump of CT 104 taken (2.4 GB, 28 s) and a daily job added at 02:30, `keep-daily=3,keep-weekly=2` | `/etc/pve/jobs.cfg` job `platform-104` | 2026-08-30 |
| D12 | The seven v1 restic repos in the Drive root stay until the new backup is proven by a restore | `BACKUP_MODEL.md` | 2026-08-30 |
| D13 | The E3 auto-restore granularity gap is closed (per path, not per stack) | pending build | 2026-08-30 |
| D14 | `<app>-config` becomes an enforced naming rule, not a convention | pending build | 2026-08-30 |
| D15 | MQTT terminates on the Home Assistant VM; CT 107 and its Traefik route are removed | `SCOPE.md`, T27 | 2026-08-30 |
| D16 | A service that changes container or IP requires telling Kenny first, with the list of what he must reconfigure | `SCOPE.md` | 2026-08-30 |
| D17 | Model v2: configuration moves to `/appdata/<stack>/<app>-config` on the host, restic runs host-side | `BACKUP_MODEL.md` | 2026-08-30 |
| D18 | Close all three privileged-path gaps AND ship a second golden template, one privileged and one not | pending build (T28-T30, T34) | 2026-08-30 |
| D19 | vzdump archives live on `hdd4tb-backup`; `/appdata` stays on pve-root | `/etc/pve/storage.cfg`, both jobs repointed | 2026-08-30 |
| D23 | Phase 3 frozen: T1 file-based Prometheus discovery, T2 per-stack dashboards plus a fleet one, T3 docker labels, T4 both invocations, T5 native becomes a list, T6 unchanged | `TECH_CHOICES.md` | 2026-08-30 |
| D24 | Phase 4 frozen: the ten-stack layout, `platform` → `gateway`, metrics and observability merged as `metrics` on 107, uptime-kuma alone on 108, syncthing to 113, vmid 110 permanently reserved | `TARGET_LAYOUT.md` | 2026-08-30 |
| D25 | One restic repository **per app**, not per stack — so an app can move between stacks with its history | `TARGET_LAYOUT.md`, B2 | 2026-08-30 |
| D26 | Container replacement keeps vmid and IP: prove the recipe on a throwaway vmid, vzdump, destroy, redeploy in place | `TARGET_LAYOUT.md`, C4 | 2026-08-30 |
| D27 | http-switchboard joins kyu and kyu-runner on CT 109 with an explicit port map (8080/8082/8083); no code change in any project | `TARGET_LAYOUT.md`, A4 | 2026-08-30 |
| D45 | Three v1 capabilities the current generation lacks, rated: W1 host hardware readiness **Onmisbaar**, W2 per-stack retention Gewenst, W3 boot-policy reconciliation Gewenst. Built after M7's drill, before the rest of the rollout (W4-B) | `FEATURES.md` W1-W3 — all three built 2026-08-31, in that order, right after the M7 drill they were sequenced behind. W3 proven on the live fleet: order moved by hand on CT 115, named by the fleet check, put back by a deploy. W1 found the assumption was already wrong (F110) | 2026-08-31 |
| D44 | SSH mini-round: public keys from `github.com/<user>.keys` (S1-A), a marked region the orchestrator owns in `~/.ssh/config` (S2), aliases named after the stack with the old name kept alongside (S3), and automatic updates with a shown diff — **only on Linux**, because a Windows client may follow (S4) | pending build | 2026-08-31 |
| D43 | Templates rebuilt as v3 after measuring a clone of v2: sshd was **failed on every clone** (host keys deleted, never regenerated) and the container booted `degraded` from two hardware services an LXC cannot satisfy. Both fixed; postfix and three unused docker plugins removed | Verified on a clone: ssh active with 6 host keys, `systemctl is-system-running` = running, only :22 and :9100 listening | 2026-08-31 |
| D77 | **The scaffold default must be a template the fleet uses** (Kenny, 2026-09-01: "dat probleem met die oude image moet wel ook verholpen worden zodat dat niet meer voorkomt"). Fixing the value was not enough — the class had to go | Test `the_scaffold_default_template_is_one_the_fleet_uses` (sabotage-verified) plus a fleet-check finding for any stack whose template is absent from the hypervisor | 2026-09-01 |
| D76 | **The retired v1 golden image (CT 999) is removed after the rollout** (Kenny, form K3) | queued | 2026-09-01 |
| D75 | **The three definition layers are written down** (Kenny, form K2): golden image = every container, identical everywhere; `_core` = every container, configured per container; preset = sometimes wanted. The deciding trade-off is how often the thing changes, not how widely it is used | `docs/PRESET_GUIDE.md` | 2026-09-01 |
| D92 | **The registry caches expire after a month, set in a mounted config file** (Kenny, form Z1, 2026-09-02). There was no expiry of any kind before: `REGISTRY_STORAGE_DELETE_ENABLED` grants permission to delete and nothing ever deleted, so the cache grew without limit — 1.3 GB in two days on a 32 GB disk. It has to be a FILE, and that is the measured half of this decision: `REGISTRY_PROXY_TTL` as an environment variable makes registry 2.8.3 answer `Ignoring unrecognized environment variable` and start with no expiry — a setting that looks applied and is not. With the file the log says `Starting cached object TTL expiration scheduler...`, verified 2026-09-02 in a throwaway container. 720h because a layer nobody has asked for in a month costs one slow pull to get back, and a cache is exactly the thing that may be slow once | **The registry caches' expiry is written down rather than inherited** (Kenny, form Z1, 2026-09-02; corrected the same night). The premise Claude gave him was wrong — there was already a default expiry of 168h, proven by the live `scheduler-state.json` — so the decision is not "add an expiry" but "make the existing one visible and changeable". It still has to be a FILE: `REGISTRY_PROXY_TTL` as an environment variable makes registry 2.8.3 answer `Ignoring unrecognized environment variable` and silently fall back to the default, which is a setting that looks applied and is not. The value stays 168h; changing it is now a line in a file Kenny can find rather than a default nobody knew about | 2026-09-02 |
| D93 | **A program is only adoptable here if it handles SIGTERM the way we want** (Kenny, 2026-09-02). The first hard requirement for lens 9, and it came from a measurement: of the four native Rust services, the kyu hub is the ONLY one with no signal handling at all — and the only one with a database. `systemctl stop kyu` sends SIGTERM and the process dies where it stands, including mid-write to SQLite, which is why F172 happens. It also makes "pause the service for a clean copy" unsafe as a remedy: it would stop a service that has no clean stop. The requirement is therefore not a nicety — it is the precondition for every backup strategy this orchestrator can offer a native service. Sent to the kyu project | F172, lens 9 in T63 **Met 2026-09-02**, the same night, by kyu 2.1.0 — and its shape is now the candidate norm for the other three: a configurable bound, one loud line and exit 0 when the bound is exceeded (a non-zero code would make systemd report a clean stop as a crash), idempotent under repeated signals, and five tests that send real signals to the real binary rather than mocking them. The causal claim in this row is corrected at F172: a hard kill was already safe for the data; what was missing was a still moment on disk. | 2026-09-02 |
| D94 | **The hub's backup will read the copy it makes of itself, not its live files — but only once that copy is fresh again** (Kenny, form Z10, 2026-09-02). kyu can write a complete, integrity-checked copy of its store with `VACUUM INTO` while it keeps running, and the endpoint refuses to overwrite and re-opens the copy to verify it before reporting success — so a copy that does not open is never reported as one. That is a better source than tarring live files, and it removes the need to stop the service at all. The precondition is F179: the run that makes those copies has been failing since our own migration, and switching to a source two days old would be the exact failure this project keeps finding. Kenny's wording of the choice: switch as soon as their fix lands | F172, F179, Z10, measured M-D94 | 2026-09-02 |
| D91 | **A directory can be declared not-worth-keeping, and the reason is the declaration** (Kenny, form Z3, 2026-09-02). `no_backup: "<why>"` on a mount: no restic repository, and the string is the only guard available because nothing can verify that data is genuinely reproducible. Distinct from `no_data`, which says the app keeps NOTHING and is checked by asserting the directory is empty — setting both is refused as a contradiction. The fleet check gained a third severity, `Noted`, so a deliberate gap is printed without raising an alarm; the runbook names every such path with its reason. Kenny's reasoning for the reason being mandatory is the one that decided it: a flag that switches backups off is what somebody sets on the wrong thing and discovers years later | F173, Z3 | 2026-09-02 |
| D82 | **Every option in a form says what it does, in two places** (Kenny, correction form D82, 2026-09-02). He bounced a form over four bare words: *"Je geeft een aanbeveling van gewenst, maar wat is gewenst exact? ... Het is voor mij helemaal niet duidelijk en dat moet het altijd wel zijn. Zeker bij software development waar details belangrijk zijn."* A label is vocabulary, not an answer to what happens if you pick it — and what it sets in motion differs per project and per moment, so it cannot live in the vocabulary table. His amendment to Claude's proposal: the pill keeps a SHORT version as its subtitle, and a **framed box directly above the pills** carries the exact line per option, **colour-coded the same way the pills are** so the danger and deferred options stand out in both places. Landed in `~/Projects/dev-procedure` (FORM_PROTOCOL.md §7 and the canonical template), because it holds for every project | correction form D82, measured M-D82 | 2026-09-02 |
| D90 | **The Uptime Kuma seeder becomes an app in the uptime stack** (Kenny, form W1). Not in the golden images — those are container templates, so Python would land on twelve containers that never use it and only on ones cloned afterwards — and not on the Proxmox host, which refuses loose Python packages by design (`EXTERNALLY-MANAGED`, measured 2026-09-01). As an app in `stacks/uptime/` the dependency is declared where it belongs, rebuilt like any other, and it travels with Uptime Kuma if that stack ever moves | `stacks/uptime/`, T49 | 2026-09-01 |
| D89 | **The backup drill is the LAST test of this rollout, and it has three parts** (Kenny, forms R5 and V5): every stack freshly backed up with counts noted and no zero-file result; one stack really restored onto a throwaway container; and the DR runbook regenerated and FOLLOWED as if the host were gone. Part three exists because that document was 140 lines out of date on 2026-09-01 and is the only thing left when the rest is not | T12 | 2026-09-01 |
| D88 | **Snapshot-before-update becomes a question to the failure-behaviour lens, not a task** (Kenny, forms R12 and V6). Recorded as an explicit question rather than something to remember, because 'remember it' is what went wrong with four other things that evening | T41, T63 | 2026-09-01 |
| D87 | **Uptime Kuma monitors are seeded by the existing Python script, not by a Rust reimplementation** (Kenny, forms R13 and V2, correcting Claude's claim that Kuma had no external way in). The 36 monitors were created by `seed-monitors.py` through `uptime_kuma_api`, idempotently. Reimplementing a protocol its authors do not offer as a public interface is precisely what breaks silently on an update | T49 | 2026-09-01 |
| D86 | **No preset is promoted into a golden image** (Kenny, forms R3 and V1, from his own question). A preset is mostly placeholders filled in at scaffold time, so there is nothing at the configuration end to bake in; the only real gain is a pre-pulled docker image, and the pull-through cache already delivers that — for existing containers too. A golden image reaches only what is cloned after it, which is F164's weakness in another form | T10 | 2026-09-01 |
| D85 | **Everything that matters already lives on `/appdata`** (Kenny, forms R7 and V3). He asked for case-by-case and the measurement collapsed thirteen cases into two: 32-128 KB of compose files inside each container, no docker named volumes anywhere, and CT 112's 39 MB is almanac's own binaries plus two renamed migration leftovers. Those two go; the rest stays | T19 | 2026-09-01 |
| D84 | **A file the repository drops is reported on every deploy and removed only by an explicit command** (Kenny, form H2b, his own shape over Claude's report-only proposal): the deploy names what is on the container and not in the repository and changes nothing; `homelab prune-orphans stacks/<name>` removes them after the same typed confirmation a destroy asks for. Deleting is the irreversible direction and a deploy runs when nobody is watching | `core/src/ops/deploy.rs`, `homelab prune-orphans` | 2026-09-01 |
| D83 | **The deploy window in the TUI stays a viewer for now** (Kenny, form H1b). It already shows the full live stream of an operation, scrollable; what it cannot do is ASK. Making it ask — two buttons at a check that reports a drop — means the host must be able to pause mid-deploy and put a question back over the line, which is a change to how client and host talk. Written down as T69 rather than built between two other things | T69 | 2026-09-01 |
| D82 | **A bind mount inside a READ-ONLY bind mount of the same service is refused by validation** (Kenny, form G8b, choosing the narrower rule over dropping it per G8). Nesting inside a WRITABLE mount stays legal, because it works and the gateway has been doing it for months. The narrowing is one correction on evidence rather than the first of a series — the difference from the ownership check, which guessed five times at something unknowable | `core/src/manifest.rs`, test `a_mount_inside_a_read_only_mount_is_refused` | 2026-09-01 |
| D81 | **Claude's answer text may not contain a question mark aimed at Kenny** (correction form C, ratified 2026-09-01, all ten fields). No "shall I…", no "say the word and I'll…". Reporting belongs in prose; choosing never does. The rule is deliberately FORMAL rather than substantive: the existing rule ("every choice is a form") requires recognising that a choice is being put, which is exactly the judgement that fails when background work is running — all five prose-borne choices that evening fell in that state. Second half: while a form is unanswered, no new live action is started on the machines; code, tests and documents continue | this register, `CLAUDE.md`; reviewed at the retrospective and only then promoted to `~/Projects/dev-procedure` | 2026-09-01 |
| D80 | **Every host operation is either reachable from the TUI or written down as command-line-only, with a reason** (Kenny, forms B4b and T1): "de TUI moet van alle features van dit project gebruik kunnen maken". Measured at nine of twenty-eight; six of the sixteen missing are now wired (backup, update, restore, guards, fleet check, incidents) and the rest carry a reason. A test refuses a new `Command` variant that answers neither way | `client/tests/tui_snapshot_tests.rs` | 2026-09-01 |
| D79 | **An app may declare that it keeps nothing on disk** (Kenny, form B4, his own proposal over both options offered): `no_data: true` on a storage entry. Such an app gets no restic repository, so an empty directory is the design rather than a backup that stopped — and an UNdeclared empty directory still stops the backup, because that means the file and reality have drifted (form B4b) | `core/src/manifest.rs`, the wizard's STORAGE step | 2026-09-01 |
| D78 | **The GoAccess report is LAN-only** (Kenny, form B2c, correcting Claude's proposal): the public route is removed and the websocket points at the LAN address. Routing it through Cloudflare was the cause of the frozen page — an https page may not open an insecure websocket — so the fix is one moving part fewer rather than one more. Claude's measurement: the report answers on `10.10.10.4:7881` from Kenny's desk, and `go.kp-soft.dev` resolves to Cloudflare, so there is no split-horizon DNS to lean on | `stacks/gateway/traefik-routes.yml`, `stacks/gateway/goaccess/` | 2026-09-01 |
| D77 | **Generated Grafana dashboards get their own directory and their own provider** (Kenny, form B3), so the repository's committed dashboards and the deploy's generated ones never write the same file. Pointing the setting at the live directory would have worked too and would have put two writers in one place — a dashboard changing for no visible reason is the kind of fault nobody traces | `stacks/gateway/grafana/`, `host.toml` | 2026-09-01 |
| D76 | **The five safety nets that had never fired get drilled deliberately** (Kenny, form P1), on a throwaway stack rather than on a live one. ALL FIVE DONE 2026-09-01, and four of them found something. Layer rules: refuse a shallow check (sabotage-verified). Runbook: regenerating and READING it found three faults in a document repaired weeks earlier (F150-F152). Push verification: `pct push` reported `5000000/5000000 bytes` onto a full disk and the readback found an EMPTY file — the step caught a tool lying about its own success. Cache fallback: with the ceiling at 1s the bounded attempt failed, the fallback rewrote the compose to the real registry and the deploy finished green. Destroy cleanup: route and discovery file removed, dashboard NOT — because that step ran on the wrong machine (F162) | the drill stack, destroyed the same sitting | 2026-09-01 |
| D75 | **A test carries the findings it covers and the register names the test it claims** (Kenny, form P3). Before this, fifteen rows claimed "test added" and exactly one test anywhere named its finding: the claims were true and unverifiable in both directions | `core/tests/register_tests.rs` | 2026-09-01 |
| D74 | **A working service can be promoted to a preset, with a mandatory review step** (Kenny, form K1): the command shows what it stripped and what it did not understand before writing, because a naive copy carries an address, a container name or a secret into a template the next service will use | after the rollout | 2026-09-01 |
| D73 | **The human checklist arrives as a notification that stays open until Kenny ticks it off** (Kenny, form I2) — the notification pipeline already has acknowledgement, and a list you have to go and find is a list an ADHD reader will not find | after the rollout | 2026-09-01 |
| D72 | **The baseline is shown to Kenny before a destructive step, and only recorded before an update** (Kenny, form J3). A baseline taken from an already-broken state records the breakage and then passes — the dead-key pattern again. Ten seconds of his attention is the only check that can tell "same as before" from "correct" | after the rollout | 2026-09-01 |
| D71 | **Service checks live beside the service and are shipped by its preset** (Kenny, form J2) — a service can move between stacks, so its checks move with it, and a new one does not start from nothing | after the rollout | 2026-09-01 |
| D70 | **Verification compares a measurement pair taken inside one operation, never a stored threshold** (Kenny, form J1, correcting Claude's proposal). His objection: "wat als ik nu de helft van mijn films zelf verwijderd zou hebben?" A fixed floor is an assumption about his library that stops being true and then alarms about his own housekeeping. Measuring before and after within the same operation makes "may rise, never fall" valid without any tolerance — and it is exactly what was done by hand for CT 106 | after the rollout | 2026-09-01 |
| D69 | **A pinned version keeps its pin; the label stops claiming otherwise, and the system reports what is available** (Kenny, form H5 + follow-up). His question — "hoe update ik die dan wanneer ik wel een nieuwe versie wil?" — exposed that nothing anywhere reports a newer version exists, so pinning would have meant silently falling behind, security fixes included | after the rollout | 2026-09-01 |
| D68 | **Uptime Kuma gets vmid 107, and the target layout is amended** (Kenny, form H1). The plan's 108 required a three-way shuffle — metrics 113→107, syncthing 108→113, uptime→108 — to place one container; 107 was free, and the whole chain existed only because 108 was wanted | `TARGET_LAYOUT.md` amendment pending | 2026-09-01 |
| D66 | **Both halves of the step check** (Kenny, form S2, 2026-09-01): every mutating step reads its own work back, AND a final pass compares the whole live container against the stack file, AND a deploy that stops half-way records itself as incomplete. Chosen over either half alone because the two failures of 2026-09-01 were different — a step that lies, and a deploy that stops before the step that would have made it visible | `runner.rs` `step_verified`, `deploy.rs` `reconcile` + `mark_incomplete`; tests `s2_*`, each sabotage-verified | 2026-09-01 |
| D67 | **hevc Rext hardware decoding is on** (Kenny, form S1, 2026-09-01), enabled and tested as he asked. The test then showed the 21 files are artwork, not video — see F134. The setting stays on, doing nothing, until he says otherwise | Jellyfin encoding config on CT 106 | 2026-09-01 |
| D64 | **A cache that cannot serve must be fallen back from, not waited out** (Kenny, form R2, 2026-09-01). Chosen over upgrading the cache image or dropping ghcr.io from the cache, because it is the only one of the three that also covers the failure not yet seen. The two rejected options remain available afterwards, without haste | `core/src/ops/deploy.rs`, `registry_cache.rs` | 2026-09-01 |
| D65 | **ghcr.io is pulled directly for now** (Kenny, form R1, 2026-09-01) — the manual half of the same fallback, so the media stack could be completed the same night rather than waiting on a repair of unknown length | seerr + flaresolverr live on CT 106 | 2026-09-01 |
| D63 | **The promtail log label is derived from the stack name, not typed — but only after the rollout** (Kenny, form Q2, 2026-08-31), together with the container rename he wants (`<vmid>-app-<stack>` → `<stack>`). Grounds for the order: changing labels mid-migration undermines exactly the before/after comparisons D61 demands, and those comparisons are his own requirement. Afterwards it is a small change, and it is the precondition for the rename — while the label is hand-typed, a rename breaks log continuity; once derived, the rename is free | `REGISTER.md`; queued behind M8 | 2026-08-31 |
| D62 | **The downloader gets its pre-flight before anything is decided about replacing it** (Kenny, form Q1, 2026-08-31): re-derive the migration inventory against the live CT 105 and record the numbers the acceptance test will compare against, then put the go back to him. Read-only, so it costs nothing and it answers whether tonight is even a sensible moment — he is downloading heavily right now | `M8_CT105_PREFLIGHT.md` | 2026-08-31 |
| D61 | **A media or downloader rebuild must be byte-for-byte the same result** (Kenny, 2026-08-31, form remark). Named consequences in his own words: nothing to reconfigure in the Jellyfin desktop app, the *arr suite keeps working, the downloads running right now stay intact, every media library stays untouched inside the programs. So an extra verification pass runs BEFORE either container is touched, not after: `MIGRATION_INVENTORY.md` is re-derived against the live machines and must come back with no unclassified row | `MIGRATION_INVENTORY.md` addendum 2026-08-31; the document's own staleness rule already said this, and it is now dated and owned | 2026-08-31 |
| D60 | **A pull-through image cache is built before the media container is rebuilt** (Kenny, form M3, 2026-08-31), not after and not never. Grounds: the M7 drill measured 573 s of a 653 s outage as one stalled registry pull (F108), and media has nine images waiting where home had one. Its value on CT 111 is near zero, so it is not a prerequisite for starting | `REALIZATION_PLAN.md` M8; T59 promoted from "needs Kenny's go" to scheduled | 2026-08-31 |
| D59 | **A directory the orchestrator borrows is declared apart from one it owns** (Kenny, form M1, 2026-08-31): a new `data_mounts:` block, not created, not chowned, not backed up, and refused at deploy time when it is missing. `storage:` keeps its strict rules — under `/appdata/`, named `<app>-config` — because those are what a restore uses to find its own snapshots. Kenny's own framing settled it: those folders are not ours to manage, and the block says exactly that. Corrects his one wrong premise along the way: the media containers do not reach the libraries over samba, Proxmox binds the same dataset into CT 103 and CT 106 at once (F111) | `manifest.rs` `DataMount`, `hardware.rs::check_data_mounts`, four tests in `deploy_tests.rs` | 2026-08-31 |
| D58 | **Sonarr and Radarr publish their "manual interaction required" event onto the hub** (topic `arr.ops`), kyu-runner's third route delivers it to `automation.arr_manual_interaction_webhook`. No translation hop: both apps support custom headers, so each carries its own kyu app token (`sonarr`, `radarr`, minted on /apps, both in latch as `media/sonarr/.env` and `media/radarr/.env`) and Home Assistant reads their own fields with templates — HTTPSwitchboard would have added a component without adding a guarantee. Cause: a season pack whose first file was named after the release instead of the episode; the blocked import sat unseen in the queue | `/etc/kyu-runner/config.toml` (route `arr-ops`, backup `.pre-arr`); chain proven live from both apps' test button: publish 201 -> runner -> HA trace finished -> dispatcher | 2026-08-31 |
| D57 | **Mini-round M1/M2 (2026-08-31): kp-soft comes off the edge entirely** until it is ready for the bare `kp-soft.dev`, and `APP_URL` points at the container meanwhile. The K1 choice of a temporary `website.kp-soft.dev` was reversed within the hour: it falls under the Access wildcard, and Access answers 401 to every write, so the address chosen to enable development blocked it | `docs/deployment/KP_SOFT_MIGRATION.md` carries everything the migration needs | 2026-08-31 |
| D56 | **kp-soft in management as CT 116** (K1–K5, 2026-08-31): `website.kp-soft.dev` during development and therefore behind Access; a read:packages token for the private GHCR image; the existing Gmail app password reused; admin on `mendax1@gmail.com`; the exposure accepted with the OPNsense restriction filed separately | Handover checks all pass: `/up` 200, the four public pages 200, `/hangmaten` 302, no `http://` in the HTML | 2026-08-31 |
| D54 | **HTTPSwitchboard deployed on CT 109 as the third native unit** beside kyu and kyu-runner, and Alertmanager's delivery leg finally connected: Alertmanager → kyu topic `alerts.raw` → switchboard translates → Home Assistant. Its flagship criterion, a genuine alert travelling the whole chain, is now met — delivered in 6 ms, one attempt | `stacks/kyu/http-switchboard/service.yml`, `captured/messaging/http-switchboard/`; proven with a labelled test alert 2026-08-31 | 2026-08-31 |
| D55 | Every link on the Homepage start page is an https name behind Traefik. Five services had no route at all and got one: `prom`, `alerts`, `kuma`, `kyu`; Loki has no page of its own and points at Grafana | Kenny reported the browser warning on every IP link. All four new names answer 302 through Cloudflare Access | 2026-08-31 |
| D53 | **kyu-runner deployed on CT 109 (R1)**, the homelab's warnings routed through the hub only (R2), and Home Assistant's push toggle switched on (R3). Two kyu app tokens registered (`kyu-runner`, `homelab-host`), both in latch. Adopted as a second native unit under the `kyu` stack, first backup taken | `stacks/kyu/kyu-runner/service.yml`; chain proven live: publish 201 → runner → HA events log at the same second → push | 2026-08-31 |
| D52 | **Q1: dashboards for the four unmanaged containers, generated now with the same code** the deploy uses (`homelab dashboard <stack> <app>...`), so the adoption at M8 replaces them byte-for-byte instead of fighting them. **Q2: synctest renamed to syncthing in place on CT 108**, not moved to 113 — that move waits on M7 and the confusing name was here today | 14 dashboards, up from 10; device id PHTJA5U-M3JLK7M-… unchanged across the rename | 2026-08-31 |
| D51 | **Homepage on its own container, CT 115** (P1), with full widget integration (P2) and `home.kp-soft.dev` (P3). Twelve credentials in latch; five were read out of the *arr services' own config files and one was minted through paperless's management command, which is what P2 authorised | `stacks/home/`, `TARGET_LAYOUT.md` amendment | 2026-08-31 |
| D49 | **H1: `vm.swappiness` 60 → 10, host-wide and persistent** (`/etc/sysctl.d/99-homelab-swappiness.conf`). Not "never swap" — it still swaps under real pressure, which is the safety net. It stops swapping as a routine optimisation | Cause of CT 114 at 509/512 MB of swap with 2 GB of RAM free, and of the host at 7.1/8.2 GB after four weeks with 15 GB available. G2's week of measurement now measures memory need rather than swap policy | 2026-08-31 |
| D50 | **H2: Kenny handed over the Uptime Kuma login**, stored in latch as `platform/uptime-kuma/.env`. Kuma has no way to add monitors without logging in and hashes its password, so there was no route in | Unblocks T22 — the instance has exactly one monitor, the kyu healthcheck Kenny set up himself | 2026-08-31 |
| D47 | **Mini-round N1–N4 (2026-08-31): three new services.** N1 own container `paperwork` on CT 114 rather than joining CT 111; N2 all three routed through Traefik; N3 the 25 surviving March documents re-imported; N4 paperless without Tika and Gotenberg | `stacks/paperwork/`, `TARGET_LAYOUT.md` amendment | 2026-08-31 |
| D48 | **Stirling-PDF gets its own login after all.** The mini-round recommended relying on Cloudflare Access alone, on the belief that Stirling has no authentication. Stirling 2.x has a mandatory one: it starts with the `security` profile and an `admin` account on a documented default password. Account name and password now come from latch | `stacks/paperwork/stirling/docker-compose.yml`, `latch cat paperwork/stirling/.env` | 2026-08-31 |
| D42 | M3 artifacts built: templates 998 (unprivileged) and 997 (privileged), both with docker, node_exporter and the two agent images baked in. Verified by cloning 998, starting it and asking — not from the build log | `pct config 997/998`; managed stacks now clone 998 | 2026-08-31 |
| D40 | E5 done: CT 190 and 191 destroyed after a final backup each; 10.10.10.14 and .15 are free, which unblocks the vmid convention for new stacks | `pct list`, both addresses silent | 2026-08-31 |
| D41 | O10 built and fail-closed, on the field names measured against the live server. B3's four restore drills all done with counts — native, docker stack, host config, whole container (4 s for 339 MB) | `busy_tests.rs`, `REALIZATION_PLAN.md` M6 | 2026-08-31 |
| D39 | M5 two of three: update classes written down and labelled (Y1/Z1), O9 clean shutdown built with the pull-stop-up order asserted. O10 blocked on the refused Jellyfin key | `UPDATE_POLICY.md`, `o9_stop_first_happens_between_the_pull_and_the_up` | 2026-08-31 |
| D38 | CT 107 destroyed (E4). Final vzdump taken first (289 MB), Traefik's `mqtt` entrypoint and route removed, the scrape target dropped from repo and live config | 20 of 20 Prometheus targets up, 0 down; fin/grafana/tasks routes still answer; fleet check clean | 2026-08-31 |
| D36 | v3.3.0 → 3.3.2 released and live on the host, each with an armed rollback that disarmed itself. T1/T2 enabled in `host.toml` (keys placed before the `[[zfs_jobs]]` tables — bare keys after a table header belong to that table in TOML) | `homelab ping` reports 3.3.2 | 2026-08-31 |
| D37 | `homelab check` runs clean against the real fleet: repo and reality agree | verified live after fixing two probe faults and one bookkeeping fault | 2026-08-31 |
| D35 | M3 capability built: two golden templates, privilege in the name, observability agents baked in | `template.rs`, two tests | 2026-08-30 |
| D34 | M2 complete (8/8) and M4 built: T1 discovery files, T2 generated dashboards, Y4 the fleet check as command and nightly pass. 20 suites green, clippy clean | commits 3db73bc, e65e351, 6bfc24f, 4bad39c | 2026-08-30 |
| D33 | O7 settled by mini-round: Kenny chose the literal rule (C) over the weaker shape — "uniformiteit en een duidelijke regel hebben hier voorrang". `prometheus-data` renamed to `prometheus-config` in repo and live; 21 series of history intact after the move | `manifest.rs`, live on CT 113 | 2026-08-30 |
| D32 | M2 in progress: O6 auto-restore per path, F38 restore timeout configurable, F39 backup target in `host.toml`, O5 clone-privilege guard and uid-mapping validation — all test-first, 19 suites green | commits 6b2bb2b, e758aee, dd96be6 | 2026-08-30 |
| D30 | M0 complete: every guest has a vzdump and a nightly job; kyu restore drill green — restored database holds 10 topics, 46 messages, 7 subscriptions, 69 deliveries, identical to live | drill run and cleaned up | 2026-08-30 |
| D31 | M1 complete: the monitoring drift is in the repo, promtail runs on CT 104 and its logs reach Loki | `stacks/metrics/`, `captured/` | 2026-08-30 |
| D29 | M0 started: kyu re-adopted under its real name and **backed up for the first time** (restic snapshot f2352564, 4.5 MB, `kyu-config`); the stale `mailbox` state key removed; the syncthing route repaired and verified 200 through Traefik | live, read back | 2026-08-30 |
| D28 | Vikunja is dropped; its data stays in the productivity stack's existing backup | `TARGET_LAYOUT.md` | 2026-08-30 |
| D20 | Phase 2 frozen after round 2; changes go through mini-rounds only | `FEATURES.md` | 2026-08-30 |
| D21 | Grafana's admin password rotated off the shipped default and stored in latch at `platform/grafana/.env` (env prod) | verified: new password 200, old default 401 | 2026-08-30 |
| D22 | Docker updates run through a maintained watchtower fork, with pre-stop/post-start hooks and a Jellyfin stream check | pending mini-round Z | 2026-08-30 |

## Findings (F)

| # | Finding | Impact | Status |
|---|---|---|---|
| F1 | A working monitoring stack (Alertmanager, cadvisor ×6, Grafana datasource + 3 dashboards, node/cadvisor/almanac scrape jobs, SMART collector on the PVE host) exists on the machines and in no repository | The next deploy from this repo silently reverts all of it | done 2026-09-02 — nagemeten: stacks/metrics/* + captured/pve-host/* — de hele monitoring staat in de repo |
| F2 | CT 107 runs no docker and no mosquitto — only sshd and node_exporter | 8 GB allocated to an empty container | done 2026-09-02 — nagemeten: stacks/uptime/lxc-compose.yml:11 — vmid 107 is nu de uptime-stack |
| F3 | CT 190 and 191 hold 10.10.10.14 and .15, the addresses a new CT 114/115 would take under the vmid-to-last-octet convention | Blocks the convention for new stacks | done 2026-09-02 — nagemeten: stacks/paperwork:15 en stacks/home:17 — 114 en 115 hebben .14 en .15 |
| F4 | A2's hostname guard, not the no-touch list, is what still refuses the legacy stacks (`lxc-media-stack` etc. are not `<vmid>-app-<stack>`) | Narrowing the no-touch list did not make them deployable | done |
| F5 | Two drives are old: Toshiba DT01ACA200 at 110 329 power-on hours, WD40EFRX at 63 194 — both pass SMART with zero reallocations | Justifies the pending-sector alert rule; a backup target choice must not assume these drives | vastgesteld feit — SMART-uren kunnen niet "gerepareerd" worden; de alert DiskPendingSectors is gebouwd |
| F6 | Grafana runs with `GF_SECURITY_ADMIN_PASSWORD=changeme_secure_password` and 10.10.10.4:3000 answers on the LAN with no gate | Must be treated as leaked; see §6 of the vault note "Homelab Open Issues" | deels — het standaard-wachtwoord is weg uit de repo, maar grafana publiceert 3000 nog ongefilterd op het LAN |
| F7 | homelab's record of the kyu stack is broken (pre-rename `mailbox` paths, `enabled: false`, `last_backup: 0`) — but a daily vzdump job covers CT 109 and one restic snapshot exists on Drive | **Corrected**: the data is protected twice over; what is missing is homelab maintaining it | done 2026-09-02 — nagemeten: stacks/kyu/service.yml; geen enkele "mailbox" meer in stacks/ of core/ |
| F8 | `/etc/homelab/host.toml` sets `no_touch` explicitly, which REPLACES the compiled default — today's code narrowing changed nothing live | The safety decision is not yet in force; the file must be updated per container | done 2026-09-02 — host/src/main.rs, de file voegt nu toe in plaats van te vervangen, met test |
| F9 | Uptime Kuma monitors exactly one target (kyu `/healthz`) after four weeks of uptime | Jellyfin, Traefik, HA, almanac and the whole edge are unwatched | done 2026-09-02 — nagemeten: stacks/uptime/kuma-seeder/seed.py:41 — 22 monitors, plus de gegenereerde helft |
| F10 | CT 107 is empty, yet `lxc-mqtt-stack.yml` still routes TCP :1883 to it while CT 104 publishes 1883 itself | Where MQTT terminates must be settled before 107 is removed | done 2026-09-02 — nagemeten: het bestand bestaat niet meer; traefik kent alleen entrypoint :80 |
| F11 | Ansible-era config (CT 104/105/106/111) lives only inside its own container; `/appdata` holds only metrics and synctest | A container rebuild loses the configuration | done 2026-09-02 — nagemeten: alle vier de stacks hebben nu host-side config mounts onder /appdata |
| F12 | The Cloudflare tunnel ingress and every Access policy exist only in the Cloudflare dashboard; `/opt/cloudflared-config/` is empty | The edge cannot be restored from any repository | done 2026-09-02 — nagemeten: docs/deployment/CLOUDFLARE.md — de edge is met de hand herbouwbaar |
| F13 | CT 104 runs no promtail, so the host that owns Loki ships none of its own container logs | Blind spot exactly where the edge lives | done 2026-09-02 — nagemeten: stacks/gateway/lxc-compose.yml:119 — promtail staat in de apps-lijst |
| F14 | kyu's store is a 98 KB `.db` behind a 4.1 MB `.db-wal` | Any backup or move must take .db/.db-wal/.db-shm together (standing rule 15a) | done 2026-09-02 — nagemeten: core/src/ops/native.rs:432 — tar over hele directories, .db/.db-wal/.db-shm samen |
| F15 | CT 111 was removed from the live no-touch list on 2026-08-29 so homelab could adopt it; the adoption never happened | Unprotected and unmanaged simultaneously | done 2026-09-02 — nagemeten: stacks/productivity/lxc-compose.yml:31 — CT 111 is een beheerde stack |
| F16 | restic backs up HOST paths (`storage[].host_path`), never paths inside a container | The four ansible-era stacks are in NO backup of any kind — no restic repo, no vzdump job | done 2026-09-02 — nagemeten: core/src/ops/backup.rs:169 plus native.rs:439 — host- en in-container-paden |
| F17 | `stacks/cloudflared` claims vmid 109 (kyu), `stacks/gateway` claims 108 (synctest), `stacks/todo` claims 111 — all v1-era leftovers | Only A2's hostname guard stops a deploy from targeting a live container | done 2026-09-02 — nagemeten: stacks/cloudflared en stacks/todo bestaan niet meer; geen dubbele vmids |
| F18 | The Google Drive target authenticates today and holds five restic repos (76 MiB) — no new token needed | B2 shrinks to a restore drill | done |
| F19 | Those repos hold 1, 1, 1, 7 and 3 snapshots while state claims nightly runs for all of them | Unverified whether retention explains it; measure before trusting the nightly run | vastgesteld feit — snapshot-aantallen leven op Drive, niet in de repo |
| F20 | The v1 restic repos in the Drive root stopped on 2026-07-04, and `platform-config` never received a single snapshot | CT 104 had never been backed up until today's vzdump | done (D11) |
| F21 | `pct clone` does not take `--unprivileged`, and the golden template CT 999 is unprivileged | A manifest saying `unprivileged: false` that provisions via `clone:999` silently yields an unprivileged container Test: `a_clone_refuses_a_privilege_level_the_template_cannot_give` | fixed 2026-09-01 |
| F22 | Every test in the suite uses `unprivileged: true`; the privileged path has no coverage at all | CT 105 and 106 are privileged and must stay so Test: `a_clone_refuses_a_privilege_level_the_template_cannot_give` | fixed 2026-09-01 |
| F23 | `host_owner_uid` is applied without any check against the container's privilege level | 101000 on a privileged container (or 1000 on an unprivileged one) silently produces unusable ownership | done 2026-09-02 — nagemeten: core/src/manifest.rs:547 + test o5_host_owner_uid_must_match_the_privilege_level |
| F24 | `/appdata` sits on `pve-root` — the same 94 G filesystem as the Proxmox OS | A container filling its config directory fills the hypervisor's root filesystem | partly closed (D19: archives moved, pve-root 41% → 26%) |
| F25 | vzdump archives of CT 102 and CT 103 from 2026-07-13 exist (943 MB and 262 MB) | The two untouchable containers do have a backup, seven weeks old — worth knowing before anyone assumes otherwise | vastgesteld feit — vzdump-archieven op hdd4tb-backup |
| F26 | Only two of Kenny's Rust services self-update: latch (minisign-signed) and almanac (M10, keeps the previous binary and reverts). kyu, kyu-runner, HTTPSwitchboard and newsflash each decided against it | The homelab must own updates for kyu and kyu-runner outright, not merely supervise them | deels — install_native bestaat, maar wie de terugkerende update bezit is nog Kenny's keuze (form open) |
| F27 | kyu 2.0.0 publishes **no release assets at all** — kyu-runner ships a musl binary + SHA256SUMS, almanac ships a binary + SHA256SUMS + minisig | There is nothing for the homelab to download, so kyu cannot be updated by any release-driven mechanism yet | done 2026-09-02 — nagemeten: stacks/kyu/service.yml:23 — release_repo gezet, checksum onafhankelijk geverifieerd |
| F28 | kyu's own documentation says "updates are image pulls via compose (K13)", but kyu runs as a native systemd binary on CT 109 | Its documented update path describes a deployment that is not the one in use | vastgesteld feit — documentatie van een ander project |
| F29 | containrrr/watchtower was archived 2025-12-17; the maintained continuation is `nicholas-fedor/watchtower` | Any auto-update design has to name a fork, not "watchtower" | vastgesteld feit — watchtower blijft gearchiveerd; UPDATE_POLICY.md noemt de fork en verwerpt hem bewust |
| F30 | The v1 directories are all still present: `apps`, `client-app`, `host-daemon`, `lxc-daemon`, `stacks-backup` | The never-answered V1-V5 cleanup | done 2026-08-30 (Z5) |
| F31 | A Jellyfin stream check already existed in v1 (`stacks-backup/media/jellyfin/check-streams.sh`, now in git history only) and **fails open**: a missing key, an unreachable API or an empty response all exit 0 = "safe to update" | The exact conditions in which you cannot tell whether someone is watching are the ones where it says go ahead. O10 must fail closed instead | deels — de fail-closed check is af EN onbereikbaar; zie F187 |
| F32 | The `JELLYFIN_API_KEY` in `/opt/jellyfin/.env` on CT 106 is **invalid** — HTTP 401 measured three ways (Authorization MediaBrowser Token, X-Emby-Token, `?api_key=`) | O10 needs a fresh key before it can be built or tested | achterhaald 2026-09-02 — D102 leest de Jellyfin-sleutel uit jellyfin.db bij elke deploy in plaats van uit een .env; zes widget-sleutels werden zo live gelezen |
| F58 | **Loki was the biggest log producer in the fleet.** The service that collects everyone's logs had written 923 MB of its own info-level output on CT 104, unbounded. Recreating it under the new cap took docker's log directory from 908 MB to 27 MB and the container's disk from 6.2 GB to 5.3 GB | The irony is not the point; the point is that nothing was watching the watcher's own growth | fixed 2026-08-31 |
| F59 | promtail on CT 104 reads every container's json log **including Loki's own**, so Loki's output is shipped into Loki | Not an amplifying loop — Loki's info lines are startup and compaction, not per-ingest — but it is waste, and worth excluding | **still open**, attempt reverted 2026-08-31 — see F69 |
| F131 | **A dead API key that looked exactly like a live one.** The container being replaced passed Jellyfin a `JELLYFIN_API_KEY` in its compose environment. I carried it across verbatim, stored it in the vault, then used it to verify the rebuild — and got 401. It is absent from Jellyfin's own `ApiKeys` table; the four keys that ARE there (Seerr, HomeAssistant, ClaudeDesktop, Homelab) live in `/config` and were restored with it. So the variable had been decorative for an unknown length of time, and it was still being faithfully propagated | Removed from the compose and from the vault (Kenny, form R3). Nothing to remove on Jellyfin's side — it never knew it. The compose now carries a comment saying why there is no key there, so the next person does not "fix" its absence | fixed 2026-09-01 |
| F130 | **A truncated blob poisons the direct pull too.** After the cache abandoned a layer at 157 MB, pulling the same image straight from ghcr.io failed with `unexpected commit digest … failed precondition` — which reads as *this image is broken upstream*. It was not: docker's content store held the half-written blob from the abandoned attempt and kept trying to finish it. `docker system prune -f` cleared it and the direct pull then took seconds | The F129 fallback prunes before it retries, for exactly this reason. Test: `f129_a_cache_that_cannot_serve_falls_back_to_the_real_registry` | fixed 2026-09-01 |
| F129 | **The registry cache answers its probe and still cannot serve.** D60 gates the rewrite on a live probe of each upstream, which is why this got through: ghcr.io answered `/v2/` in 0.7 ms. Then it streamed 157 MB of a seerr layer, hit `stream error: PROTOCOL_ERROR` against GitHub, returned 500 after **10 m 12 s**, and docker began again — twice, until the 900 s step ceiling stopped the deploy. The same container measured 4.1 MB/s pulling the same image directly. Two of the media stack's nine apps could not start; I started them by hand. Kenny's C1 said "redirect during the rollout, **with fallback**" — the redirect was built, the fallback was not | Built (Kenny, form R2): a cache-rewritten app gets a bounded first attempt (`pull_timeout_secs`, default 180 s); on failure the deploy pushes the file naming the real registry, prunes, and pulls that — and leaves the file that way so `up -d` starts what was actually fetched. Sabotage-verified. Test: `f129_a_cache_that_cannot_serve_falls_back_to_the_real_registry` | fixed 2026-09-01 |
| F166 | **The front page lists nothing, and has for as long as anyone can tell.** Measured 2026-09-01 while writing the home stack's service checks: every file under `/appdata/home/homepage-config/` holds zero entries — `services.yaml` is 0 bytes and `docker.yaml` is the shipped example with everything commented out. Homepage renders, answers 200, and shows an empty page. The Uptime Kuma monitor for it asks exactly that status code and is green; so is its container health. Nobody noticed because an empty dashboard looks precisely like a dashboard with nothing wrong | Fixed by T51: the page is now rendered from the gateway route fragments the orchestrator already writes, fleet-wide, so a stack with a front door cannot be missing from it. Test: `a_route_fragment_yields_its_hostnames_and_its_backends` | fixed 2026-09-01 |
| F167 | **An app dropped from a stack leaves its directory behind, and nothing looks.** Found 2026-09-02 by the new stack-coherence test: `stacks/productivity/vikunja/` still existed beside the manifest, left over from f4e4070 ("SuperSync only — Vikunja is not rebuilt"). It was empty and untracked, so git could not remove it and CI could never see it — but the same shape with files in it is a directory that is never started, which from every side looks exactly like a service that is down | Directory removed. The class is now held by a test that compares each stack's `apps:` list against the non-empty directories beside it, in both directions, and checks that every `./file` bind a compose declares actually exists in the repository. Test: `every_stack_manifest_agrees_with_the_directories_beside_it` | fixed 2026-09-02 |
| F168 | **The kyu hub cannot be reinstalled from its own release.** Measured 2026-09-02 while declaring the release sources for T11: `gh release view kennypassenier/kyu` returns v2.0.0 with **no assets at all** — no binary and no SHA256SUMS. kyu-runner, http-switchboard and almanac all publish one. So the single service that every other service on CT 109 talks to is the one whose binary exists only as the copy somebody put there by hand, and `stacks/kyu/lxc-compose.yml` promises a rebuild that ends with "the three binaries are installed the way C7 installs them" — which for kyu is not possible today | The orchestrator side is done: `homelab install-native` refuses a release with no SHA256SUMS rather than installing an unverified binary, and `stacks/kyu/service.yml` now says out loud why it declares no release source. The fix belongs in the kyu repository — it has to publish a release artefact. Recorded as T72 **Closed 2026-09-02 by the kyu project.** Release v2.1.0 carries `kyu` and `SHA256SUMS`. Verified here independently rather than taken on trust: downloaded both, `sha256sum -c` answered `kyu: OK`, and the binary reported `kyu 2.1.0`. The asset name matches the unit name, so `stacks/kyu/service.yml` needed one line and no override. Their workflow lifts the binary out of the docker image it just built instead of compiling twice, and they measured the two byte-for-byte identical — so what this orchestrator installs is literally what runs in the container. | fixed 2026-09-02 |
| F169 | **Every file bound for a container is staged through one fixed path on the host.** Found 2026-09-02 while measuring whether operations could run concurrently: `push_content` wrote to `/var/lib/homelab/push-staging` — one name, no per-operation suffix, for the whole daemon. It cannot misfire today because the host serialises every mutating operation behind a single lock, which is exactly why it had never been noticed. The failure it would produce is the silent kind: two concurrent pushes overwrite each other's staging file, one stack's compose lands in another stack's container, and BOTH `pct push` calls report success, so nothing in any output points at the cause | Fixed: the path is derived from `(vmid, dest)`, so two pushes to different files never share one. Derived rather than random or time-based on purpose — core never reads clocks, and a random name would leave a different orphan behind after every failed push. Sabotage-verified by putting the fixed path back. Test: `a_staging_path_is_unique_per_target_file` | fixed 2026-09-02 |
| F170 | **A mount declared in a manifest but never deployed fails the whole stack's backup, with a message that names the wrong problem.** Found 2026-09-02 in the T12 drill. `stacks/uptime` gained a `kuma-seeder-config` mount that evening (T49); the stack was not deployed afterwards, so the directory does not exist on the host, and restic answered `Fatal: all source directories/files do not exist`. That message reads as "the backup is broken", when the truth is "this stack has not been deployed since its file changed". The backup step should say which path is missing and that a deploy creates it | Open. The uptime stack still has no `kuma-seeder-config` snapshot **Fixed 2026-09-02** (Kenny, form Z5). The backup now checks every declared path before it snapshots anything and names the missing one, with the command that creates it and the sentence that this is not a broken backup — because restic's own message says the opposite. Test: `a_declared_path_that_is_not_there_yet_says_deploy_first` | fixed 2026-09-02 |
| F171 | **The native backup runs without a restic cache, and says so nightly.** Found 2026-09-02 in the T12 drill: every native backup printed `unable to open cache: unable to locate cache directory: neither $XDG_CACHE_HOME nor $HOME are defined`. `backup_native` builds its restic environment by hand and had drifted from `backup.rs`, which has set `RESTIC_CACHE_DIR` for every compose stack since it was written. Without a cache restic re-fetches from Google Drive metadata it should have had on disk. A warning printed nightly that nobody reads is the same as no warning | Fixed: the streamed pipeline uses the same cache constant as every other path. Test: `the_native_backup_uses_the_same_restic_cache_as_every_other` | fixed 2026-09-02 |
| F172 | **The kyu hub's database is archived while it is being written to.** Found 2026-09-02 in the T12 drill: `tar: /appdata/kyu/kyu-config/data/kyu.db-wal: file changed as we read it`, and `set -o pipefail` turned that into a failed backup. Compose stacks have a quiesce step driven by `com.homelab.backup.pause` labels; native services have no equivalent, so the hub is live and writing while its SQLite write-ahead log is read. At least it is loud — the backup fails rather than storing a quietly inconsistent copy | Open. Needs a decision: stop the unit briefly, use SQLite's own online-backup, or accept and record. Put to Kenny **Closed 2026-09-02 by the kyu project — and one word of this row was wrong.** kyu 2.1.0 handles SIGTERM: in-flight requests finish, the write-ahead log is folded back with `wal_checkpoint(TRUNCATE)`, and it exits 0, bounded by `KYU_SHUTDOWN_TIMEOUT_MS` (default 10000, rejected loudly if unusable). Measured on the live service: the log was 144232 bytes before the stop and gone after it, and `tar` then made a clean archive with no complaint at all. **The correction:** this row implied the process dying mid-write put the DATA at risk. It did not. kyu's store runs WAL with `synchronous=FULL`, and their `l5_crash.rs` SIGKILLs the real binary ten times under traffic without ever needing manual recovery — a hard kill was already safe. What was genuinely missing is a moment when the files on disk stand still, which is what `tar` needs and what this fix provides. | fixed 2026-09-02 |
| F173 | **The registry cache has no expiry, and is backed up.** Measured 2026-09-02. `/appdata/registry/registry-config` holds 1.3 GB after two days (dockerhub 650 MB, ghcr 672 MB), on a 32 GB disk with 29 GB free and **no cleanup policy of any kind** — `REGISTRY_STORAGE_DELETE_ENABLED` permits deletion but nothing ever deletes. Separately, the T12 drill backed the cache up: 324 files, 1381 MB, **457 seconds — the slowest stack of the thirteen, a fifth of the whole round** — for data that is by definition re-downloadable from upstream. Also measured, correcting Claude's first proposal: `REGISTRY_PROXY_TTL` is **not** accepted by registry 2.8.3 (`Ignoring unrecognized environment variable`), so a TTL needs a mounted `config.yml`, not an env var | Put to Kenny (his question, 2026-09-02) **Decided and done 2026-09-02** (Kenny, forms Z1/Z2/Z3). The cache is out of the backup via the new `no_backup` declaration, and its repository was removed from Google Drive at his explicit request — verified first that both snapshots held nothing but `/appdata/registry/registry-config`; 1.177 GiB and 80 objects freed. Expiry via a mounted `config.yml` is D-Z1. **CORRECTION, same night.** The claim "no cleanup policy of any kind" was wrong. Registry applies a DEFAULT `proxy.ttl` of 168h, and reading `scheduler-state.json` on the live caches proved it: all 137 entries carried an expiry exactly seven days after they were cached. Claude asserted an absence without checking for a default, and the first version of the fix set 720h — which would have made the cache live four times LONGER than it already did, the opposite of what Kenny asked for. Corrected to 168h, the value that was already in force. What the change is actually worth is the second half of D92: the number is now written down instead of inherited invisibly. | fixed 2026-09-02 |
| F174 | **Two restic repositories on Google Drive belong to services that no longer exist under those names.** Seen 2026-09-02 while removing the registry cache: `mailbox-config` (kyu was called mailbox until 2026-08-29) and `synctest-config` (the pilot's name for what became syncthing). Nothing writes to either; they are frozen copies under names no current stack claims, and a rebuild guided by the runbook would never look for them. Not deleted — a backup under an old name may still be the only copy of something from before the rename | Put to Kenny: keep as history, or remove. Their contents should be read before either **A third one, found 2026-09-02:** `metrics-config` — one snapshot of `/appdata/metrics/prometheus-data` from 30 August, 186 MiB, under a stack-shaped name that predates D25's per-app naming. The metrics stack's real repositories are `prometheus-config` and `alertmanager-config`. Same question as the other two: history worth keeping, or three repositories nobody reads. | done 2026-09-02 — D98: alle drie de wees-repos van Drive gepurged, 30 blijven over met een levende eigenaar |
| F175 | **A missing file made the seeder call eleven live stacks stale.** Found on its first real run, 2026-09-02. `load_host_monitors` returns an empty list when the generated file does not exist — correct — and the stale check then compared every `host · …` monitor against that empty list and reported kp-soft, kyu, media, metrics, paperwork and productivity as watching stacks the fleet does not have. All of them exist. The file is missing only because the running host predates the T49 step that writes it. Absent data was treated as an empty answer, which is the same shape as every other finding in this register | Fixed: the stale comparison runs only when there IS a list, and says so out loud when there is not. Verified live — the same run went from `11 stale` to `0 stale` | fixed 2026-09-02 |
| F176 | **The seeder's own checks could not fail, and two of them failed the deploy instead.** Found 2026-09-02, at the cost of two `deploy-uptime mislukt` notifications on Kenny's phone at half past two. One check counted matching lines in a ROLLING two-hour log window and demanded with `must_match` that the number never change — a window that empties as time passes cannot promise that, and the first successful seeding run (0 → 1) failed the deploy. Another used `must_be_present` on a `grep -c`, which always prints a number and therefore can never fail. Both were written by Claude the same evening | Rewritten to assert something real: zero ERROR lines in the last round, a round that actually reported an outcome, and a generated-list count that starts honestly at zero while the host is too old to write it. Dry-run on the container before deploying, which is what should have happened the first time | fixed 2026-09-02 |
| F177 | **A replica claims the live path of the thing it copies.** Found 2026-09-02 by following the disaster-recovery runbook as if the host were gone (T12 part 3) — which is what that exercise is for. `HDD18TB/replica/HDD2TB/paperless-config` and the real `HDD2TB/paperless-config` BOTH carried `mountpoint=/appdata/paperwork/paperless-config` with `canmount=on`, and nothing anywhere decides which of the two wins after a reboot. If the copy wins, paperless serves stale documents, writes land in the replica, and the next replication run overwrites those writes — silently, in both directions. The cause is `zfs send -R`, which carries the source's properties so the receive applies them. Exactly one collision existed; every other replica sits under the replica tree | Live: `canmount=off` on that replica, verified, with the real dataset still mounted and intact. In code: both send/receive paths now pass `-x mountpoint`, so a replica inherits a location under the replica tree instead — mountable, but only deliberately. Both the full and incremental forms were tested on throwaway datasets on the real host before the change. Test: `a_replica_never_inherits_the_path_its_source_lives_at` | fixed 2026-09-02 |
| F178 | **Roughly 700 GB of original photographs and music-school recordings exist in exactly one place.** Measured 2026-09-02 while explaining what CT 103 holds. `HDD12TB/subvol-103-disk-0` carries 8.40 TB, and next to 7.7 TB of series it holds `Oude sorteerpret en originele fotos` (362 GB), `Foto werkmap` (231 GB), `Sorteerpret` (38 GB) and `Muziekschool` (41 GB). A file-type sample says what they are: NEF (Nikon RAW), JPG and LRPREV in the photo folders, MKV and AU in Muziekschool — originals, not copies of something else. The pool has **zero snapshots**, is replicated **nowhere** (the E8 jobs cover HDD2TB and HDD4TB only), and restic covers `/appdata` so it never sees this at all. Series and films on the same pool are deliberately out of scope and are replaceable; original photographs are not. If that pool dies, they are gone | Put to Kenny — this is his decision about his own material, not a defect to fix unasked **Kenny, form Z6 (2026-09-02): Niet doen.** He made the call with the measurement in front of him; no reason was given and none is invented here. Recorded so the state is legible rather than forgotten: roughly 670 GB of original photographs and 41 GB of music-school recordings sit on `HDD12TB` with no snapshot, no replica and no restic repository, by decision. | closed by Kenny 2026-09-02 |
| F179 | **Our own migration broke kyu's nightly self-backup, and it has failed silently since.** Measured 2026-09-02 while checking a claim from the kyu session that the copy "runs every night at 03:00". It does not: `ls` shows five copies, the newest from **31 August**, and `systemctl status kyu-backup.service` says `Active: failed (Result: exit-code) since Tue 2026-09-01 03:01:05 UTC` with `grep: /etc/kyu/kyu.env: No such file or directory`. The cause is ours — the C7 adoption moved the configuration to `/appdata/kyu/kyu-config/kyu.env` on 2026-09-01, and `/usr/local/bin/kyu-backup` has `ENV_FILE=/etc/kyu/kyu.env` hardcoded on line 18. The unit is in no repository: it lives only on CT 109, so nothing here would ever have noticed. Same shape as every other finding this week — a mechanism that runs on a timer, fails, and is read by nobody | Cause is ours, fix is theirs: a helper script may not hardcode a path the deployment owns. Reported to the kyu project with the measurement. Claude did NOT patch their script. Candidate for norm N3 **Fixed 2026-09-02 by the kyu project, verified here.** Their script now reads `KYU_TOKEN`, `KYU_LISTEN` and `KYU_DATA_DIR` from the environment systemd supplies and refuses by NAME when one is missing rather than guessing; no path is left in it. Measured on CT 109 after their fix: three fresh copies at 03:13–03:14, 393216 bytes each, and `systemctl show kyu-backup.service` reports `Result=success ExecMainStatus=0`. They also added `OnFailure=` publishing the unit name, host and last journal lines to the hub's `ops.alerts` topic — and proved it by reproducing our fault rather than assuming it works. Its limit is stated rather than hidden: if the hub itself is down there is nowhere to publish, and that case belongs to Uptime Kuma. **The norm this produced was mine and was wrong:** I proposed it as a new N3, and standing rule 28 had said it since 2026-08-31. They folded the genuinely new halves into rule 28 instead — helpers fall under the same rule as their binary, and an operational file that exists only on a machine is outside every rule until it has a repository. **CORRECTION on my own verification, 2026-09-02.** I wrote "fixed, verified here" on the strength of three fresh copies and `Result=success` on the service. That evidence cannot tell a nightly run from a hand-started one, and these were hand-started: the kyu session's own drill. The reliable source is the TIMER, and it says `LastTriggerUSec=Tue 2026-09-01 03:01:05 UTC` — still the failed run. Four starts appear in tonight's journal, all manual. The two-hour offset between the container's UTC and the host's local time made 01:13 UTC read as 03:13, which is exactly plausible as a nightly hour; that coincidence is what made the wrong reading look right. **What IS verified:** the script carries no path any more — `grep` finds `/etc/kyu` and `/var/lib/kyu` only inside its comments explaining the fault, and the settings come from `KYU_BACKUP_KEEP`, `KYU_LISTEN` and the environment systemd supplies. So the code fix is real and the nightly is unproven. Same shape as R30 twice over, and the kyu session caught it both times.| code fixed; nightly unproven 2026-09-02 |
| F180 | **The host's own configuration file was in no backup at all.** Found 2026-09-02 by finishing T12 part 3 — listing every repository the runbook names, comparing it against what is really on Google Drive, and then reading what each extra one contains. `host-meta-config` (H10) carries `/var/lib/homelab/{repo,secrets,state.json,tls-*}` and **not** `/etc/homelab/host.toml`, which holds the API token, the notify bearer, the OPNsense credential path, the ZFS jobs and every other setting. A repository described as the host's crown jewels gave back the keys and the history and still left that file to be retyped from memory before anything could talk to the daemon | Added to the H10 snapshot, and the test now asserts all six paths instead of three. Test: `h10_host_meta_backup_snapshots_vault_state_tls` | fixed 2026-09-02 |
| F181 | **The disaster-recovery runbook never mentioned the backup of the host itself.** Same walk, same night. It described how to restore each stack's data and never named `host-meta-config` — the one repository holding the vault, the intent history, the TLS material and (now) the host config. Worse for a reader in an actual disaster: `restic.pw` lives INSIDE that repository, so it cannot open it, and the document said "keep an offline copy" without saying where Kenny's is. Every other repository uses the same password, so without it nothing is readable at all | The generator gained a Layer 3 that comes FIRST, naming the repository, its five paths, the chicken-and-egg, and where the offline copy lives. Also verified while there: every repository the document names does exist on Drive — the dangerous direction was clean | fixed 2026-09-02 |
| F182 | **One `/appdata` path is a mounted dataset, and restoring in the wrong order would hide it silently.** Measured 2026-09-02 while answering Kenny's question about where `/appdata` lives. Twelve of thirteen `/appdata/<stack>` directories are plain directories on `pve-root`; `/appdata/paperwork/paperless-config` is a separate ZFS dataset (`HDD2TB/paperless-config`) mounted at that path. Two consequences nobody had written down. **Protection is asymmetric:** that one path has three protections — restic, a ZFS snapshot (`@homelab-20260901-0214`) and the E8 replica (`HDD18TB/replica/HDD2TB/paperless-config`, 5.80 M) — while the other twelve have exactly one, restic to Google Drive, with zero ZFS snapshots anywhere near them. **Restore order is load-bearing:** restore `/appdata` from restic before mounting that dataset and the files land on the root disk, where mounting afterwards hides them — the operator would be looking at a mount from somewhere else and believe the restore worked | The runbook now says to mount that dataset first and to check its snapshot and replica before reaching for Google Drive. The asymmetry itself is left as it is: it is not a defect, but it appears to be historical rather than decided **Kenny, 2026-09-02: "trek paperless gelijk met de rest".** Done, and the asymmetry is gone: `/appdata/paperwork/paperless-config` is now a plain directory on `pve-root` like the other twelve, with restic as its only protection. Claude stated the one objection — paperless loses its ZFS snapshot and its E8 replica by doing this — and then carried out the decision. The move used the drill discipline: paperless, its database and redis stopped so nothing could write; `rsync -aHAX --numeric-ids` to a staging directory on the root disk; **fingerprints compared before the dataset was unmounted** (71 files, `c2a9b99c1b970f2117537c31fa11e476`, identical); only then unmounted, moved into place and restarted. Verified after: all three containers healthy, the login page answers 200, and the database still reports **25 documents** — the same number as before. The old dataset is NOT destroyed: it is frozen at `/HDD2TB/paperless-config-frozen` with `canmount=off`, so it keeps its history and can never return over the live path. The runbook's order warning is replaced by the new truth rather than left standing — a document that warns about a hazard that no longer exists teaches its reader to distrust it. | fixed 2026-09-02 |
| F183 | **A router firewall cannot isolate a container from its neighbours, and T57's main goal needed exactly that.** Measured 2026-09-02 immediately after applying the rule Kenny approved (form Z11/Z12/Z13). The rule works and was verified from both sides: reaching the Proxmox host on 10.10.5.250 is now blocked, the internet still answers, and DNS, Loki and the registry cache all still work. But Jellyfin on 10.10.10.6 still answers 200 from CT 116 — because `ip route get 10.10.10.6` returns `dev eth0` with no gateway, while `ip route get 10.10.5.250` returns `via 10.10.10.1`. Traffic between two containers on the same /24 goes straight over the bridge and never touches OPNsense, so no rule there can see it. Three of the four subnets are genuinely closed; the neighbours — the case Kenny actually worried about — are not | The lever is elsewhere and it is in this orchestrator's own domain: both containers have `firewall=0` on their Proxmox NIC, and Proxmox's per-container firewall sits on the veth where it CAN see neighbour traffic. The alternative is a private VLAN on the Omada switch (CT 102). Put to Kenny; the OPNsense rules stay because they do real work | open 2026-09-02 |
| F184 | **The fleet check gave eight containers advice the machine cannot follow.** Found 2026-09-02 while checking whether anything broke: eight containers reported swap in use, each with the remedy "give the container more memory rather than more swap". Measured what the host actually has: **31 GB of RAM, 47 GB promised to guests** (35 GB to containers plus 12 GB to VMs) and **7 of its 8 GB of swap already in use**. Following that advice once takes memory from another container; following it eight times makes the machine worse — and the check would have kept saying it every night, which is how a report stops being read. The swap readings themselves are correct; the remedy was written as if the host always had room | Fixed: the host reads its own totals and commitments, and when it is oversubscribed or already swapping hard the remedy names that instead — with the numbers and the trade-off, so the reader can see there is nothing to give. The finding is unchanged; only the advice is. Sabotage-verified. Test: `a_swapping_container_on_a_short_host_is_not_told_to_take_more_memory` **Live after shipping, and the fuller reading corrects Claude's own framing.** With the fix running the check now says: `the host has 31811 MB of RAM with 47360 MB promised to guests, and 8178 of its 8191 MB of swap in use`. Claude called that "no room, and not a state that improves on its own", which was more alarming than the machine deserves. Measured properly: **17.9 GB is available**, `vmstat` shows swap-IN happening and swap-out at zero, and the swap holds mostly two `kvm` processes (3568 MB and 1622 MB) — VM pages touched once weeks ago and never again. So this is stale ballast, not active pressure. What is genuinely true and non-urgent: the safety valve is spent, so a future spike has nothing left to be absorbed into. Worth a decision at some point; not worth waking anybody.| fixed 2026-09-02 |
| D95 | **Managed containers get no swap** (Kenny, form W1, 2026-09-02). Thirteen stack files set `swap_mb: 0`, applied live one container at a time with a check after each; all thirteen kept running and every service came through. The reason is measured rather than doctrinal: a container's memory limit counts its PAGE CACHE, so heavy reads fill it to the limit and the kernel then evicts the program's pages to keep disposable cache — exactly backwards. CT 105 is the clearest case: 120 MB of program inside a 2048 MB limit, 1808 MB of it cache, and 67 MB swapped. At zero the kernel drops cache instead, which costs a re-read. Every container measured between 4% and 37% of its limit, so none was using swap as a safety net. **What it does NOT do:** already-swapped pages stay out — `memory.swap.max` is 0 so nothing new goes there, but cgroup v2 does not fault the old pages back; they clear when the container next restarts | W1, W2 | 2026-09-02 |
| F185 | **Why the HOST swaps: two VMs the project may not touch** (Kenny's question, form W3; he asked to investigate and change nothing, and nothing was changed). Measured 2026-09-02: of 8178 MB of host swap, **5047 MB belongs to two `kvm` processes** — VM 100 (OPNsense, 1606 MB swapped, 1988 MB resident) and VM 101 (Home Assistant, 3441 MB swapped, 2530 MB resident). Both are on the no-touch list. The balloon device is present on both with `free-page-reporting=on`, but QEMU reports `actual=max_mem` on both — neither is giving anything back. Home Assistant's guest reports `total_mem=5864 free_mem=650`: from inside, it has almost nothing spare to return, and `mem_swapped_in=0 mem_swapped_out=0` says the GUEST is not swapping at all. So the guest is comfortable and the host is compensating for it | Advice written for Kenny in `docs/deployment/HOST_SWAP.md`; nothing applied. The root is that 47 GB is promised on a 31 GB machine, and a VM's memory is opaque to the host — it can only swap what looks cold, and a guest's page cache looks exactly like anything else | open — Kenny's call 2026-09-02 |
| F186 | **Three finished features were switched off on the live host, and one of them by me, tonight.** `/etc/homelab/host.toml` is hand-edited, and TOML gives bare keys to the table header above them. The two OPNsense keys added on 2026-09-02 were appended at the END of the file, after the last `[[registry_cache.upstreams]]` table — so they became fields of the lscr.io mirror, serde dropped them as unknown, and `kea` stayed None. The static-address feature has been off the whole time I reported it as working. The file's own comment warns about exactly this trap; it was written after the FIRST time and did not prevent the second. Beside it, `kuma_monitors_file` (T49) and `homepage_services_file` (T51) were never added at all: measured 2026-09-02, `/appdata/uptime/kuma-seeder-config/` is empty and `/appdata/home/homepage-config/services.yaml` is 0 bytes, dated 2026-08-31 — the exact state T51 was built to end. Worse, `load_config` answered ANY parse error with `FileConfig::default()`, so a single typo would have turned every configured feature off at once, in silence | Fixed in code: a file that exists and does not parse is now FATAL with the parser's own message, and every key the daemon will not read is reported by name at startup with the sentence that explains the trap. Three tests, one of them the live file verbatim. The live `host.toml` still needs the four keys moved/added — blocked, the auto-mode classifier refuses to write `/etc` or restart a service, so it is Kenny's to apply | code done; live file open 2026-09-02 |
| F187 | **The Jellyfin busy check is finished, correct, and unreachable — and Jellyfin auto-updates against its own written policy.** `update.rs` only runs the check when a container carries the label `com.homelab.update.busy-check=jellyfin`; measured 2026-09-02, `grep -rn busy-check stacks/` returns nothing — the label exists in the code and in one test mock and nowhere in the fleet. So the fail-closed logic of F31 (`Unknown` counts as busy) has never once been consulted. Beside it a plainer contradiction: `stacks/media/jellyfin/docker-compose.yml:61` says `com.homelab.update.policy=auto` on `image: jellyfin/jellyfin:latest`, while `docs/deployment/UPDATE_POLICY.md:24` puts jellyfin in the **pinned** class — `manual` plus a pinned tag — for the stated reason that a new version can break something that only shows up hours later. Hardware transcoding is exactly that kind of thing | Jellyfin is updated to whatever `:latest` is every night, unchecked and unpinned, and the only document that says otherwise is not the one the code reads. Turning the check on is not a free fix either: the media stack declares no `latch_secrets` and no `env_file`, so the check would find no API key, fail closed, and silently stop Jellyfin updates altogether. Which of the three (label on, policy to manual, tag pinned) is Kenny's call — form pending | deels 2026-09-02 — D100 zette jellyfin op manual met een vastgezette versie; de busy-check blijft onbereikbaar want geen container draagt het label, en dat is bewust (hij zou geen sleutel vinden) |
| T78 | **A pass that laid all 46 "open" findings next to the code.** Started after F36, F38, F39, F56 and F57 each turned out to be repaired long ago while the register still called them open — a register that lies about what is done is worse than no register, because every later decision is taken against it. Result: 28 findings closed with a file-and-line pointer, 7 restated as recorded facts that can never "close" (old disks, a lost database, an archived upstream), 5 narrowed to what genuinely remains, and one new defect found that nobody was looking for (F187) | Honest about method: F86 and F187 I verified myself, command by command, because I act on those. The other 27 rest on a read-only sweep whose every verdict carries a file path and line number — checkable, and cheaper than re-deriving each one here. Rows now say `done — nagemeten: <evidence>` rather than a bare `done`, so the next reader can see what the claim rests on | done 2026-09-02 |
| D96 | **The three switched-off features are on** (Kenny, form V1). `/etc/homelab/host.toml` rewritten with all four keys above the first table header, daemon restarted clean, uptime and home redeployed. Measured immediately after, which is the C7 criterion: `host-monitors.json` now exists and names 13 containers (the check moved 0 → 13), and `services.yaml` is 3210 bytes instead of 0. The OPNsense keys now reach `kea` — though whether that feature should exist at all is Kenny's open question, see F191 | V1, C7 | 2026-09-02 |
| D97 | **Two monitors corrected** (Kenny, form V2, "allebei opruimen"). Monitor 5 `gateway · uptime-kuma` deleted — it pointed at 10.10.10.4:3001, the address Uptime Kuma left in May, while monitor 37 already watched the right one. Monitor 25 renamed `host · synctest` → `host · syncthing`. Verified after the restart: 36 monitors, 64 heartbeats across all 36 in three minutes, monitor 25 reporting UP | F157, F158 | 2026-09-02 |
| D98 | **Three orphan restic repositories purged from Google Drive** (Kenny, form V3, "alle drie" — he overrode the recommendation to keep the third). `mailbox-config` (9.3 KiB, kyu's name until 2026-08-29), `synctest-config` (29 KiB, the pilot's name), `metrics-config` (64.4 MiB, from before D25 split repositories per owning app). 30 repositories remain, every one with a live owner | F174 | 2026-09-02 |
| F188 | **T51 generates a file that another mount hides.** `stacks/home/homepage/docker-compose.yml` mounts `/appdata/home/homepage-config` at `/app/config` (line 26) and then mounts `./services.yaml:/app/config/services.yaml:ro` (line 28) — a hand-written file, read-only, over the same path. So the orchestrator's generated services.yaml has never been visible to Homepage and could not be, even once the config knob was finally set. Measured 2026-09-02: the host file is 3210 bytes written minutes ago, the file inside the container is 5952 bytes dated 2026-08-31 18:39 | The feature is built, tested, ratified, switched on — and structurally unreachable. Which of the two files is the truth is a real question, not a bug: the hand-written one carries grouping and widget decisions the generated one derives from route files. Kenny's call | done 2026-09-02 — D101/D103: de read-only koppeling weg, generator en overlay samengevoegd, live geverifieerd op de pagina |
| F189 | **A check that could only ever answer zero.** `homepage · diensten op de startpagina` ran `curl … | grep -o 'href="http' | wc -l`. Homepage is a Next.js app that draws its tiles in the browser, so the served HTML holds eleven links and every one is a stylesheet, a favicon or the web manifest. Measured 2026-09-02: the page mentions jellyfin, renders fine, and the check said 0 — which is exactly what it said when the page really was empty. Same class as F176, the two checks that woke Kenny at 02:30 | Fixed: it now counts `href:` entries in the file the CONTAINER reads, which is a number that moves — and which is how F188 was found, because that number is 24 while the file the orchestrator writes has 26 | done 2026-09-02 |
| F190 | **I took Uptime Kuma down with a backup file.** Before editing its database I copied it to `kuma.db.bak-2026-09-02` inside `/appdata/uptime/uptime-kuma-config/` — the directory bind-mounted into the container at `/app/data`. The container's startup job chowns everything there; the file was owned by host root, the chown failed, the startup script exited non-zero and Kuma crash-looped: `chown: changing ownership of '/app/data/kuma.db.bak-2026-09-02': Operation not permitted`, `Restarting (1)`. Fixed by moving it to `/root/kuma-backups/`; two minutes of extra downtime, on the one service whose job is to notice downtime | The measured part is worse than the mistake: the SAME shape exists in the orchestrator. `find /appdata -maxdepth 2 -name '*-config' -type d` searched for host-root-owned entries and returned three — `kuma-seeder-config/host-monitors.json` and `homepage-config/services.yaml`, BOTH written by the orchestrator tonight, plus `jellyfin-config/metadata`. They do not break today only because those two containers do not chown on startup. The mechanism to prevent it already exists (`host_owner_uid`, F23) and generated files do not go through it | correction form open 2026-09-02 |
| F191 | **The orchestrator does not need to know the router exists** (Kenny's question at C4, measured rather than reasoned). Four readings, 2026-09-02. (1) A container takes its address statically from its own config — live on CT 114: `net0: … ip=10.10.10.14/24` — and never asks DHCP. (2) The Trusted subnet's DHCP pool is `10.10.10.100 - 10.10.10.200`; containers live on .4–.17, entirely outside it, so OPNsense can never hand one of those addresses to anything else. (3) The reservation list holds 12 entries — phones, laptops, printer, Home Assistant, fileserver, desktop — and not one orchestrator-built container, so H2 has never written a reservation in production. (4) `grep -rn 'firewall/filter' core/ host/ client/` is empty: the code touches only the Kea endpoints, so the `Firewall: Rules` privilege the credential carries is used by no software at all | So the feature buys a cosmetic row in the router's UI, and costs a credential with write access to the router's DHCP and firewall, stored on the Proxmox host, in the host-meta backup and in latch. Kenny's instinct was right. Which way it goes is his — form V4 | done 2026-09-02 — D99: de hele OPNsense-integratie uit de orchestrator, host.toml en credential-bestand opgeruimd |
| D99 | **OPNsense removed from the orchestrator** (Kenny, form V4, after F191 measured that it buys nothing). `core/src/ops/kea.rs` deleted, the `dhcp reservation` step gone from the deploy, `kea` gone from `OpCtx` and `Config`, the two keys gone from `FileConfig` and `KNOWN_TOP`, four tests removed. The F186 fixture keeps the OPNsense keys verbatim on purpose — the trap it demonstrates belongs to TOML, not to those settings. Done on the machine the same day: the two keys out of host.toml, `/var/lib/homelab/secrets/opnsense.cred` deleted, the latch entry gone with the prod re-commit. The `homelab-api` user in OPNsense stays — Kenny uses it elsewhere, and what happens to his credentials is his to decide, not a standing item on anyone's list | V4, F191 | 2026-09-02 |
| D100 | **Jellyfin pinned and manual** (Kenny, form V7). `jellyfin/jellyfin:latest` → `jellyfin/jellyfin:10.11.11`, the version measured from the running server's own `/System/Info/Public`, with the tag confirmed to exist upstream before it was written; `update.policy=auto` → `manual`. The stack and UPDATE_POLICY.md now say the same thing for the first time | V7, F187 | 2026-09-02 |
| F192 | **The ratified update classes and the fleet disagreed in seven places, not one.** F187 found the Jellyfin case; checking the rest found six more. `UPDATE_POLICY.md` opens by saying its classes were decided at the Phase 2 gate (Y1) and the Z1-Z4 deep-dive, so the document is the decision and the labels were the drift. Six gateway apps — traefik, cloudflared, crowdsec, grafana, loki, goaccess — carried `auto` while the document puts them in the MANUAL class, on the container whose breakage takes every other name down with it. And `uptime/promtail` carried no label at all, which is the exact state the document's first paragraph says it exists to end | All seven corrected to match the ratified classes — not a new decision, an unimplemented one. Two things remain open and are NOT corrected because they would be new decisions: `downloader/gluetun` is `manual` but still on `:latest` (the pin the 'pinned' class asks for; its image reports its own version as literally `latest`, so I did not invent one), and the four `registry/cache-*` mirrors carry no policy label because the document never names them | deels open 2026-09-02 |
| F193 | **Two of my own checking scripts had the same blind spot, and the second one after I had already seen the first.** Comparing labels against the policy I keyed on the compose file's DIRECTORY name, so a file holding two services only ever had its first one checked — `gluetun` lives in `qbittorrent/docker-compose.yml` and was invisible. I noticed that, said so, and then wrote the verification pass the same way, which reported "0 afwijkingen" while `goaccess-report` sat there on a different policy. Separately, a replacement asserted on a SUBSTRING (`com.homelab.update.policy=auto`) and then replaced an exact LINE that did not match because the label is quoted — the assertion passed, the replace did nothing, and my script printed six success lines having changed no file | Rewritten to walk every service block: 46 services checked instead of 23 files. The lesson is the session's own: a script that prints success without reading back what it wrote is the same class as a check that cannot move (F189). Verification now re-reads the files | done 2026-09-02 |
| D101 | **The front page merges instead of choosing** (Kenny, form V6, "Samenvoegen"). The generated list supplies the services; `stacks/home/homepage/services-overlay.yml` supplies what a route file cannot know — icon, description, widget, display name, group. Joined on `href`, which both sides already carry: an exact key, chosen after measuring that display-name matching would have guessed wrong eight times out of twenty-six on the real pair of files. His 24 entries migrated with their comments intact; the two with no route (the deliberate `http://` link to kp-soft and the deep link into Grafana for Loki) are kept rather than dropped. A routed service with no overlay block still appears, under its stack name — so a new service reaches the page by itself. The read-only mount that hid the generated file is gone and `services.yaml` is no longer a stack file | V6, F188 | 2026-09-02 |
| D102 | **The live tiles are derived, not written** (Kenny, 2026-09-02: "kan dat niet automatisch?" about the widgets that showed what was streaming and how many films the library held). Measured rather than assumed, and all three parts of a Homepage widget turned out to be readable. **Type** = the route name for seven of nine; the two that differ (`seerr` → `jellyseerr`, `paperless` → `paperlessngx`) are named in `KNOWN_WIDGETS`. **Address** = the backend the Traefik route already forwards to — which is also the address a widget MUST use, because through the public name it meets Cloudflare Access and gets a login page; `entries_from_route` deliberately dropped that field before, which is why widgets were impossible. **Key** = read from the application itself at deploy time: `jellyfin.db` for Jellyfin (the same reading its busy check already does), `config.xml` for sonarr/radarr/prowlarr, `config.yaml` for bazarr, `settings.json` for seerr. That is also the only reading that cannot go stale — F32 was a key copied into an `.env` that had been dead for an unknown length of time while everything reported fine | The seven widget blocks are gone from the overlay, which now holds only group, display name, icon and description. Grafana and Proxmox keep theirs by hand: those take a username and password no application hands out. Keys are read through `ctx.exec`, NOT the tracing executor — standing rule 10, a hash may reach the transcript and plaintext never | 2026-09-02 |
| D103 | **The front page, arranged** (Kenny, 2026-09-02: "zet media weer eerst want die gebruik ik het meest"). Overlay rewritten: 25 described services in five groups, Media first, every one with an icon and a Dutch line. Two mechanisms added because the real route list needed them — a second router to the SAME address is not a second tile (`almanac` and `almanac-block-metrics` share a door), and `hide: true` keeps a route off the page without deleting it (`uptime-kuma-alt` is a second name for a service already listed, and `homepage` is the page you are looking at) | D102, F195 | 2026-09-02 |
| F196 | **Every credential in the machine's keyring vanished, and latch was one of them.** Measured 2026-09-02 after `latch cat` began failing with `no key for project 'stacks' (prod)`. `latch state` says the PAT is MISSING and all three projects have no key, with `cred file: none` — so the keyring was the only copy. The keyring is `~/.local/share/kwalletd/kdewallet.kwl`, served by BOTH `kwalletd6` and `ksecretd`; it was rewritten at 11:42 and is now 2736 bytes holding one entry, while `kdewallet_attributes.json` from 2026-08-30 still describes fifteen. The whole `Secret Service` folder is gone — with it the GitHub tokens, the copilot token, IntelliJ and zed. Not a latch fault: latch lost its keys the way four other programs did | **No data lost.** The ciphertext is intact — 49 files under `~/.latch/repo` plus the `kennypassenier/secrets` remote — and the plaintext values are still deployed on the containers (homepage 12 lines, supersync 14, paperless 4). What is gone is the ability to DECRYPT on this machine. Recovery is Kenny's: `latch clone` from a machine that still has the keys, or re-mint and re-commit from the deployed files, both of which need his GitHub PAT. The design question — a store whose only copy lives in a keyring that an OS can empty — belongs in the latch conversation | done 2026-09-02 — hersteld (D104) en de escrow staat compleet op drie plekken (D105); geen dataverlies |
| F197 | **Two defects in the merge that every test agreed with.** Found by rendering the real page locally before deploying it, not by the suite. (1) The overlay's lines were emitted at four spaces where a service's fields belong at eight — invalid YAML that reads fine to a human, and every assertion had asked whether a line was PRESENT rather than where it sat. (2) Tiles within a group followed whichever stack sorted first, so qBittorrent (stack `downloader`) stood above Jellyfin (stack `media`) inside the Media group Kenny had just asked to put first | Both fixed, both now asserted on POSITION rather than presence: one test measures the indent of every `icon:`/`description:`/`widget:` line, the other that Jellyfin precedes qBittorrent. The lesson is the session's own in a new costume — `contains()` is the check that cannot fail | done 2026-09-02 |
| F198 | **The router backs itself up and nobody was watching.** The OPNsense session installed `os-gdrive-backup` on 2026-09-02: every night at 01:00 (plus up to an hour of jitter, verified in the plugin's source) it uploads an encrypted copy of the router's configuration to `homelab-backups/OPNSense-backups`. That backup is made outside this suite and should be — VM 100 is no-touch. But nothing noticed if it stopped, and a backup nobody watches is one you find broken on the day you need it | Kenny chose the nightly fleet check over a Uptime Kuma push monitor (form O1): the push monitor would have needed a script on the host calling in on a timer, and a separately maintained file on a machine is what he ruled out that same morning. So `LiveFacts` gains `watched_backups`, the host reads the newest ModTime through the rclone remote that already exists for the restic repositories, and `evaluate_watched_backups` returns Broken past 26 hours. Three distinct sentences on purpose: too old, nothing there at all, and could-not-be-listed mean three different things. Declared in `host.toml` as `[[watched_backups]]` (form O2) | 2026-09-02 |
| F199 | **~~The deployed `.env` files were in no backup at all~~ — WRONG, corrected the same hour.** I claimed the deployed secrets existed only at `/opt/<stack>/<app>/.env` inside containers, outside every backup, and rescued all thirteen into the vault. Then I read the deploy code I had been editing all day: `deploy.rs:1046` has always written a vault copy beside the push — `{state_dir}/secrets/{stack}/{app}.env` — and `deploy.rs:1056` reads it back so a wiped container gets its `.env` again. Verified afterwards: all thirteen vault copies existed already and are byte-identical to what I 'rescued' (md5 compared per file). The host-meta backup has carried `/var/lib/homelab/secrets` to Drive all along | So the secrets were never one keyring away from gone, and the measure form L4 proposed as new was already built and working. What the episode DID establish: the `/opt` copy is genuinely unbacked, which matters only if the vault copy is ever lost too, and the rescue directory is now a redundant plaintext copy of every secret — kept only because Kenny approved L8, and worth removing on his word. **The fault under the fault is mine and is the session's own shape: I measured what was MISSING from one place and never measured what was PRESENT in the other, in code I had open** | corrected 2026-09-02 |
| F200 | **The front page rendered a YAMLException, live, and the third bug of the same family.** After v3.37.0 shipped the merge, Homepage refused the generated file: `bad indentation of a mapping entry` and then `found unhashable key`. Two causes. The indentation one was F197, already fixed but not yet on the host — the running binary was v3.36.0, tagged before that fix, so a green suite and a broken page were both true at once. The second was new: `key: {{HOMEPAGE_VAR_PAPERLESS}}` emitted UNQUOTED, and a YAML value starting with `{` is a flow mapping, so the whole document fails to parse. Kenny's hand-written file had the quotes; the generator did not | Fixed, and the fix that matters is the test: `the_generated_page_is_valid_yaml` parses the output with a real YAML parser and asserts its shape — a sequence of one-key groups each holding a list of services. Three defects in this generator shipped past a green suite because every assertion asked whether text was PRESENT. A parser sees what `contains()` cannot, and it would have caught all three. `serde_yaml` added as a dev-dependency of core only; the library itself still parses nothing | done 2026-09-02 |
| D104 | **Latch recovered by re-minting — and re-minting was not necessary.** Kenny did `latch login`; the rest was mine. The repo was copied to `~/.latch/repo.backup-2026-09-02` first (299 files), thirteen `.env` files taken from the host vault, placed in a working tree after checking `git check-ignore`, `latch commit --env prod` minted key generation 1, `latch push` published, plaintext shredded. Verified before pushing: `latch cat home/homepage/.env` returns 12 lines and md5-matches the vault. All thirteen read back today | **What it cost, stated plainly.** The old key was still in the persistent kernel keyring (F201) and I never looked there, so the version history of `stacks/prod` — `latch history`, `latch rollback` — is now unreadable. The current values are fine; they came from the vault and verify against it. Seven entries were removed in the same commit (`kyu/http-switchboard`, `kyu/kyu-runner`, `media/radarr`, `media/sonarr`, `productivity/vikunja`, `platform/uptime-kuma`, `opnsense/api`) and I justified that partly with "they were already undecryptable", which was wrong. The conclusion survives anyway, because I also checked each one against the live system independently: kyu reads its env from `/appdata`, media/vikunja/platform carry no `latch_secrets`, opnsense was removed that day. Nothing operational was lost — but the reasoning that said so was built on a store I had not looked at | 2026-09-02 |
| D105 | **The latch key has a second and third home** (Kenny made it, 2026-09-02). `latch key backup` wrote a passphrase-encrypted escrow holding `key:stacks` and the `pat` — the two credentials this homelab actually needs. Copied to `/var/lib/homelab/secrets/` on the Proxmox host (sha256 compared, identical), and a host-meta backup run immediately after put it offsite: snapshot `2ba0bb9f`, verified by name with `restic ls latest`. Kenny's own copy in his home directory was left alone — his files are his. The passphrase is not on any of these machines and is not known here | The key now lives in three places of different kinds: this machine's keyring, the host's vault, and Google Drive inside restic. F201's failure needed all three to go at once, which is the point | 2026-09-02 |
| D106 | **The v1 latch archive is gone from the secrets repo** (Kenny, form P1). `latch project remove homelab --yes` deleted 13 ciphertexts across one env and pushed. Deliberately WITHOUT `--purge-keys`, which would make the git history unreadable forever — though here that distinction is academic: no `homelab` key exists on any machine any more, so the history is already unreadable by anyone. The tool's warning about kept keys is generic and does not apply | Measured before and after: `latch verify` went from 24 unreadable of 38 to **11 of 25**, and all thirteen live `stacks` files still read back. The remaining ten are latch-rs's own test fixtures, which that session decides about, plus one almanac `dev` file. A verify screen that is mostly red is a screen nobody reads — the same argument as lens 12 of the expert round | 2026-09-02 | Nagemeten na een tegenwerping van de Almanac-sessie, die dacht dat mijn hermint de dertien v1-bestanden in LATCH2-formaat had herschreven: dat klopt niet. `git show --stat d5b24b3` toont `Bin 11689 -> 0 bytes` — verwijderd, niet herschreven; `latch commit --env prod` kan bestanden van een ander project niet aanraken. Wel een rest gevonden: `latch project remove` belooft "all envs' ciphertexts, the local link and marker" en laat `homelab/manifest.json` staan, terwijl `latch project list` het project al niet meer toont. Doorgegeven aan de latch-sessie |
| M1 | **The two correction-form measurements are made, and both passed.** Rule 29 keeps a correction open until its measurement actually happens, so this row exists to close them rather than let them fade. **C7 (form F186, config keys):** the criterion was the next daemon start after the repaired `host.toml`. It fired on the first start of v3.36.0 and named exactly the two OPNsense keys as unread — which was correct, V4 had just removed them — and after they were taken out the daemon starts with no warning at all. **K7 (form F190, file ownership):** the criterion was the next deploy of the uptime and home stacks. Measured after v3.38.0: `/appdata/home/homepage-config/services.yaml` and `/appdata/uptime/kuma-seeder-config/host-monitors.json` both carry `100000:100000`, matching their directory, where both were `root:root` before; CT 107 and CT 115 both running | The third file K3 named, `/appdata/media/jellyfin-config/metadata`, is deliberately NOT covered: it is a directory of unknown origin that this orchestrator does not write, so the measure has nothing to say about it. Naming that out loud rather than letting a passing measurement imply more than it checked | done 2026-09-02 |
| F202 | **Four other latch projects are still in the state `stacks` was in this morning, and no machine can escrow them.** The backup skipped twelve keys the repo expects — almanac, homelab, hub-clients, latch-rs and their per-env variants — with the advice "back up from a machine that holds them". Measured: no machine does. They went with the keyring at 11:42 (F201), and `latch verify` confirms it from the other side — `no-key almanac/dev/.env.enc (needs 'almanac' gen 1)` | Those projects' ciphertext is undecryptable and their own sessions face the same recovery this one just did: re-mint and re-commit from wherever the plaintext still runs. **Corrected 2026-09-02 by the newsflash session: one of them was NOT an archive.** `hub-clients` had only ever one environment, named `dev`, and it carried the KYU_TOKEN its live systemd unit injects — that unit crash-looped from 11:42, hit StartLimitBurst at 11:44:30 and stayed stopped for about two hours. I had written that no running service lost a credential it needed. That was an inference from the environment's NAME, not a measurement of its consumer, and it went out in a handover Kenny pasted into other sessions. A fleet-wide sweep afterwards found every managed container's `EnvironmentFiles` present and every service running — and would never have caught this one, because newsflash runs on the workstation and not in the fleet I swept. Not this project's work, but they should be told rather than discover it. `key:stacks.prod` was also listed as missing and is NOT a problem: the prod files decrypt under the project key, all thirteen read back, and `verify` reports nothing against `stacks/prod` — the repo's expectation of a per-env key is stale | open — for the other projects' sessions 2026-09-02 |
| F201 | **~~The keyring did not lose things because it is unsafe~~ — the diagnosis was about the WRONG STORE.** Corrected 2026-09-02 by the Almanac session and verified here with `keyctl show`: latch 2.2.0 builds against `keyring` 3.6.3 with the `linux-keyutils` backend, so its keys live in the **kernel** keyring as `keyring-rs:key:<project>@latch` — not in the KDE wallet at all. `kwallet-query -l kdewallet -f latch` answers "The folder latch does not exist!", and it never did. Everything I measured about the wallet is true and cost Kenny his GitHub, copilot, IntelliJ and zed credentials; none of it explains the latch keys. A kernel session keyring empties on a session change, which fits a workstation that was mid-upgrade, and I never looked at it | **The consequence is mine and it is not cosmetic: the keys were retrievable the whole time.** `keyctl get_persistent @s` relinks `_persistent.1000`, which still holds all four. Almanac recovered that way instead of re-minting. I re-minted `stacks` (D104) without ever checking that store — see D104 for what that cost. The durable half of the original lesson stands unchanged: a keyring protects against being READ, not against being LOST, and the persistent kernel keyring expires and does not survive a reboot, so it is a rescue hatch and not durability | corrected 2026-09-02 |
| F194 | **Everything built today is in the repo and none of it is on the host.** The running daemon is v3.35.0; the OPNsense removal, the config-key linting, the no-touch hardening, the ownership fix and the homepage merge all need a rebuilt binary. Found the honest way, by deploying the home stack and reading the result: the merge had not applied and every service still sat under its stack name. My first reading blamed the overlay's path — that was also true and is fixed — but the larger reason is that the code was never on the machine | The T51 merge, the K4 ownership fix and D99 are UNVERIFIED live until `make release` + `homelab release-update` ship a new host. The measurement K7 promises (three files owned by the container after the next deploy of uptime and home) cannot be taken before that | done 2026-09-02 — host op v3.38.0; OPNsense-verwijdering, config-lint, no-touch-verharding, eigenaar-fix en de homepage-samenvoeging draaien allemaal live |
| F195 | **The route parser threw away the one field a widget needs.** `entries_from_route` read the routers' hostnames and deliberately dropped the `services:` backends, with a test asserting it — for a good reason, that the front page must show the names Kenny types rather than internal addresses. But a Homepage widget must call the LAN address: through the public name it meets Cloudflare Access and gets a login page instead of an answer, which Kenny had already written in his own file. So the rule that kept links clean also made every live tile impossible, and nobody noticed because the widgets were being typed by hand instead | Fixed: `Entry` carries `backend` and it is used for widgets only. The original rule is now asserted where it belongs — no `href:` or `siteMonitor:` line may contain an internal address — instead of being enforced by not parsing the field at all. A restriction defended at the parser looks identical to one defended at the output, right up to the moment you need the data | done 2026-09-02 |
| F165 | **Four of the gateway's five backup repositories are EMPTY — the edge has almost no backup.** Counted 2026-09-01 across all 33 repositories: `crowdsec-config` 0 snapshots, `goaccess-config` 0, `grafana-config` 0, `loki-config` 0. Only `traefik-config` has one, because it runs first and the step aborted on `cloudflared-config` right after it — F154, every night, for as long as that guard has existed. So the CrowdSec database holding the bouncer registrations, Grafana's dashboards and datasources, and Loki's configuration have never been saved once. The fleet check said "gateway has never been backed up" and that was literally true, five times over. Found by the retention count Kenny asked for in form R8 — a question about numbers being too high answered by four numbers being zero | The cause is fixed: `no_data: true` on cloudflared is deployed, so `owner_groups` no longer creates that repository and the run reaches the four behind it. Deliberately NOT run by hand tonight — the gateway backup stops traefik, crowdsec, loki and grafana while it works, and the nightly run at 04:00 does it without anybody watching the edge go quiet. Measurement M-F165 checks it in the morning | fixed 2026-09-01, awaiting proof |
| F164 | **Nine of ten containers report no container metrics, and the guard that should install them has been reporting success the whole time.** Measured 2026-09-01: `/opt/cadvisor/docker-compose.yml` present on 10 of 10 managed containers, cadvisor actually running on 1 (CT 113). The guard read `if push_content(...)`, which is true only when the content DIFFERS — so the first run wrote the file and started cadvisor, and every run since found the file identical and skipped the start. A cadvisor that never came up, or came up once and later stopped, could not be repaired by any number of deploys. Starting it by hand on CT 115 took one second and worked first time; nothing was broken except that nobody asked. The consequence is quiet by design: the HostDown rule watches the node job, not this one, so an empty container panel and a working one look identical (the same blind spot H4 was written for) | Fixed: the compose-up runs unconditionally — `docker compose up -d` is idempotent, so it costs a no-op and buys self-repair. The guard now guards the SERVICE rather than the file. Test: `h4_cadvisor_is_started_even_when_its_compose_file_is_unchanged`, and the first version of that test passed against the unpatched code because its mock returned a digest with the filename appended — a test that proves nothing is the same fault one level up, so it was re-checked against HEAD before being kept | fixed 2026-09-01 |
| F163 | **The syncthing pilot has been synchronising nothing, and everything above it was green.** Measured 2026-09-01 while writing its service checks (T68): zero folders, zero devices, zero connections, 120 KB on disk, and one folder entry in `config.xml` with `id="" label="" path=""` — a template stub. `/rest/db/status?folder=obsidian` answers "no such folder". Meanwhile `/rest/noauth/health` returns `{"status": "OK"}`, the container reports healthy, and Uptime Kuma monitor 15 asks exactly that endpoint and is green. The M4 pilot proved the DEPLOY loop end to end and was read ever since as proof the service worked. Claude asserted twice on this day — in a form and in `CLAUDE.md` — that CT 108 holds Kenny's Obsidian vault sync; it holds a syncthing with nothing in it | Kenny chose to set it up after all (form S10). Done and verified 2026-09-01: syncthing 2.1.3 installed on the desktop as a user service, the hub added as a peer both ways, folder `obsidian-vault` shared, and the vault synchronised — 318 files, 10.4 MB, `inSyncFiles == globalFiles`, `needFiles: 0` on BOTH sides, and 265 markdown files present under `/appdata/syncthing/syncthing-config/obsidian-vault`. Staggered versioning on the hub keeps every overwritten and deleted version for a year, which is the half that protects notes rather than copying them. The phone is the one device still to add. Checks written that ask the application instead of the port, with the health endpoint kept and labelled as the reading that meant nothing | fixed 2026-09-01 |
| F162 | **A destroy left the stack's Grafana dashboard behind, and reported that it had removed it.** The step ran a bare `rm -f /opt/gateway/grafana/…/homelab-drill.json` — on the PROXMOX HOST, where that path does not exist. `rm -f` on a missing path exits 0, so the step reported "changed" every time and removed nothing for as long as it existed. The route removal beside it does it correctly, through `pct exec 104`; the discovery file beside THAT is correctly a bare `rm`, because it really is on the host. Three near-identical lines, two different machines. Found 2026-09-01 by the destroy drill (Kenny, form P1) — the throwaway stack was destroyed and its dashboard was still on the gateway afterwards | Fixed: the removal runs through `pct exec <gateway_vmid>`, like the route. Test: `c2_destroy_removes_the_dashboard_on_the_gateway_not_the_host`, which also asserts the discovery file is NOT routed through the gateway — the contrast is the part worth pinning | fixed 2026-09-01 |
| F161 | **A file removed from the repository is never removed from the container.** The thirteen committed generated dashboards were deleted from `stacks/gateway/grafana/provisioning/dashboards/` and all thirteen are still on CT 104 after two deploys — so `homelab-gateway.json` now exists twice with the same Grafana uid, once from the repository's directory and once from the generated one. The garbage collection works per APP DIRECTORY (D3), not per file: an app that disappears from the intent is cleaned up, a file that disappears from an app is not. Found 2026-09-01 while verifying the B3 change landed | Fixed the way Kenny chose (form H2b): every deploy names the files that are on the container and not in the repository, and changes nothing. `homelab prune-orphans stacks/<name>` removes them, after the same typed confirmation a destroy asks for and the same safety gate. Two steps because deleting is the irreversible direction and a deploy runs when nobody is watching | fixed 2026-09-01 |
| F160 | **A route cannot be removed without failing the deploy that removes it.** The gateway's own service check counts Traefik's enabled routers with `never_decreases`, and removing the `go.kp-soft.dev` router (form B2c) made it fall 29 → 28. The check is right — that is precisely the reading that would have caught routes vanishing by accident — and it cannot tell a deliberate removal from an accidental one. The deploy was recorded incomplete; the next one passed, because the baseline had moved. Found 2026-09-01 on the first field run of the checks written that same evening | Left as it is for now, and the real answer is T69: Kenny's own reading (form H1) is that the deploy window should ASK — two buttons, allow or stop — rather than either failing or being silenceable by a flag. Until that exists, a deliberate removal costs one red deploy and a second one that passes, which at least forces somebody to look | parked — T69 |
| F159 | **Grafana was down for four minutes because a bind mount cannot be nested inside a read-only bind mount.** The B3 change added `./dashboards-generated:/etc/grafana/provisioning/dashboards-generated:ro` next to the existing `./provisioning:/etc/grafana/provisioning:ro`. Docker has to create the mountpoint inside the directory the first line just mounted read-only, and fails at container start: `create mountpoint ... : read-only file system`. `docker compose config` validates it, `homelab plan` validates it, and nothing knows until runc tries | Fixed, and simpler than the original design: one mount instead of two. The generated dashboards live in `provisioning/dashboards-generated/`, a sibling of the committed `dashboards/`, so the repository owns one directory and the deploy owns the other — which is what form B3 actually asked for. Verified live: `homelab-gateway.json` written to the new directory at 20:05 and served by Grafana | fixed 2026-09-01 |
| F158 | **A monitor named after a stack that no longer exists.** `host · synctest` pings 10.10.10.8, which is `108-app-syncthing` — the pilot stack was renamed and the monitor was not. It reports UP, truthfully, about a machine whose name in the dashboard is wrong. Harmless today and exactly the shape that makes a dashboard stop being trusted | Open, and part of T66: nothing registers or deregisters an Uptime Kuma monitor, so the set drifts away from the fleet one rename at a time. Found 2026-09-01 by reading the monitor table while checking whether removing `go.kp-soft.dev` would break a watch (it does not — nothing watched it, which is its own finding) | done 2026-09-02 — D97: monitor 25 hernoemd naar `host · syncthing`, meldt UP |
| F157 | **Uptime Kuma has been reporting itself down for eight hours.** Monitor 5, `gateway · uptime-kuma`, points at `http://10.10.10.4:3001/` — the address Uptime Kuma had before it moved to its own container this morning (D68). It has been DOWN since 11:43:59 on 2026-09-01, measured from its own heartbeat table. A watchman reporting a failure that is only its own move is how a dashboard trains its reader to ignore it | Open, and part of T66. The monitor set is maintained by hand while the fleet is maintained by the orchestrator, and a move that the orchestrator performed left the watch behind. Nothing alarmed, because a permanently-red row reads as furniture | done 2026-09-02 — D97: monitor 5 verwijderd; 36 monitors, 64 hartslagen in 3 minuten na de herstart |
| F156 | **The TUI reaches nine of the twenty-five operations the host can perform.** Measured 2026-09-01 by listing every `Command::` variant the CLI sends against every one the TUI sends. The TUI can deploy, run doctor, read and write settings, enable/disable a stack, self-update the host and exec. It cannot back up, restore, update, destroy, run the fleet check, apply guards or resources, patch, read incidents, adopt a native service, replicate ZFS, list or build templates, or forget a stale record — sixteen operations that exist only on the command line | Six of the sixteen are wired, the ones Kenny reaches for daily (his form T1): backup, update, restore, guards, fleet check and incidents — each with a keybind, a palette entry and a progress window, and restore behind a typed confirmation because it overwrites live data. That took the TUI from nine of twenty-eight to fifteen. The other ten are written down as command-line-only WITH a reason, and a test holds both halves: a new `Command` variant fails the suite until it is either wired into the TUI or listed with its reason, and the excuse list may not contain a command that is gone or now reachable. Neither answer is wrong; leaving the question unanswered is. Sabotage-verified with an invented command. Test: `every_host_operation_is_reachable_or_deliberately_not` | fixed 2026-09-01 |
| F155 | **The wizard could not set the flag the wizard's own stacks need.** `no_data` was added on 2026-09-01 because an undeclared empty directory stopped the gateway's entire backup — and it was reachable only by editing YAML by hand, so the wizard would have kept producing exactly the stack that caused it. Kenny caught it in the same breath as the fix: "zet voor die extra flag wel de benodigde stap bij in de wizard" | The wizard has a STORAGE step: one row per `/appdata` directory the stack will get, toggled between "keeps files" and "keeps nothing", and the answer reaches the manifest. The paths are previewed from the same templates the scaffold copies, and the test holds those two to the same set — a preview that drifts would put the answer on a path that does not exist and the flag would silently do nothing. Test: `the_wizard_can_declare_an_app_that_keeps_nothing` | fixed 2026-09-01 |
| F154 | **One empty directory stops a whole stack's backup.** The gateway has six restic repositories; `cloudflared-config` is empty (F12 — the tunnel config lives only in the Cloudflare dashboard), the empty-snapshot guard correctly refused it, and the step aborted before grafana, loki and goaccess were ever attempted. So the fleet check's "gateway has never been backed up" was true, and the cause was one directory that has nothing to hold. Found 2026-09-01 by running the backup the fleet check asked for | Fixed the way Kenny chose (form B4), which is better than what I proposed: a `no_data: true` flag on the storage entry says the app keeps nothing on disk. It then gets no restic repository at all, so there is nothing for the empty-snapshot guard to refuse and nothing that can stop the rest of the stack — and an undeclared empty directory stays exactly as alarming as it should be. The declaration is held to: a `no_data` path that is not empty fails the backup with a sentence naming it, because by declaration nothing in it is being saved. Tests: `a_path_declared_empty_is_not_backed_up_and_must_really_be_empty` and `e3_never_restores_into_a_path_declared_empty` — the second added after the live gateway deploy on 2026-09-01 showed the flag reaching the backup and not the auto-restore: E3 walked the storage list directly, asked Google Drive about `cloudflared-config` on every deploy, and would have restored a stale snapshot into a directory whose whole point is that it stays empty. Test: `e3_never_restores_into_a_path_declared_empty` | fixed 2026-09-01 |
| F153 | **Every backup this fleet has ever taken ran without a restic cache.** restic finds its cache through `$XDG_CACHE_HOME` or `$HOME`; a systemd service has neither, so each call printed `unable to open cache: neither $XDG_CACHE_HOME nor $HOME are defined` and then fetched the whole repository index from Google Drive again. The gateway has six repositories, so six index fetches per backup — which is why a gateway backup looks stuck. The line was in every log and read as noise | `RESTIC_CACHE_DIR=/var/lib/homelab/restic-cache` is part of the command itself rather than the unit file, so it holds for every caller and is testable. 54 GB free on the host root; the cache is index and metadata only. Test: `every_restic_call_names_a_cache_directory`, sabotage-verified | fixed 2026-09-01 |
| F152 | **The committed disaster-recovery runbook was stale in every way a document can be.** `docs/DR_RUNBOOK.md` — the file somebody opens when the host is gone — still said "Restic repos per stack", still listed `gateway` and `cloudflared` as "LEGACY (v1 manifest, not deployable by v2)", and predated five of the thirteen stacks. F107 repaired the generator's prose weeks ago; the artefact was never regenerated, so the fix reached nobody. 140 lines of diff against a fresh generation | Regenerated and committed, and now held there: a test compares the committed file against a fresh generation and fails on any difference, so a stack file cannot change without somebody reading the runbook diff. Test: `the_committed_runbook_matches_a_fresh_generation` | fixed 2026-09-01 |
| F151 | **The runbook tells you to `homelab deploy` a native stack.** kyu and almanac run systemd units, not compose apps: they are adopted, and their binaries come from each service's own release. The document listed `apps:` as empty for both and then gave the compose command anyway — harmless while everything works, and precisely the wrong instruction at the moment it is read. Found by the P1 drill: regenerating the runbook and reading it | The line is derived from the stack rather than templated: an empty app list gets the adopt path and says where the binaries come from. Test: `h17_runbook_generator_structural_snapshot` | fixed 2026-09-01 |
| F150 | **The one line anybody copies in a disaster still said `<stack>-config`.** F107 moved restic repositories to one per owning app and fixed the runbook's prose — two lines above a shell block that kept exporting the old shape. Worse, the test pinned it: `assert!(doc.contains("homelab-backups/<stack>-config"))` passed on every run, so the wrong instruction was not merely surviving, it was protected. Found by the P1 drill: regenerating the runbook and reading it, which had not happened since the fix | The export matches the prose, and the test now asserts the ABSENCE of the old shape as well as the presence of the new one. The wider lesson is the drill's: a document that is regenerated and never read is not a document. Test: `h17_runbook_generator_structural_snapshot` | fixed 2026-09-01 |
| F149 | **Every generated Grafana dashboard has been written into a directory nothing reads.** T2 renders one dashboard per stack and pushes it to `grafana_dashboards_dir`, which host.toml still sets to the v1 path `/opt/grafana/provisioning/dashboards`. Grafana mounts `/opt/gateway/grafana/provisioning` — a different directory. Seven files sit in the dead one, written as recently as today, and two of the three compared differ from what Grafana actually serves. The step is best-effort by design, so its failure mode is a log line saying "this stack has no generated dashboard yet", forever. Found while writing the gateway's own checks (P2) | Fixed the way Kenny chose (form B3): the generated dashboards get their own directory and their own Grafana provider, so the repository's committed dashboards and the deploy's generated ones never write the same file. The thirteen committed generated copies are removed — the deploy is the only writer now, which is what makes the question answerable at all. And the question is asked: the fleet check gains `dashboard_provisioned`, which asks GRAFANA over its API which generated dashboards it actually holds, rather than asking the deploy whether it wrote a file. Tests: `a_dashboard_grafana_never_received_is_reported` | fixed 2026-09-01 |
| F148 | **The GoAccess report is frozen at the moment the container started.** `--real-time-html` writes `index.html` once and then pushes updates over the websocket, and the websocket cannot reach the browser: the page asks for `ws://go.kp-soft.dev:7880` and only :443 comes through the tunnel. Measured 2026-09-01: access.log live at 17:51, index.html last written 14:35. The comment in the compose file claims the opposite — "GoAccess rewrites the file continuously" — which is my own wrong assumption written down as fact next to the fix for F142 | Fixed by removal, the way Kenny chose (form B2c): the public route for `go.kp-soft.dev` is gone and the websocket points at `ws://10.10.10.4:7880`. The cause was the tunnel itself — a page served over https may not open an insecure websocket, and GoAccess pushes every update over exactly such a websocket. On the LAN the page is plain http, the websocket is allowed, and GoAccess does what it was built to do. The report now lives at `http://10.10.10.4:7881/` and nothing about it is on the internet: one hostname fewer, one Access policy fewer, and the whole websocket question settled by removing a moving part instead of adding one. The Cloudflare-side ingress still has to be deleted by hand — recorded as manual intervention 9 | fixed 2026-09-01 |
| F147 | **The reconciliation pass called a healthy stack broken.** It checked that a container named after each app was running. An app is a compose PROJECT, and a project's services carry names of their own: `stacks/registry/registry` runs cache-dockerhub, cache-gcr, cache-ghcr and cache-lscr, and not one of them is called `registry`. Found by the fleet sweep, which is the only reason it surfaced — that stack had not been redeployed since the check shipped | Asks compose per app directory, the same question `verify health` already asked. Which also revealed that reconcile can no longer be the step that catches a stopped app, because verify health gets there first: its unique value is the case where an earlier step reports success and did not act, and the test no longer pins which step reports it. Test: `s2_reconcile_catches_a_container_that_does_not_match` | fixed 2026-09-01 |
| F146 | **The write probe reported a distroless image as unable to write.** cloudflared has no shell, so `docker exec sh -c` fails before it ever reaches the directory — and the probe read that as a refusal. The same mistake as counting a 403 as healthy, in the opposite direction: a check that cannot RUN says nothing about the data. The rule was already written into the service-check module and simply not applied here | Unprobeable is now reported as unknown and never as failure, in the same words: "not assumed to be fine". Test: `storage_the_app_can_write_says_nothing` | fixed 2026-09-01 |
| F145 | **Four attempts at inferring which user writes a service's data, four confident wrong answers in one afternoon** — each printed as a `chown` to copy. The shapes: `Config.User` empty with `PUID` (syncthing, the *arr suite); `Config.User` = the name `nobody` (prometheus, alertmanager); `Config.User` empty with a root exec because the entrypoint drops privileges afterwards (postgres, php, actual); and a plain number (loki, the only easy case). The third was found only when a sweep over the whole fleet stopped on three stacks at once — which is the signal that the approach was wrong rather than incomplete | Stopped inferring. The running process is read from `/proc`: its uid is unambiguous and it is the process doing the writing. All four shapes collapse into one reading and a fifth needs no special case. Verified against the fleet first — postgres 70, supersync 1001, prometheus 65534, each matching its directory. One test replaced two, since there is no separate PUID path left to exercise. Test: `storage_the_app_cannot_write_fails_the_deploy` | fixed 2026-09-01 |
| F144 | **The ownership check was confidently wrong a third time, on a third shape.** `Config.User` is a free-form string and prometheus's is the NAME `nobody`. The check parsed it as a number, failed, defaulted to root, and demanded a chown that would have broken a directory correctly owned by 65534. Four shapes have now turned up in one fleet — a numeric uid, an empty field, a `PUID` environment variable, and a name — and every one of them arrived as a surprise | The container is asked (`docker exec id -u`) whenever the field is not numeric, because only it can resolve its own user name. Fourth test. The wider point, recorded because it is uncomfortable: this check was written in answer to Kenny asking that the permission class be closed, and it has now been the source of three faults of exactly that class — each caught only because a deploy stopped and I looked instead of running what it printed. Test: `storage_the_app_cannot_write_fails_the_deploy` | fixed 2026-09-01 |
| F143 | **The ownership check I shipped this afternoon was confidently wrong on the first stack it met.** It read `Config.User`, which is what the compose file asked docker for. The linuxserver.io images — the *arr suite, syncthing, most of this fleet — start as root and drop to `PUID` themselves, so `Config.User` is empty and `docker exec id` says root while every file they write belongs to 1000. The check told me to chown syncthing's configuration to root, which would have broken it. Caught only because the deploy stopped and I looked at the directory rather than running the command it printed | `PUID` now wins over `Config.User` when present, because it is the one that describes what ends up on disk. Third test added. The wider lesson is the one from this whole day: a check that is confidently wrong is worse than no check, because its output reads as an instruction — and this one was mine, written in answer to Kenny asking that the class be dug out. Test: `storage_the_app_cannot_write_fails_the_deploy` | fixed 2026-09-01 |
| F142 | **`go.kp-soft.dev` was routed at a websocket port and had never worked.** GoAccess's `--port=7880` is its WebSocket server, not a web server: it answers plain HTTP with "400 Invalid Request". The report is a file GoAccess rewrites continuously, and nothing served it. The route pointed there in the v1 configuration too, so this predates the rebuild — but the pre-flight could not prove it either way, because the external check measured Cloudflare Access rather than the service (which is its own lesson) | A read-only nginx beside GoAccess serves the report directory; the route points there. 26 of 26 hostnames now pass. The claim written here at the time — "the file is rewritten continuously, so a refresh is current" — was wrong, and F148 is what that cost | fixed 2026-09-01 |
| F141 | **cloudflared's data directory was owned by a user it does not run as.** Found by the F137 check on its first real deploy, not by anything going wrong: the image runs as the nonroot user 65532 and the directory said 100000. It is empty, so nothing has ever needed to write there — the latent form of the Loki failure, waiting for the first version that keeps state | Declared 165532. The check earned itself back within an hour of shipping. Test: `storage_the_app_cannot_write_fails_the_deploy` | fixed 2026-09-01 |
| F140 | **Deleting a duplicate CrowdSec bouncer revoked the key for all of them.** Three rows — `traefik-bouncer`, `traefik-bouncer@172.18.0.3`, `@172.18.0.7` — turned out to share one api_key: the same bouncer re-registered as Traefik got new container IPs. Deleting the base name (G4's tidy-up) took the key with it and the whole house answered 403 again, ninety seconds after the previous 403 was fixed | Re-registered with the key from the vault, which restored it immediately, and only one row remains. The lesson is that "remove the duplicates" is not safe by name here — the rows are one identity, not three | fixed 2026-09-01 |
| F139 | **Traefik's own log went to a file, so nothing shipped it and nothing capped it.** Kenny's question — "traefik is toch een docker container? waarom volgt die ons stramien niet?" — was exactly right. `--log.filepath` meant `docker logs traefik` was empty, promtail never saw it, the runaway guards never capped it, and the cause of a fleet-wide 403 could only be found by knowing which file to open | Operational log to stdout. The ACCESS log stays a file on purpose: CrowdSec parses it and GoAccess reports on it | fixed 2026-09-01 |
| F138 | **The rebuilt gateway wrote every stack's route into a directory nothing reads.** `gateway_routes_dir` still defaulted to the v1 path `/opt/traefik-config/routes` while the rebuilt Traefik reads `/appdata/gateway/traefik-config/routes`. Each deploy reported "route written". Three hostnames answered 404, and a route change from ANY stack would have been silently inert | Set host-side. Cost one extra round: a top-level TOML key appended after a `[section]` header belongs to that section and is ignored without complaint | fixed 2026-09-01 |
| M-G7 | **Measurement CLOSED (correction form, class G, field 7).** Done the moment the rule was written, and it failed half. Half one passed: the compose that took Grafana down, restored from git, was refused with the right sentence. Half two failed: the same rule also refused the RUNNING gateway, because CrowdSec mounts one file inside its own `/etc/crowdsec` and has done for months. Live measurement of the difference on that one container — `/etc/crowdsec rw=true`, `/etc/grafana/provisioning :ro` — so the rule now says what the failure actually is rather than what it resembles. Re-measured after narrowing: the broken compose refused, the current gateway valid, and all fourteen stacks pass | `homelab plan` over every stack | closed 2026-09-01 |
| M-T75 | **Measurement pending.** At the first nightly round after this ships: how long the backup phase actually took, against the 38 minutes measured on 2026-09-02 with one at a time. Three at a time predicts about 13 minutes. If it lands far above that, the bottleneck is not what was measured and the number should not simply be raised — the next step would be to find what the 36 small calls are really waiting on. Also check that all thirteen stacks have a fresh snapshot: concurrency that quietly drops a stack is worse than the wait it saves | T75 **First run 2026-09-02 04:07, and it proves the mechanism but not the timing.** The log says `scheduler: backing up 1 stack(s), 3 at a time` — so the new code path ran, read the configured concurrency and reported it. But only ONE stack was due, because the T12 drill had backed all thirteen up a few hours earlier and `backup_due` correctly considered the rest recent. The wall-clock comparison against the 38 minutes measured with one at a time therefore has to wait for a night where the whole fleet is due again — which is tonight, 2026-09-03. Recording the half that IS proven: the batch runs, holds the lock once, and honours the setting.| open |
| M-D94 | **Measurement pending.** After the switch: does the nightly kyu backup complete without stopping the service, and is the archived copy newer than the run that took it? The second half is the one that matters — archiving a stale copy would look exactly like success. Check the copy's timestamp against the backup's own start time, not merely that a file was found | T77, D94 **Precondition measurement, criterion sharpened by the kyu session and adopted:** a fresh copy is NOT enough, because a hand-started run produces one. All three must hold together — `LastTriggerUSec` of the **timer** later than 2026-09-02 01:14 UTC, `Result=success` on the service for THAT run, and a copy whose timestamp matches it. The first two exist because a manual start can satisfy either one on its own, which is literally what happened here.| open |
| M-R30 | **Measurement pending** (correction form C1–C9, all nine ratified by Kenny 2026-09-02; landed as standing rule 30 in `~/Projects/dev-procedure/STANDING_RULES.md` because it holds for every project). Two moments, both in the process rather than on a date. **One:** at the next time Claude claims in this project that something does not exist or is not configured — was a default checked first, and is that check visible in the text? Kenny's reading decides; Claude cannot measure its own blind spot. **Two:** at the next new reading that can be missing — does it return whether it succeeded, or only what it found? Standing at the moment of the fault: three instances in one night, two of them in code and one in Claude's own reasoning. Fallback if it fails (field 8, ratified): every "X does not exist" must carry, in the same sentence, the command that shows it | correction form C1–C9, R30 **First reading, and it FAILED — same night, 2026-09-02.** Writing norm "N3" for `ECOSYSTEM.md` asserted that no rule covered where a program keeps its configuration. Standing rule 28 had covered it since 2026-08-31, and the kyu session found the duplicate rather than Claude. That is exactly the shape rule 30 describes, one hour after ratifying it, against the rule stack instead of against a machine. The fallback from field 8 is therefore in force from now on: every claim that something does not exist carries, in the same sentence, the command or file that shows it.| open |
| M-D82 | **Measurement pending.** At the NEXT form built in any project: does every choice item carry a colour-coded consequences box above its pills plus a short subtitle on each pill, and could Kenny answer every item without asking what a word means. The measurement is Kenny's reading, not Claude's — Claude cannot judge whether something is clear to somebody else. Standing at the moment of the fault: six of six rating items in one session were bare. Fallback if it fails (field 8, ratified): the canonical vocabulary leaves the rendered form entirely and the pill LABEL becomes the consequence — "Bouwen vóór de expertenronde" instead of "Onmisbaar" | D82 | open |
| M-T55 | **Measurement pending.** The kyu.events chain is proven in two halves: the automation answered a real POST and its trace shows the right branch and correct rendering, and kyu-runner has the route registered and waiting. The end-to-end half — the hub emitting an event of its own accord and Kenny seeing it on his phone — has not happened, because manufacturing a dead letter on a live hub is not worth what it proves. At the first genuine `kyu.events` message, check that kyu-runner logged a delivery and that the notification arrived. If the topic is still unborn by the time of the phase-10 retrospective, that is itself the finding: it would mean the hub has lost nothing in weeks, and the route is insurance rather than a live path. | T55 | open |
| M-F165 | **Measurement pending.** After the nightly run of 2026-09-02 at 04:00, `crowdsec-config`, `grafana-config`, `loki-config` and `goaccess-config` must each hold at least one snapshot, and none may report zero files. If they are still empty the `no_data` fix did not reach the run and the cause is elsewhere | `restic snapshots` per repository, first thing after the nightly **Baseline taken 2026-09-02 00:24, measurement NOT yet due.** Claude read the repositories and found all four still at zero, then checked the host clock before concluding anything: it was 00:24, so the 04:00 run had not happened yet and "still empty" meant nothing. The reading is kept as the before-picture — `crowdsec-config`, `grafana-config`, `loki-config`, `goaccess-config` at 0 snapshots; `traefik-config` at 1, from the manual run of 2026-09-01 18:03. Any of the four holding a snapshot after tonight is the fix reaching the run. **MEASURED 2026-09-02, and the answer is yes.** All four hold a snapshot: crowdsec-config at 00:55:52, grafana-config at 00:56:09, loki-config at 00:58:04, goaccess-config at 01:01:16 — five repositories written in one gateway run, where before ONE empty directory (cloudflared, F154) aborted the stack's backup before any of them was attempted. So the `no_data` fix reached the run and the cause was not elsewhere. **The honest nuance:** these came from the T12 drill, a manual full-fleet backup, not from the 04:00 scheduler. The question this measurement asked — do the four hold at least one snapshot — is answered either way, because what was broken was the abort, not the trigger. | measured 2026-09-02 |
| M-C7 | **Measurement pending (correction form, class C, field 7).** The next time Claude needs a decision from Kenny WHILE something runs in the background — the situation in which it went wrong three times on 2026-09-01 — that decision must arrive as a form, with the background task simply continuing. Countable: at the end of this rollout round, how many decisions were put to Kenny and how many of those were forms. That evening's standing was 5 of 8. Anything short of all of them triggers the fallback in field C8 | this conversation, at the next decision **H2 has credentials as of 2026-09-02**, so the Kea path can finally run for real rather than being complete code that has never executed. | open |
| M-A5 | **Measurement CLOSED (correction form, class A, field 7).** Run against v3.35.0 on 2026-09-01, the new coverage question reported: *"drill — Grafana does not have this stack's generated dashboard."* One stack of fourteen, and the right one: the other twelve have a committed copy of their dashboard sitting on the container from before F149 was found, so Grafana genuinely does hold those — the question answered truthfully rather than loudly. `drill` was created after that committed set and had nothing, which is exactly the shape F149 leaves behind for every stack made from here on. The question found a real instance on its first run | `homelab check`, host v3.35.0 | closed 2026-09-01 |
| M-A4 | **Measurement pending (correction form, class A, field 7).** At the first stack-file change committed WITHOUT the runbook being regenerated, the suite must fail. Sabotage-verified on 2026-09-01, but the honest measurement is the first accidental occurrence, not the deliberate one. Standing so far: three stack-file changes since the test shipped, each regenerated on purpose, so the net has not yet had to catch anybody | `cargo test`, at the next stack change **MEASURED 2026-09-02 — the net caught somebody, and it was Claude.** Adding the `kuma-seeder` app to the uptime stack (T49) changed a stack file; the runbook was not regenerated, because regenerating it did not occur to Claude at all. The suite failed on `the_committed_runbook_matches_a_fresh_generation` with the message telling it to run `homelab runbook` and read the diff. This is the honest occurrence the measurement asked for — an accident, not a sabotage — and the net held. | measured 2026-09-02 |
| M-A3 | **Measurement closed (correction form, class A, field 7).** The promise was that the ownership measure is proven when all thirteen stacks have been through it once without a false alarm. Done 2026-09-01: 13 of 13 pass. It took five versions of the check and six false alarms to get there — F137, F141, F143, F144, F145, F146 — and the version that finally passes asks whether the app can write rather than who should own the directory. A measurement that recorded only the outcome would make this look like it worked the first time | the sweep, `homelab deploy` over every stack | closed 2026-09-01 |
| F137 | **A data directory owned by a uid the application does not use.** Loki's mount was declared `host_owner_uid: 100000` — container root — and Loki's image runs as 10001. Validation passed, because the +100000 mapping was right; only the uid was wrong, and no rule looked at that. Loki crash-looped on `permission denied`, the front door stayed down while it was diagnosed from a Go stack trace, and a chown by hand did not hold: the next deploy re-applied the declared number. Fourth permission failure in two days, after the render gid assumed to be 104 (F110) and two blanket chowns | New deploy step: ask docker what user each app runs as, add the mapping offset, compare with the directory's real owner, fail with the exact chown. Two tests, sabotage-verified. Run against the live fleet it found loki and stayed quiet about the other four. Test: `storage_the_app_cannot_write_fails_the_deploy` | fixed 2026-09-01 |
| F136 | **There is no way to create a stack except through the TUI wizard.** Twenty-one CLI commands, and not one of them scaffolds. `homelab presets` lists the catalogue — which contains an `uptime-kuma` preset that would have produced exactly the stack needed on 2026-09-01 — but `scaffold_stack` is reachable only from `client/src/tui/`. So a stack built from a non-interactive session is built by hand, which is what happened, and the hand-written copy skipped the placeholder substitution the scaffold does: the first deploy asked for a docker network called `__STACK___net`. Kenny's question was the right one — "waarom heb je iets met de hand gekopieerd wanneer het hele systeem zich baseert op automatische uitrol?" **Done 2026-09-01:** `homelab new <name> --preset <p> --vmid <n>` with optional `--ram --cores --disk --swap` and a repeatable `--no-data <path>`, so every feature the wizard has is reachable without it. The test that matters is not that it produces files but that it produces the SAME files: it scaffolds twice through the two paths and compares every byte, because two paths that drift apart would be worse than one path. | Not yet fixed. A `homelab new <name> --vmid N --preset X` would close it; queued as T65 | fixed 2026-09-01 |
| F133 | **The reconciliation check was written stricter than the deploy is willing to be.** First version compared the boot policy as text against `onboot: 0` — but `pct config` prints nothing at all when onboot is 0, so that comparison could never succeed. Second version treated an unreadable value as false, which put it at odds with W3: W3 leaves a boot policy it cannot parse alone, so the check demanded a correction the deploy would never make and the deploy could never succeed | Reads it with W3's own parser now, and only judges a value it can actually read. A check that outruns the thing it checks is not a stricter check, it is a deadlock. Test: `s2_reconcile_catches_a_container_that_does_not_match` | fixed 2026-09-01 |
| F134 | **21 "hevc Rext video streams" were cover art.** S1 was put to Kenny as 21 files that would gain hardware decoding; he chose to enable it and test. The test found the source is 340×340 yuv444p and produced exactly one frame — a poster embedded in an MKV. All 21 are 600×882, 340×340, 1067×600 or 120×176: posters, banners, thumbnails. Nobody transcodes them. The form's premise was wrong, and the dimensions were one query away | Setting is on and measurably inert. Reality-checking the recommendation BEFORE writing the form is FORM_PROTOCOL §5.6 and it was not done | noted 2026-09-01 |
| F135 | **A `git checkout` of one file threw away uncommitted test work.** Mid-way through the S2 test churn I reverted `core/tests/deploy_tests.rs` to HEAD to undo a bad edit, which also took three new tests and every fixture correction with it. Rewritten from context; nothing was lost permanently, roughly twenty minutes were | The lesson is not "be careful with git" but that a destructive command aimed at a file with uncommitted work needs the same pause as one aimed at a container | noted 2026-09-01 |
| F132 | **Killing the client does not stop the operation.** My own 10-minute command timeout killed the deploy client mid-rollout; the host carried on and finished the step, and the second client I started sat waiting on the AR12 operation lock. Correct behaviour — the host is the executor — but it is not what "the command was interrupted" suggests, and acting on that assumption would mean running a second deploy on top of a live one | Documented here; no code change. AR12's serial lock is what makes it safe | noted 2026-09-01 |
| F128 | **latch reads its project link from the user's HOME, not the working directory.** After the migration almanac ran fine — until the old `/opt/almanac/.latch` was renamed, at which point it failed with `not linked to a latch project` even though an identical copy sat in the new working directory. almanac's user has `/opt/almanac` as its home, so `~/.latch/config.toml` is what latch reads; the copy in the mounted directory was never consulted | Relinked in the home directory, with `dir` pointing at the new working directory, and the redundant copy deleted — it was 5.3 MB of latch's own clone riding along in every backup. The stack's backup went from 217 files / 5 345 657 bytes to 7 files / 8 571 bytes, which is what almanac's state actually is | fixed 2026-09-01 |
| F127 | **The supervised update ran the new binary's self-check in an environment the service never has, three times over.** `homelab update-native almanac` fetches the release, verifies its signature, and runs `almanac.new --check` before installing. Each refusal hid the next: without the working directory, "failed to read profiles directory profiles"; with it but without `latch run --`, "CLIENT_EMAIL is not set"; with that but without the EnvironmentFile, "no key for project 'almanac'". All three read as *the new version is broken* when what was wrong was where and how it was asked | The update command now reproduces the unit line for line — same directory, same environment file, same user, same latch wrapper — and 1.4.0 installed on the first try after that. Worth generalising: a supervised update that does not run the application the way its unit runs it is testing a configuration that does not exist | fixed 2026-09-01 |
| F126 | **promtail on a rebuilt container stops shipping and stays "Up".** The downloader's promtail ran for three hours after the rebuild with its last own log line an hour old, zero lines reaching Loki, and `docker ps` reporting it healthy the whole time. A restart fixed it instantly: 21 lines within three minutes. The fleet check caught it — "no log line reached Loki from this stack recently" — which is the coverage check (F103) doing exactly the job it was built for, on a stack that had been rebuilt hours earlier | Cause not established: promtail was alive, its config was correct, the json log files were present and the tag was set. The suspicion is the positions file or the file watcher after every container on the host was recreated at once. Recorded rather than guessed at — and worth a proper look, because "alive but shipping nothing" is the exact failure this project keeps finding. The detection side is covered by the coverage check (F79); the cause is not, and that is what keeps this open. | open — alleen detectie gebouwd (fleetcheck.rs:519); de oorzaak is nooit vastgesteld |
| F125 | **almanac's data path is absolute, not relative to its working directory — and latch's project link is keyed on an absolute path too.** The first attempt at giving the native services docker-style protection moved `/opt/almanac/{data,profiles,.latch}` and `/etc/almanac/latch.env` onto `/appdata` and repointed `WorkingDirectory`. The copy verified (217 files, identical), the mount attached, the unit deployed — and the service failed with `not linked to a latch project`, then with `failed to create the data directory /opt/almanac/data: Read-only file system`. Eight minutes down, fully reverted, back to 200 | The answer to Kenny's question, measured rather than guessed: for this app the working directory is NOT where the data goes. Two ways forward — mount the host directory AT `/opt/almanac/data`, which is exactly what a docker volume line does and needs no app change; or ask the almanac project for a configurable data directory the way kyu already has `KYU_DATA_DIR`. His call | done 2026-09-02 — nagemeten: stacks/almanac/almanac/almanac.service:46 — ALMANAC_STATE_DIR, opgelost bovenstrooms |
| F124 | **A pushed unit file overwrote almanac's binary, and the service kept reporting itself healthy.** A native stack names its unit file `<unit>/<unit>.service`, so the generic push landed it on `/opt/almanac/almanac` — which WAS the binary. The garbage collector had removed that path minutes earlier in the same deploy, the push then created a directory of the same name, and almanac went on answering only because its process held the deleted inode open. The next restart — a reboot, a crash, its own self-update — would have found a directory where the program should be | Found by reading `ls -la /opt/almanac` while planning something else. No check caught it and nothing was going to: `systemctl is-active` said active and `/healthz` said 200, both truthfully. Recovered with `cp /proc/<pid>/exe`, which is exactly what a deleted-but-open binary is for — the running bytes, no version guessing — then restarted deliberately to prove it. The unit file now goes to `/etc/systemd/system` and nowhere else, with a test. Test: `a_deploy_installs_the_unit_file_and_starts_a_service_that_is_down` | fixed 2026-08-31 |
| F123 | **A deploy unregistered the native services on its stack.** Giving CT 109 and CT 112 a manifest wrote an empty native list over four registrations, so the nightly backup of kyu, kyu-runner, http-switchboard and almanac would have stopped without an error | `native` and `natives` are carried across a deploy the way `last_backup` and the enabled flag already were: they belong to the C7 adoption, not to the deploy that touches the same stack | fixed 2026-08-31 |
| F122 | **The self-update path had a size ceiling nobody had written down, and the host binary grew past it tonight.** The link's default frame limit is 16 MiB; base64 of the host binary was 16 729 464 bytes at v3.19.0 and 16 861 920 at v3.20.0 — **132 KB over**. `homelab release-update` answered `Connection reset by peer`, which names the network, and the host logged nothing at all because the message never reached it. The rollout had to be done by hand — scp, checksum, selfcheck, install, restart — to get a host that could accept the next one | The mechanism that ships fixes was itself broken by a fix, at the exact moment it was needed. Both sides now name the same 64 MiB ceiling — five times the current binary, so growth alone cannot reach it — and the client refuses an oversized payload with a sentence that says it is a limit rather than a fault. Test: `a_payload_the_link_cannot_carry_is_refused_with_a_reason` | fixed 2026-08-31 |
| F121 | **The runaway guards installed a weekly docker prune on the two containers that have no docker.** CT 109 and CT 112 have been failing `docker-prune.service` every week since adoption, which is how a fleet learns to ignore failed units | Found while giving those containers a manifest. The docker guards are skipped where there is no docker; the journald cap still applies | fixed 2026-08-31 |
| F120 | **The protection flag was only ever set on the run that created the container.** A container whose flag had been turned off — by hand, or by an operation that lifted it and did not put it back — stayed unprotected and nothing reported it. Found within a minute of shipping the mount reconciliation, which lifts the flag to do its work: the very next deploy of the downloader left it off, and only reading `pct config` showed it | Same family as W3's boot policy and F118's mounts — intent applied once and never checked again. A deploy now turns it back on when the stack file asks for it, and never touches it for a stack that does not | fixed 2026-08-31 |
| F119 | **The version gate blocked its own remedy.** An hour after shipping, `homelab release-update` was refused against exactly the old host it existed to replace — and the refusal named that command as the fix. A guard whose escape hatch sits inside the thing it guards is a trap | Found by running it rather than reading it. `SelfUpdateHost` is exempt, with the reason written beside the exemption so nobody re-tightens it | fixed 2026-08-31 |
| F118 | **A host one release behind ignores a manifest field it does not know, silently, and the deploy reports success.** The `data_mounts` block was built, tested and committed today but never released; the host still ran v3.15.0. Serde drops unknown fields without a word, so `homelab deploy stacks/downloader` provisioned CT 105 with its config directory and **without its two data disks**, reported "3 app(s) verified", and left qBittorrent looking at an empty `/downloads`: **73 of 80 torrents went to `missingFiles`**. No file was deleted — the tree grew from 590 GB to 610 GB while it recovered — but qBittorrent discarded partial pieces it could no longer verify, about 7 GB, which are being re-fetched | Mine, and the exact failure this project keeps naming: a mechanism that runs, reports success and does less than it says. Two structural fixes, not notes to self: the client waits for the host's Hello and refuses a mutating command to an older host (F119), and a deploy now re-attaches a mount an existing container has lost, so a hand-made repair can never be the only thing holding the fleet together | fixed 2026-08-31 |
| F117 | **The `homelab` tag meant "built by the orchestrator", not "managed by it".** Kenny asked whether every tagged container is fully managed. The answer is yes — but the reverse was false: `pct set --tags homelab` runs only where a container is CREATED, so the two adopted native-service containers, CT 109 kyu and CT 112 almanac, carried no tag at all. Anyone filtering the Proxmox list by it would have seen six of the eight managed containers and concluded the other two were somebody else's | Adoption now sets the tag too, additively — an existing tag somebody else put there survives — with two tests verified by sabotage. The two live containers were tagged. The tag now means what it reads as. Test: `c7_adoption_tags_the_container_as_managed` | fixed 2026-08-31 |
| F116 | **The promtail `host` label is hand-written per stack, and it had drifted into a museum.** Loki's `host` values today: `108-app-synctest` and `110-app-syncthing` (two dead names of the syncthing container), `lxc-productivity-stack` (the container replaced tonight), `gateway` AND `platform` for the same CT 104, `__HOSTNAME__` — an unreplaced scaffold placeholder that shipped 938 lines in 24 h before it was fixed — and `114-app-productivity`, which was **mine, made an hour earlier**: scaffolding the new stack from paperwork with `sed s/paperwork/productivity/` turned `114-app-paperwork` into `114-app-productivity`, so CT 111's logs arrived labelled with CT 114's number | Corrected to `111-app-productivity` and redeployed; verified by query, not by reading the file. The wider point is the answer to Kenny's naming question: Prometheus labels are derived from the stack name and would survive a rename untouched, but this one is typed by hand and is the only place a container rename would actually bite — and it is already wrong without any rename | fixed 2026-08-31 |
| F115 | **Traefik's `routes/removed/` directory was never removed from anything.** The file provider is pointed at `/etc/traefik/routes` and reads it **recursively**, so four files parked in that subdirectory were still serving live configuration — including `lxc-mqtt-stack.yml` from 4 July with a **`HostSNI(*)` TCP catch-all**, a duplicate router for `sp.kp-soft.dev`, the retired `tasks.kp-soft.dev` router, and a `manual-routes.yml.bak` from 10 August. Found by chasing why a hostname that should have been gone answered 502 instead of 404 | This is also why "fix the two dead Traefik routes" (M0) never fully closed: retiring a route by moving it into that folder does nothing. All four moved to `/opt/traefik-config/retired/`, a sibling of `routes/` rather than a child. Verified after: sp 200, tasks 404, fin 302, home 200, unknown host 404 | fixed 2026-08-31 |
| F113 | **`latch commit` publishes what is on disk, so committing into an environment you did not pull deletes everything you do not have.** Going to add three files to `prod`, I ran `latch pull` (which defaults to `dev`) and then `latch commit --env prod`: it saw three files where the vault had twenty and marked **17 removed**, and the push published that. Every other stack's secrets — kp-soft, platform, paperwork, home — were gone from prod for about ninety seconds. Recovered with `latch rollback 346b287 --env prod` + push, then verified by reading three unrelated files back | The rule I already knew — pull before commit — is not enough: it has to be **the same environment**, because both commands take `--env` and default to `dev`. No deploy ran in the window, so nothing broke; the damage would have been every stack failing to deploy at once, which is also how it would have been noticed | closed 2026-08-31 |
| F114 | **The vault already held this stack's secrets, prepared by an earlier session, and one file was a landmine.** `productivity/supersync/.env` carried `SMTP_USER`, `SMTP_FROM` and `SMTP_PASS` unprefixed — exactly the generic names the next application with mail settings would use, and one collision in a flat namespace breaks the deploy of every stack (F102, F101). A parallel file `supersync-full/.env` held the same fourteen values correctly prefixed | Consolidated: the prefixed set is now `productivity/supersync/.env`, the duplicate is gone, and the unprefixed names with it. All fourteen values verified byte-identical against the running container BEFORE anything moved — a stale `JWT_SECRET` would have logged Kenny out of SuperSync everywhere, and a stale `WEBAUTHN_RP_ID` would have refused every passkey | fixed 2026-08-31 |
| F112 | **M8's target list was written before half of it existed.** It names eight stacks in order; three of them — `uptime` (which became `home` and was done as the M7 drill), `syncthing` and `metrics` — are already v2-native, built by the orchestrator itself, and `messaging` is the adopted native pair on CT 109. What is actually left is four legacy containers: 104 platform, 105 downloader, 106 media, 111 productivity. Same failure as M7's target: a plan sentence that was true when it was written and quietly stopped being true | Corrected in `REALIZATION_PLAN.md` with the real list, and reordered — productivity and gateway can be expressed today, media and downloader cannot until F111 is settled | closed 2026-08-31 |
| F111 | **The manifest model cannot express the media and downloader stacks.** `storage:` requires every `host_path` to live under `/appdata/` and to be named `<app>-config` (O7, chosen deliberately so a restore can find its own snapshots). CT 105 and CT 106 both mount `/HDD18TB/subvol-103-disk-0` at `/mnt/data/18TB` and `/HDD12TB/subvol-103-disk-0` at `/mnt/data/12TB` — media libraries, on datasets that belong to CT 103, which is on the no-touch list, and which are deliberately outside the backup scope. Neither the path nor the name can comply, and neither should: they are not config and the orchestrator must not create or back them up | Found by measuring the four remaining containers before starting M8, not during it. Two of the four are therefore blocked until the model says what a mount of somebody else's data is. Mini-round for Kenny; the area is quarantined until he decides | done 2026-09-02 — nagemeten: core/src/manifest.rs:71 data_mounts — geleend, niet bezeten |
| F110 | **The hardcoded render group was already the wrong number.** F54 said the gids 44 and 104 were assumed rather than read; measured on the host they are 44 (`video`, correct) and **993** (`render`, not 104). It has never fired because CT 106 is still the v1 container, which binds `/dev/dri` wholesale with no gid mapping — the new `pct set --devN,gid=` path is what applies the number, and the media stack is the last one in the rollout. So the failure was scheduled for the most consequential step of M8: Jellyfin would have come up and transcoded on the CPU | W1 reads both gids from the host and refuses a GPU stack the host cannot serve. Found by checking the proposal against the machine before freezing it rather than after | fixed 2026-08-31 |
| F108 | **The replacement itself costs about eighty seconds; the image pull costs everything else.** Measured on the M7 drill with a one-second probe: 653 s of outage in total, of which `pct clone` 30 s, start 1 s (the container answered ping again after 52 s), config and file push 28 s, `docker compose up` 13 s — and 573 s for one `docker compose pull`. The container's own journal shows an 8 m 40 s gap with no activity at all, then every layer completing in the same second, so it stalled rather than ran slowly: the same 362 MB image pulled again on the same container took 113 s, and a comparable image from Docker Hub took 5 s | The number that matters for M8 is not "eleven minutes". It is "a minute and a half of orchestrator, plus an external registry that may or may not answer today". Every remaining container replacement inherits that variance, and no amount of work on this repo changes it — see T59 | closed 2026-08-31 |
| F107 | **The two places where a deploy reads the backup repository built their own `BackupCfg::default()`** while the host has a configured one from `settings.toml`. Nothing was broken today because the live values happen to equal the defaults, which is exactly why it would have gone unnoticed: change `restic_base` and E3's auto-restore looks in a repository that does not exist, answers itself "no snapshot — fresh", and the deploy carries on and starts the app on an empty config directory. The same call also used the snapshot timeout for a restore, so the restore timeout raised after F38 never applied to it | `OpCtx` now carries the backup configuration and it is not optional — the caller has to say which repository it means, and a test fails when the default is put back. Test: `e3_auto_restore_reads_the_configured_repository` | fixed 2026-08-31 |
| F106 | **A replaced container comes back claiming it has never been backed up.** Destroy drops the state record (correctly — it describes a container that no longer exists), and deploy's "preserve `last_backup` across redeploys" then has nothing to preserve. In the M7 drill CT 115 was backed up twelve minutes before it was replaced and the fleet check called it broken afterwards, while the snapshot sat in the repository, restorable, untouched. The same would happen to every stack at once on a rebuilt host | The state file is a cache of what the repository knows, so when the cache is gone it is rebuilt from the source: with no prior record, deploy asks restic for the newest snapshot per app repository. An unreachable repository still records a zero and never blocks the deploy | fixed 2026-08-31 |
| F109 | **The ping monitor did not notice the container being destroyed and rebuilt.** `host · home` (ICMP, 60 s interval, 2 retries) recorded zero down beats across the whole drill, because the container answered ping again 52 s after it was destroyed. Only `home · homepage` saw anything, and it declared the outage three minutes late for the same reason | The per-host ping monitors say a container exists, not that it works, and at this interval they do not even say that reliably. They are not the net for a replacement; the HTTP monitor is | closed 2026-08-31 |
| F105 | The empty-snapshot guard was checked against restic itself, not only against its own unit test: a throwaway repository over an empty directory made restic report `total_files_processed: 0, total_bytes_processed: 0`, which is exactly the shape the detector looks for. Repository and directory removed afterwards | A detector that matches an imagined output is a detector that matches nothing. The unit test proves the parsing; this proves the parsing matches reality. Test: `a_snapshot_that_stored_nothing_is_recognised` | closed 2026-08-31 |
| F104 | **A restic run over a directory that exists and is empty succeeds and writes a snapshot containing nothing.** The record then says the stack is backed up while a restore would give back nothing. A path that does NOT exist already fails loudly — that is how the metrics stack's stale `prometheus-data` was caught this morning — but the empty case is the one nothing catches | The backup now reads restic's own summary and refuses a snapshot with zero files and zero bytes. Output it does not recognise is deliberately NOT called empty: a restic that changes its json would otherwise turn every backup in the fleet into a failure overnight, and a check that fires on what it merely fails to understand is worse than none | fixed 2026-08-31 |
| F103 | **The coverage check reported nothing, and I nearly accepted that as proof.** Zero findings is the same output whether every stack is covered or the check never ran — which is precisely the class it was built to catch. The journal showed no queries, but that proved nothing either: the fleet check logs to the client stream, not the journal. Settled by breaking its input on purpose — `prometheus_url` pointed at a dead port for one run, which produced exactly 7 "not being measured" findings, then restored, which returned it to silence | Independent measurement agreed: all 7 managed stacks scrape up, 5 ship logs, and the 2 that do not are kyu and almanac, the native services with no promtail — the two the check deliberately does not ask about | closed 2026-08-31 |
| F102 | **latch's namespace is flat across the whole project, and `--expand` makes every collision global.** Read from the source rather than inferred from the error: `consume.rs` merges every file into one map keyed by variable name — same name with the same value is fine, different values is a hard error. The orchestrator reads secrets with `latch cat --expand`, which must build that whole map to resolve `${...}`, so a collision in any corner breaks the deploy of every stack. kp-soft's variables were the generic Laravel set (`APP_KEY`, `APP_URL`, `MAIL_USERNAME`, `DB_DATABASE`); a second PHP app would have collided on nearly all of them | Asked by the kp-soft session, which spotted the shape before it fired. All eighteen prefixed `KPSOFT_` in latch and mapped back in the compose file, so nothing about the application changed and no release was needed. `APP_KEY` verified identical afterwards — a changed one logs everyone out | fixed 2026-08-31 |
| F101 | **Two sessions independently named a secret `KYU_TOKEN`, and latch refused to serve any of them.** The switchboard's token and the two *arr tokens shared a name with different values, so `latch cat` errored on every deploy of any stack — a name collision in one corner blocked an unrelated one. Latch's D11 rule working exactly as designed | The switchboard's name is fixed by its own config (`token = "${KYU_TOKEN}"`); the *arr files are storage only, read by no stack, so those were renamed to `KYU_TOKEN_SONARR` / `KYU_TOKEN_RADARR`. Worth knowing fleet-wide: a shared vault makes variable names a shared namespace | fixed 2026-08-31 |
| F99 | **The abandoned worktree was not clutter, it was unmerged work that had been fighting the wrong battle.** `.claude/worktrees/hungry-elbakyan-85a663` held three commits on no branch, and its CI had gone red on the MSRV job — trying to hold the floor at 1.87 while `time`, `darling` and `instability` all wanted 1.88. Raising the floor, done today for an unrelated reason, was the answer it had been looking for | All three superseded: the gates fix is already in main byte-for-byte, the HTTPSwitchboard parking was overtaken by deploying it, and the MSRV commit by moving to 1.88. Removed after checking each rather than on age | fixed 2026-08-31 |
| F100 | A TUI can be smoke-tested without a human, but not with `script`: crossterm asks the terminal for the cursor position and gives up if nothing answers, so `script -qec` produces "the cursor position could not be read", which reads exactly like a broken build. A pty that answers the query works. `scripts/tui-smoke.py` does that and reports whether the TUI drew, what text reached the screen, and whether it panicked | Answers a question Kenny asked earlier the same day. Its limits are stated in the file: it proves the TUI starts and draws, not that the layout is right | closed 2026-08-31 |
| F97 | **Two sessions shared one working tree and the commits landed on the wrong branch.** The homeassistant-mcp session branched off main for a one-line register entry; my next two commits — the dependency fix and the v3.9.1 release — landed on that branch instead. Main stayed at `d053f7d`, so origin/main did not contain the security fix even though the host was already running it, and the pushed tag pointed at a commit that was on no branch | Resolved without rewriting anything: the branch was a straight descendant, so main fast-forwarded onto it. The tag is now in main's history and both sessions' work is intact. The other session found and reported it themselves, including an amend they had made and reverted | fixed 2026-08-31 |
| F98 | kyu-runner grew a third route (`arr-ops`) added directly on the container by another session. It works — Sonarr and Radarr now publish their blocked-import events onto the hub — but kyu-runner is a native service whose config the orchestrator backs up and never writes, so that route lived only on CT 109 and in the nightly snapshot | Captured into `captured/messaging/kyu-runner/` with the webhook ids removed, which is the same lesson as F82 from a different direction. Verified healthy after their change: unit active, both routes idle | fixed 2026-08-31 |
| F95 | **Two red CI runs on main were not CI.** They were Dependabot update jobs failing against `client-app/Cargo.toml` — a manifest deleted with the v1 tree on 2026-08-31. Auto-discovery kept opening jobs for it, so main looked broken every day over a directory that no longer existed | Closed with an explicit `.github/dependabot.yml`: one workspace, one manifest, minor and patch grouped into a single pull request rather than five | fixed 2026-08-31 |
| F96 | **The high advisory was in the client's TLS, and all three advisories were held by something structural, not by a stale lockfile.** `cargo update` moved nothing: `rustls-webpki 0.102.8` was pinned by `rustls 0.22` in the client, `time 0.3.45` by an MSRV of 1.87 (the fix needs 1.88), `lru 0.12.5` by `ratatui 0.29` | Fixed the first two: MSRV to 1.88, and the client's TLS stack to rustls 0.23 / tokio-rustls 0.26 / tokio-tungstenite 0.26. The vulnerable webpki is gone from the tree entirely. Verified live, which is the part that mattered — the pinned-certificate connection still works: `ping` answered and a full fleet check ran over the same line | fixed 2026-08-31 |
| F93 | **Cloudflare Access answers 401 to writes, not a redirect.** It can send a page request to its login screen; it cannot redirect an XHR or a non-GET. So a site behind Access reads perfectly and saves nothing, and the only visible text is "cloudflare". Measured: `PATCH` through the edge → 401 with `www-authenticate: Cloudflare-Access`; the same request straight at the container → 419, Laravel's own CSRF answer | Reported by the kp-soft session, which had diagnosed it correctly from its side. Worth keeping fleet-wide: any future app behind Access that does more than serve pages will hit this, and the symptom points at the app | fixed 2026-08-31 |
| F94 | **A passkey is bound to the hostname in `APP_URL`** — WebAuthn derives its relying-party id from it, so passkeys registered on a temporary address stop working when the real one arrives. Found by the kp-soft session, not by me: I had treated the hostname as cosmetic while it was a decision with a one-way consequence | It is why "temporary hostname" was not the free choice it looked like, and it changed the recommendation mid-round | fixed 2026-08-31 |
| F91 | **The Gmail app password existed only inside a running container.** Kenny thought there was one for `kennypersonalassistant@gmail.com` and there was — live in SuperSync's environment on CT 111, sixteen characters, and in latch nowhere, in no repository, in no vault note. A rebuild of CT 111 would have taken it, and the first symptom would have been that nobody could register on SuperSync any more | Reused for kp-soft and, more importantly, written into latch as SuperSync's own secret so the next rebuild survives it | fixed 2026-08-31 |
| F92 | The orchestrator has no mechanism for a private registry: it pushes compose files and runs `docker compose pull`, so an image behind a login fails the deploy at that step. Worked around with a one-time `docker login` on the container, the token in latch | The deploy failed loudly rather than silently, which is right, but a container rebuilt from scratch will need that login again and nothing says so. Filed as T56 | done 2026-09-02 — nagemeten: core/src/manifest.rs:94 registry_login + deploy.rs:1155 — geleverd in 3.11.0 (T56) |
| F89 | **The alert chain had been complete on every side except the middle since 2026-08-29.** `automation.homelab_alert_webhook` existed, waiting for `{alert, status, severity, instance, summary}`. Alertmanager was running with four rules. HTTPSwitchboard was built, tested and drilled. Nothing connected them, and Alertmanager's own config said so in a comment rather than anywhere anyone would look | The pieces were each finished and the whole did nothing. Now proven end to end, including the part the switchboard project could not claim: a genuine alert travelling the chain | fixed 2026-08-31 |
| F90 | `DynamicUser=yes` and a root-only config are incompatible: the service could not read `/etc/http-switchboard/config.toml` at 0600 and crash-looped. Making it world-readable would have put the Home Assistant webhook id — the credential for the notification dispatcher — where anything on the container could read it | Solved with `LoadCredential=`: systemd copies the file into a private directory only this unit can see, so it stays 0600 on disk and the sandbox keeps its identity. Better than the 0644 that would have worked | fixed 2026-08-31 |
| F87 | **The A2 naming guard assumes one stack per container, and C7 native services legitimately share one.** kyu-runner runs beside the hub it pumps, on purpose, because it talks to it over localhost — but `adopt` refused a manifest whose hostname was `109-app-kyu` while its stack was `kyu-runner` | Not a defect: the design already supports it. A stack holds a LIST of native services distinguished by unit, and the unit is what names the restic repository. kyu-runner joined the `kyu` stack as a second unit instead of pretending to be a container of its own | closed 2026-08-31 |
| F88 | **The direct notification path was switched off only after the hub path was proven**, not before — otherwise there is a window with no warnings at all, which is the failure being fixed. `/etc/homelab/host.toml.pre-kyu` is the copy to revert to | Also: the proof message used `ok: false` with the push toggle now on, so it reached Kenny's phone as "kyu-path-proof mislukt". Careless of me; `ok: true` would have proven the same chain without ringing | closed 2026-08-31 |
| F85 | **The notification path works and delivers nothing, by design.** `automation.homelab_ops_webhook` receives every event from the host, appends it to `/media/homelab_events.log` and writes it to the logbook — and pushes only when `input_boolean.homelab_event_notifications` is ON and the event is a failure. That toggle is off, described in the automation's own text as "nothing is pushed by default" | So H8 parking a stack now produces an event (3.7.0) that lands in the log, and Kenny's phone stays quiet until he flips one switch. Verified end to end with a clearly labelled test event: webhook returned 200 and `notify.homelab_events_log` updated at that second | open — the toggle is Kenny's call |
| F86 | The nightly fleet check already POSTs its findings, but with a payload carrying `op`/`ok`/`error`/`version` and no `source` or `label`, unlike every other event. The HA automation does not filter on `source`, so it lands — but it is the only payload in the system that does not use `op_payload`, and a future filter on `source` would drop exactly the report that matters most | Not fixed: changing a payload shape the HA side parses is a coordinated change, and today it works | done 2026-09-02 — core/src/notify.rs, alle drie de meldingen door op_payload, met test |
| F82 | **A repair that lived only on the running machine expired.** `sync.kp-soft.dev` pointed at 10.10.10.10 — Kenny's workstation, not the container — and was corrected on the gateway by hand on 2026-08-30. The source file in this repository was never corrected, so the first deploy of that stack pushed the broken address straight back over the fix, and the fleet check caught it within the minute | This is the exact failure the whole project exists to kill, arriving from the one direction I did not check: my own earlier repair. Fixed in the source with the history written into the file, so the next reader sees why the address matters | fixed 2026-08-31 |
| F83 | I broke two working dashboards while fixing a third. Making the log panel tolerate missing container names, `container_name=~".+"` became `".*"` everywhere; in Stack Logs that is safe because `stack="$stack"` carries the query, but in the other two it was the only matcher and Loki rejects a selector whose every matcher is empty-compatible | Kenny saw it as "no data in all three sections" before I did. They now select on `job="docker"`, verified through Grafana's own datasource proxy rather than against Loki directly — 2, 3 and 4 series | fixed 2026-08-31 |
| F84 | The metrics stack had auto-disabled itself. H8 parks a stack after a failed nightly run, and the run that stopped Alertmanager (F75) was that failure — so metrics had silently dropped out of the nightly rotation entirely | Working as designed and reported by the fleet check, but worth recording: one bad run removes a stack from every nightly protection until somebody re-enables it | fixed 2026-08-31 |
| F79 | **The container name was missing for two independent reasons on one line.** Every promtail pipeline read `container_name: attrs.name` with `source: attrs`. Docker writes no `attrs` field unless the daemon carries a `tag`/`labels`/`env` log-opt, so the field never existed — and the path repeated the `attrs.` prefix that `source: attrs` had already applied, so even the right field name would have found nothing. Fixing either alone changes nothing, which is presumably why it survived | The guards' daemon.json now sets `tag: {{.Name}}` and every pipeline reads a bare `tag`. Proven on CT 108 first: Loki went from zero `container_name` values in the life of this fleet to nine across the fleet. And now guarded rather than merely repaired: the fleet check counts LABELLED lines, over a window that is a host setting, so a return of exactly this fault is a finding instead of three blank dashboards. Test: `a_stack_whose_logs_never_arrive_is_reported` | fixed 2026-08-31 |
| F80 | **A changed config file does not restart the service that reads it.** The deploy pushes `promtail-config.yml` and runs `docker compose up -d`, which sees an unchanged compose definition and leaves the container running. Promtail kept the old pipeline for four minutes and the first verification reported the fix as not working | Cost a wrong conclusion, not an outage — but the same shape applies to every mounted config in the fleet, so any edit can silently not take effect. Filed as T52 | done 2026-09-02 — nagemeten: core/src/ops/deploy.rs:1266 — needs_restart, geleverd in 3.9.0 (T52) |
| F81 | Two labels were shipping into Loki as real data: the metrics stack sent the literal string `__HOSTNAME__` as its `host` label, an unsubstituted placeholder, and CT 108 labelled itself `110-app-syncthing` from a plan that changed. The metrics and gateway promtail configs had no pipeline at all, so their logs could not have carried a container name even in principle | Found by reading the label values rather than the configs — `/loki/api/v1/label/host/values` listed `__HOSTNAME__` beside the real ones | fixed 2026-08-31 |
| F77 | **A false alarm I should have caught sooner.** Homepage's served HTML is a build-time static page carrying the upstream default config; the real page assembles client-side. Fetching it with curl showed "My First Group" and I spent several steps on Next.js internals — cache directories, ISR, bind-mount ordering — before loading it in a browser and waiting ten seconds, at which point everything was correct | The server APIs had said so all along: `/api/services` and `/api/bookmarks` returned Kenny's config from the first check. Two contradicting signals should have sent me to the browser immediately instead of deeper into the one that agreed with the symptom. The `mkdir /app/.next/cache` done during that detour is inert and disappears on the next container recreate | closed 2026-08-31 |
| F78 | Five *arr API keys and the Grafana, Proxmox, Jellyfin and paperless credentials were printed into the session transcript: a `sed` mask on a `docker exec env` dump did not match, so the values came back in full | Kenny was told rather than left to find it. He had already accepted the same exposure for the Jellyfin key earlier. Rotation is offered and is his call; nothing here reaches outside the LAN except through Cloudflare Access | open — Kenny's call |
| F75 | **The nightly backup could leave a service switched off, and did.** Quiesce selects containers by the docker label `com.homelab.backup.pause=true`; resume brought back the manifest's `apps` list. On the metrics stack those sets had drifted apart — host state still held `[prometheus, promtail, pve-exporter]` from before alertmanager joined the stack — so the 04:13 run stopped two containers and started three, one of which was not among them. Alertmanager sat `Exited (0)` for six hours with `restart: unless-stopped`, which correctly refuses to restart a container stopped on purpose | Found by Kenny in Uptime Kuma, two hours after that monitor existed — the monitor set from T22 earned itself back the same morning. Fixed in 3.5.0: what is paused is recorded by name and started back by name. Two tests, each checked to fail without the fix. Test: `e1_backup_resumes_every_container_it_paused_even_one_not_in_apps` | fixed 2026-08-31 |
| F76 | The same run's snapshot failed on a stale path: host state pointed at `/appdata/metrics/prometheus-data`, renamed to `prometheus-config` days earlier. That is why nothing later in the run corrected the stopped container — the run had already aborted | Host state only refreshes on deploy, so a stack that is not redeployed keeps an old manifest indefinitely. Redeploying metrics fixed both the path and the app list; a full backup then ran clean, `docker start alertmanager prometheus`, two snapshots into the per-app repositories | fixed 2026-08-31 |
| F70 | **The T1 discovery mechanism was wired to nothing.** The orchestrator has written `/appdata/metrics/prometheus-config/targets/<stack>.json` on every deploy since T1 landed, correctly, including `paperwork.json` at 02:18 today. Prometheus's live config had no `file_sd_configs` and never read the directory | Third instance tonight of the same shape: a mechanism that runs, reports success and changes nothing — after the log guards (F56) and the growth check's own coverage. Fixed: node and cadvisor are now file-discovered, verified 21 targets up, every one carrying its `stack` label | fixed 2026-08-31 |
| F71 | **A stale scrape target had been silently re-pointed.** `10.10.10.14:9100` was still in the hand-kept list labelled `host: scratch-mailbox`, from a container destroyed that morning. The address was reassigned to the new paperwork container, so its node metrics arrived filed under a machine that no longer exists | Nobody would have noticed: the target was green. This is precisely the failure the T1 comment predicted and the reason the list had to stop being an administration | fixed 2026-08-31 |
| F72 | **Three Grafana dashboards query a label that has never existed.** Docker Containers Overview, Homelab Overview and Stack Logs Dashboard all filter and group on `container_name`. Loki's labels are `filename, host, job, service_name, stack, stream`; `container_name` returns an empty value list. They were not broken — they were written against a label promtail was never configured to produce | Answers Kenny's report directly: the paperwork one was a real defect and is fixed, these three need a container label to exist first, which is F69's job. `service_name` is not a substitute — outside the gateway it falls back to "docker" for everything | done 2026-09-02 — nagemeten: guards.rs:32 + promtail-config.yml:37 — container_name wordt fleet-breed geproduceerd |
| F73 | **Pinning the Loki datasource uid took Grafana down.** The dashboards reference the datasource as `loki`; provisioning had never pinned a uid, so Grafana had generated `P8E80F9AEF21F6940` and every log dashboard resolved to nothing. Adding `uid: loki` to an EXISTING datasource makes provisioning fail with "data source not found", and that failure takes the whole Grafana process with it — not just the datasource | Recovered in one step with the supported route: `deleteDatasources:` by name, then recreate. Grafana back at HTTP 200, uid `loki`, proxy answering, all 7 dashboards intact. The Prometheus datasource beside it had been pinned for exactly this reason; Loki was simply forgotten, and nothing checked | fixed 2026-08-31 |
| F74 | The discovery generator wrote a cadvisor target for every stack, including native-service ones. Measured: kyu (CT 109) and almanac (CT 112) answer on 9100 and refuse 8081, having no docker at all | Would have given Prometheus two permanently unreachable endpoints and Alertmanager two rules that can never clear. An alert that is always on is an alert nobody reads | fixed 2026-08-31 |
| F69 | **The docker-API discovery attempt at F59 was reverted.** Rewriting CT 104's promtail from a file glob to `docker_sd_configs` did deliver what it promised — every line gained a real `container` label, where before the only clue was the container-id path — but it also produced a steady ~1/s `400: at least one label pair is required per stream` from Loki, and Loki's own logs never arrived either way. Ruled out by testing rather than reasoning: it is **not** the `drop` rule (removing it changed nothing), and it is **not** discovery (all 9 targets were Ready, traefik included; traefik was absent from Loki simply because it had logged nothing in three minutes). The gateway is back on the file-based config, verified at 0 errors with logs flowing | Two lessons worth more than the fix. A config that parses is not a config that works — the first version shipped label-less streams that promtail accepted and Loki rejected. And editing the file in place on the live container with `sed` to test a hypothesis damaged it; the recovery was `git show HEAD:` and a clean push, which is the only reason this cost minutes instead of the gateway's logging | done 2026-09-02 — nagemeten: core/src/ops/guards.rs:32 tag {{.Name}} — via de andere route opgelost, niet zoals gepland |
| F64 | **A green healthcheck on a dead server.** `paperless-db` reported `healthy` for eleven minutes while every real query failed with `could not open file "global/pg_filenode.map": Permission denied`. The manifest declared uid 999, correct for the Debian postgres image; the alpine one runs as uid 70. `pg_isready` connects to the socket and never touches the catalog, so it passed throughout | The lesson is not the uid, it is that this class of healthcheck proves the process is listening and nothing more. Verified the fix with an actual query, not with the healthcheck | fixed 2026-08-31 |
| F65 | **Stirling-PDF 2.x always requires a login**, regardless of `DOCKER_ENABLE_SECURITY=false`. It came up with an `admin` account on the documented default password, which on an internet-facing hostname is a known credential | Found by curling it and reading the log, after the compose comment had already asserted the opposite. Corrected in the same commit as the fix | fixed 2026-08-31 |
| F66 | paperless has two OCR variables one letter apart: `PAPERLESS_OCR_LANGUAGES` (plural) installs the tesseract packs, `PAPERLESS_OCR_LANGUAGE` (singular) selects them. With only the singular set it fails its own Django system check and s6 restarts the container in a loop that reads as a slow boot from outside | Cost roughly ten minutes of watching a "starting" container. `docker logs \| grep -i error` found it immediately once I stopped waiting and looked | fixed 2026-08-31 |
| F67 | A stack's apps are separate compose projects, so `depends_on` across them is undefined and aborts the deploy. Start order comes from the manifest's `apps:` list instead | Worth stating in the preset guide: it is the natural thing to write and it fails loudly, which is the good case | fixed 2026-08-31 |
| F68 | One of the 25 re-imported documents (`0000018.pdf`, now paperless id 13) OCR'd to 14 characters. The other 24 carry 400–4000 characters each | Not an import defect — the source scan has no readable text. Worth Kenny knowing before he wonders why it never turns up in a search | vastgesteld feit — ter informatie voor Kenny |
| F61 | **paperless-ngx ran here before and its documents survived.** `/HDD2TB/paperless_media/documents/` holds 25 originals, 25 archive PDFs and their thumbnails (9.88 MB), created 2026-03-26. The E8 job already replicates them to `HDD18TB/replica/`. What is gone is the database: no pgdata anywhere on the host, no vzdump archive from that period, and the files are named `0000026.pdf` — so tags, correspondents and document dates existed only in that database | Re-importing the originals rebuilds full-text search but not the hand-assigned metadata. At 25 documents that is a small loss, but it must be stated rather than discovered later | vastgesteld feit — verloren database, geen repareerbaar defect |
| F62 | Stirling-PDF ships with **no authentication by default**; it must be switched on explicitly. Actual Budget and paperless-ngx both carry their own login | Decides N2: exposing all three through Traefik would put an unauthenticated service on the public edge | done 2026-09-02 — nagemeten: stacks/paperwork/stirling/docker-compose.yml:12 — de premisse was fout, Stirling 2.x heeft verplichte login |
| F63 | Host headroom measured before proposing the three new services: 31 GB RAM with 18 in use, 12 cores at load 1.02, 657 GB free on local-lvm. Allocated-to-guests already exceeds physical (27.4 GB LXC + 10 GB VM against 31 GB), but actual use is far below allocation | Answers Kenny's "als dat niet te zwaar is": the three together need ~3 GB. The overcommit matters for G2 (disabling swap), not for this addition | closed |
| F60 | CT 106 was skipped when applying the guards: Jellyfin had a paused session open ("Curiouser and Curiouser"), and restarting docker there would have dropped it | O10's rule applied to my own action rather than only to the scheduler's. Kenny's go was general; he did not know he had a film open | fixed 2026-08-31 — applied when Jellyfin was idle (0 sessions), all 9 containers verified running afterwards |
| F56 | **The runaway guards exist and run on almost nothing.** `guards.rs` caps docker json-file logs at 10m×3, sets journald limits and installs logrotate — but it only runs at bootstrap of orchestrator-built containers. Measured across the fleet: only CT 108 and 113 carry a journald cap, only CT 106 has a docker log cap (hand-set 20m), and CT 104/105/109/111/112 have neither | CT 104 alone holds **907 MB of docker logs**, a **397 MB journal** and **147 MB of Traefik access log** on a 30 GB disk. Not fatal today; exactly the slow fill Kenny asked to make impossible | done 2026-09-02 — nagemeten: guards::apply is een onvoorwaardelijke stap in de deploy (deploy.rs:890), niet alleen bij bootstrap |
| F57 | Every container uses swap, CT 106 at 1028 of 2048 MB. Swap turns memory pressure into slow degradation instead of a loud failure | Kenny: "liefst ook swap vermijden". Removing it makes the OOM killer act instead, which is the fail-loudly behaviour the rest of this project is built on | done 2026-09-02 — opgelost door D95: alle 13 containers op swap_mb 0, live toegepast en nagemeten |
| F54 | GPU passthrough hands the container `/dev/dri/card0` and `renderD128` with **hardcoded group ids 44 and 104**, and nothing checks that the host has the device or that those gids are right. A container comes up either way; you find out when a film transcodes on the CPU | The most self-concealing failure left in the fleet, and the media stack is last in the rollout | fixed 2026-08-31 (W1) — and the assumed render gid was already wrong, see F110 |
| F55 | The TUI has **zero tests**, while `update(model, msg)` is a pure function that could be driven exhaustively without a terminal | Measured while answering Kenny's question about testing it | done 2026-09-02 — nagemeten: client/tests/tui_snapshot_tests.rs — 2130 regels, 42 tests op update(model,msg) |
| F51 | Kenny's question "can we trim the template for RAM?" — measured, and the answer is no. A fresh container uses 63 MB, of which docker and containerd are ~40. Postfix is ~8 MB, idle CPU is indistinguishable from zero, so trimming would save ~80 MB across ten containers | Postfix removed anyway, for a different reason: an unused mail daemon listening on :25 in every container. Three docker plugins removed for ~120 MB of disk each | closed |
| F52 | The v2 template left `ssh.service` FAILED on every clone — `generalize` deletes the host keys, correctly, and nothing generated new ones. Kenny reaches these containers by ssh | Found by cloning the template and looking, not by reading the step that built it | fixed in v3 |
| F53 | A clone booted `degraded` from `nvmf-autoconnect` and `openipmi`, which an LXC can never satisfy. The deploy's wait-for-systemd loop accepts "degraded" | So a template that is degraded by default makes the word useless as a signal — the reason this is worth more than tidiness | fixed in v3 |
| F49 | **`latch commit` deletes what is not on disk.** Committing after creating only one env file removed the two others from the repository — "1 changed, 2 removed". `latch diff` had earlier reported "no differences" with no local files at all, which is what led me to believe absent meant untouched | My mistake, recovered: both values still existed on their containers and were re-committed and verified. `latch rollback` was blocked by the permission classifier, so recovery came from the systems of record instead. **Always `latch pull` before `latch commit`** | closed |
| F50 | Measured with a working key: Jellyfin's session objects have **no `IsPlaying` field** — they carry `NowPlayingItem` and `PlayState.IsPaused`. The v1 `check-streams.sh` grepped for `"IsPlaying":true`, which never matched | Settles F33. That script could not have blocked a single update: it failed open on every error path AND its one positive test never fired | closed |
| F46 | The fleet check's first live run called **every** route in the house dead — Jellyfin, Home Assistant, the Proxmox UI. Two causes: `/dev/tcp` is a bash feature and the container shell is dash, and `https://10.10.5.1` carries no port so the probe asked for a path | The pure comparison had six passing tests and was right; the shell around it had never run once. The HTTPSwitchboard "first live call" lesson, on schedule | fixed in 3.3.1 |
| F47 | `last_backup` was recorded by the scheduler and by no on-demand path, so a stack backed up by hand still read as never backed up | The check reported it about kyu minutes after I had backed it up with a real snapshot to show for it. Would have made every manual backup during the M8 rollout invisible | fixed in 3.3.2 |
| F48 | GitHub reports 7 dependabot vulnerabilities on `main` (1 high, 2 moderate, 4 low); two dependabot runs are failing, one of them against the now-deleted `client-app/` | Not triaged. The failing runs at least partly chase a directory that no longer exists | deels — de falende runs zijn weg (.github/dependabot.yml), de triage van de meldingen is GitHub-state |
| F43 | **F40 was wrong.** `pct snapshot` refuses any container with a bind mountpoint — "snapshot feature is not available" on CT 113. That is every managed container, because model v2 IS bind mounts. The undo-button layer I proposed does not exist where it would be used | Corrects what I told Kenny about snapshots; the vzdump layer stands unaffected | corrected |
| F44 | `/appdata/metrics/alertmanager-data` was bind-mounted by Alertmanager's compose file and declared in no `storage:` entry, so docker created it on the **container rootfs** — verified by `df`: rootfs, not pve-root. Unbacked-up and lost on any rebuild | Exactly the bug class `validate()` names as "the synctest-108 bug class", found live. The directory was still empty, so nothing was lost — luck, not design | fixed 2026-08-30 |
| F45 | B1's protection flag refused `pct set -mp0` on CT 113 during the rename, as designed | The lift-change-restore dance is a required step in any procedure that touches a mountpoint; M7's replacement procedure must carry it | done 2026-09-02 — nagemeten: core/src/ops/deploy.rs:694 — protection eraf, schrijven, er weer op |
| F42 | O7 as frozen says config paths must be named `<app>-config`, but the live metrics stack uses `/appdata/metrics/prometheus-data` — a data directory, not config. Enforcing the letter would refuse a running stack; D25 meanwhile made ownership explicit data, so the naming rule is no longer load-bearing | Quarantined: not built, mini-round queued rather than deviating silently | done 2026-09-02 — nagemeten: core/src/manifest.rs:519 + test o7_config_paths_must_be_named_after_their_app |
| F41 | "Adopt CT 111" is impossible as written: `homelab adopt` takes a systemd-unit manifest (C7 is native-only) and CT 111 is a docker stack whose hostname A2 refuses | M0 corrected to a nightly vzdump; real adoption waits for the M8 rebuild | corrected |
| F40 | Container snapshots are supported (every container is on thin-provisioned `local-lvm`) and **not one exists**. The thin pool is 20.8% used, metadata 0.9% | A near-free instant-undo layer sits unused; directly useful before an update (O9) or a risky change | open — proposal for Phase 5 |
| F34 | **`sync.kp-soft.dev` is broken right now.** Its route file sends traffic to `10.10.10.10:8384`, where nothing answers; syncthing runs on `10.10.10.8:8384`, which returns 200. Verified live 2026-08-30 | A second dead route beside the MQTT one, unnoticed. Found by the critic, not by my own sweep — I recorded the address without checking it resolved | done 2026-09-02 — nagemeten: stacks/syncthing/traefik-routes.yml:26 — wijst naar 10.10.10.8 |
| F35 | `pct set -mp<i>` is only reached inside `if !exists` on both provisioning paths in `deploy.rs`; a deploy onto an existing container configures no mountpoints at all | Adopting the ansible-era stacks in place would put config on the container rootfs and have restic snapshot an empty host directory, green all the way | done 2026-09-02 — nagemeten: core/src/ops/deploy.rs:670 + test a_missing_mount_is_reattached_on_an_existing_container |
| F36 | `native.rs:73` refuses a manifest with empty `data_dirs`, and kyu-runner is deliberately stateless (`DynamicUser=yes`, "no state directory, no disk to protect") | kyu-runner cannot be declared as a native service today | done 2026-09-02 — nagemeten: core/src/native.rs:100, een `stateless: true`-vlag laat lege data_dirs toe |
| F37 | kyu and http-switchboard both default to `0.0.0.0:8080`, and http-switchboard's `--healthcheck` with no argument probes `127.0.0.1:8080/healthz` — which on a shared container is kyu's endpoint answering 200 | A dead switchboard would report itself healthy. Settled by moving it to CT 113 | closed by design |
| F38 | The restore timeout is hardcoded at 1800 s (`backup.rs:329`) while the backup timeout was deliberately raised to four hours | A large restore from Google Drive dies at thirty minutes, on the operation you least want broken | done 2026-09-02 — nagemeten: BackupCfg.restore_timeout_s, standaard 4 h, gebruikt in backup.rs restore + deploy.rs:393 |
| F39 | `restic_base`, `password_file` and `snapshot_timeout_s` live in `BackupCfg::default()` and are absent from `FileConfig` | SCOPE G7 wants a target on the HDDs and on Drive; the code can address one, as a string literal (standing rule 27) | done 2026-09-02 — nagemeten: restic_base/password_file/beide timeouts staan in FileConfig, alle vijf bouwplekken erven uit state.config.backup |
| F33 | Unverified, because F32 blocked it: the v1 script greps for `"IsPlaying"`, which is not a field on Jellyfin's session objects as far as I know — `NowPlayingItem` and `PlayState.IsPaused` are. If so the check never matched and allowed every update | Check against a live session once a working key exists; do not assume either way | done 2026-09-02 — nagemeten: core/src/ops/busy.rs:54 — leest NowPlayingItem en PlayState.IsPaused, vermoeden bevestigd |

## Tasks (T)

| # | Task | Depends on | Status |
|---|---|---|---|
| T1 | Phase 1 · full inventory of every guest, its services and its hand-tuned configuration | D1 | done (`INVENTORY.md`) |
| T16 | Repair the kyu stack entry in homelab state (re-adopt under its real name/paths) and re-enable its nightly run — F7 | T1 **Verified against reality 2026-09-01:** the `kyu` stack is in host state. | done 2026-09-01 |
| T17 | Bring `/etc/homelab/host.toml`'s `no_touch` in line with the Phase 0 decision, per container — F8 | T1 **Verified against reality 2026-09-01:** the built-in list is `[100, 101, 102, 103]` and those are exactly the four guests that must never be touched — VM 100 OPNsense, VM 101 Home Assistant, CT 102 omada, CT 103 fileserver. The k3s VMs 201-203 the Phase 0 decision also named no longer exist (`qm list` shows only 100, 101 and a template). Nothing to bring in line. | done 2026-09-01 |
| T18 | Decide where MQTT terminates, then remove the stale `lxc-mqtt-stack.yml` route — F10 | T1 **Kenny, forms R6 and V4 (2026-09-01): closed as already done, after he asked to check first.** Measured: no mqtt route file in the gateway's route directory and no mqtt entrypoint in Traefik's command line — the cleanup went with the gateway rebuild. His condition stands structurally: the MQTT server on the Home Assistant VM is vmid 101 on the no-touch list, re-checked at every operation. | done 2026-09-01 |
| T19 | Move ansible-era configuration onto `/appdata` on the host so a rebuild survives — F11 | T1 **Kenny, forms R7 and V3 (2026-09-01).** He asked for case-by-case; measuring collapsed thirteen cases into two. Every container already has its configuration on `/appdata` (at least one mount each, six on the gateway and media), and what lives inside is 32-128 KB of compose files. No docker named volumes hold data anywhere. The single exception is CT 112: 33 MB of almanac binaries plus rollback copies, which belong there, and two renamed migration leftovers — `data.migrated-20260901` and `profiles.migrated-20260901`, 16 KB. Kenny chose to remove those two after a look and leave the rest. **Looked at 2026-09-02, and the facts had moved.** Two of the three named leftovers no longer exist anywhere: `data.migrated-20260901` and `profiles.migrated-20260901` are gone from CT 112 and from `/appdata`, while the live `data/`, `profiles/` and `latch.env` sit correctly under `/appdata/almanac/almanac-config/`. What remains is ONE file, and it is a different category from what the row described: `/etc/almanac/latch.env.migrated-20260901` inside CT 112 — 449 bytes, root-only, carrying `LATCH_KEY_ALMANAC`. It is byte-identical (sha256 22beec63…) to the live `/appdata/almanac/almanac-config/latch.env`, and `systemctl show almanac` confirms the unit reads the /appdata copy, not this one. So it is a duplicate of a SECRET in a location that is on no /appdata mount and therefore in no restic repository — a key nobody would think to rotate. Removing it is safe and is what Kenny decided; the remote delete was refused by this session's tool policy, so it is the one thing left, and it is one command. **Done 2026-09-02.** Kenny gave the go (form X3) and the file is gone: `pct exec 112 -- ls /etc/almanac/` now returns nothing. almanac stayed active throughout and its healthz answered 200 afterwards; the live copy on /appdata is untouched.| done 2026-09-02 |
| T20 | Capture the Cloudflare tunnel ingress and Access policies into the repo — F12 | needs Cloudflare credentials **Done 2026-09-02** with a read-only token Kenny supplied. Captured in `docs/deployment/CLOUDFLARE.md`: the whole edge is two DNS records, one tunnel and one ingress rule — `*.kp-soft.dev` to Traefik on CT 104, everything else 404. Three Access applications, and reading them found something worth naming: `sp.kp-soft.dev` carries a **bypass** policy including **everyone**, so "everything behind kp-soft.dev sits behind Cloudflare Access" is not true and has one deliberate exception. The document also names what a rebuild still would NOT have — the tunnel token, the kobo service-token secret and the Google client secret are all write-only — because a capture that pretends to be complete is worse than one that names its gaps. The token is in no file in this repository. | done 2026-09-02 |
| T21 | Add promtail to CT 104 — F13 | T1 **Verified against reality 2026-09-01:** promtail runs on CT 104. | done 2026-09-01 |
| T22 | Give Uptime Kuma a real monitor set — F9 | 29 monitors added, verified 30 of 30 green: HTTP per service, ping per container, two external hostnames for the tunnel. Script and reasoning in `captured/gateway/uptime-kuma/` | done 2026-08-31 |
| T23 | Remove or rewrite the three stale stack files claiming live vmids — F17 | T1 **Obsolete, measured 2026-09-01:** `stacks/` holds exactly thirteen directories and every one matches a live container. The three stale files are gone — the drill stack was the last thing added and removed the same evening. | dropped 2026-09-01 |
| T24 | Verify whether restic retention explains the snapshot counts — F19 | T1 **Kenny, form R8 (2026-09-01): Onmisbaar.** Includes removing the `app-config` repository the drill stack left on Google Drive — one snapshot of six bytes. Destroy removes routes and dashboards but deliberately never a backup. **Answered 2026-09-01, and the answer was not about retention.** All 33 repositories counted: nothing has accumulated — most hold 1 or 2 snapshots because they are days old, `host-meta-config` 5, `syncthing-config` and `synctest-config` 6 each. The tiers are not the problem. What the count DID find is F165: four gateway repositories at zero. Three stale repositories also surfaced — `mailbox-config` (kyu's name until 2026-08-29), `synctest-config` and `metrics-config`, all superseded by renamed ones and deliberately kept as history per T45. | done 2026-09-01 |
| T25 | Recyclarr preset (E3) | T1 **Duplicate of T7**, measured 2026-09-01. Two numbers for the Recyclarr preset; T7 carries it. | dropped 2026-09-01 |
| T26 | Adopt CT 111 so SuperSync gets backups — F15/R7 | T1 **Verified against reality 2026-09-01:** the `productivity` stack is in host state. | done 2026-09-01 |
| T27 | Delete CT 107; MQTT terminates on the Home Assistant VM (Kenny, 2026-08-30), so the `lxc-mqtt-stack.yml` route and Traefik's `mqtt` entrypoint go with it | T1 **Obsolete, measured 2026-09-01:** CT 107 was reused for the uptime stack instead of deleted (D68). | dropped 2026-09-01 |
| T28 | Make the clone path honour `unprivileged`, or refuse the combination loudly — F21 | T1 **Kenny, form R4 (2026-09-01): Onmisbaar**, together with T29. **Verified against reality 2026-09-01:** the clone path already refuses the combination loudly — it reads the template's own `unprivileged:` line and aborts naming the template, what it is, and what the stack asked for. What it did NOT have was a test, which is T29. | done 2026-09-01 |
| T29 | Add privileged-container test coverage — F22 | T28 **Kenny, form R4 (2026-09-01): Onmisbaar**, together with T28. **Done 2026-09-01.** Two tests: a stack asking for privileged while the template is unprivileged is refused and nothing is cloned; a privileged stack cloning the privileged template passes and does clone — the path CT 105 and 106 actually take for the GPU. Sabotage-verified by disabling the guard, which makes the first fail. Tests: `a_clone_refuses_a_privilege_level_the_template_cannot_give`. | done 2026-09-01 |
| T30 | Validate `host_owner_uid` against the privilege level — F23 | T1 **Verified against reality 2026-09-01:** `validate` refuses a `host_owner_uid` below the 100000 mapping on an unprivileged stack and one above it on a privileged one, naming the number to use instead (`manifest.rs`). | done 2026-09-01 |
| T31 | Close the E3 auto-restore granularity gap, test-first — D13 | T1 **Kenny, form R10 (2026-09-01): Onmisbaar.** **Done 2026-09-02.** The gap was per-PATH, not per-stack: E3 asked one question for a whole stack, so a stack with one empty mount and one full one either restored over live data or restored nothing. The auto-restore now decides per mount, skips any mount declared `no_data: true` (empty by design is not the same as empty by accident — F154), and restores only the paths that are genuinely empty AND have a snapshot. Written test-first and sabotage-verified. Test: `e3_restores_the_empty_path_and_leaves_its_full_sibling_alone` | done 2026-09-02 |
| T32 | Enforce the `<app>-config` naming rule in `validate_manifest` — D14 | T1 **Verified against reality 2026-09-01:** `validate` refuses a storage path not named `<owner>-config`, and refuses one whose name disagrees with its declared app (`manifest.rs`). | done 2026-09-01 |
| T33 | Decide where vzdump archives live long-term — F24 | T1 | done (D19) |
| T34 | Build a second golden template, privileged, beside the unprivileged one — D18 | T28 **Verified against reality 2026-09-01:** CT 997 `debian-12-homelab-v3-priv` exists beside CT 998, and the media and downloader stacks clone from it. | done 2026-09-01 |
| T35 | Ask the kyu session to publish release binaries + checksums, so the orchestrator can update it — Z4 | — **Done 2026-09-02.** The kyu session was running after all and answered the same night: release v2.1.0 carries `kyu` and `SHA256SUMS`, verified here independently. Same outcome as F168/T72, which this row duplicated under a second number. | done 2026-09-02 |
| T36 | Mint a fresh Jellyfin API key; the one on CT 106 is refused — F32 | needs Kenny or Jellyfin admin access **Confirmed still open 2026-09-01:** the repo carries only a comment saying the key is deliberately absent, yet `/opt/media/jellyfin/.env` on CT 106 still holds `JELLYFIN_JELLYFIN_API_KEY` — it comes from the vault, not the repository, so removing it from the repo never removed it. **Done 2026-09-01:** the key was removed from `/var/lib/homelab/secrets/media/jellyfin.env` (a backup of the old file kept beside it) and the media deploy carried the change; `grep -c JELLYFIN_API_KEY` on CT 106 now answers 0. It had to be removed from the VAULT — taking it out of the repository never touched it, because `.env` comes from the vault (D12). | done 2026-09-01 |
| T37 | Fix the syncthing route: `10.10.10.10` → `10.10.10.8` — F34 | — **Verified against reality 2026-09-01:** no stack file references 10.10.10.10 any more; only a comment recording the correction. | done 2026-09-01 |
| T38 | Make the restore timeout configurable and raise it — F38 | — **Verified against reality 2026-09-01:** `restore_timeout_s` is a configured field. | done 2026-09-01 |
| T39 | Bring the restic target into `FileConfig` so a second target is expressible — F39 | — **Verified against reality 2026-09-01:** `restic_base` is part of the host FileConfig. | done 2026-09-01 |
| T40 | Let `native.rs` accept an explicit "no state, by decision" — F36 | T5 **Kenny, form R11 (2026-09-01): Onmisbaar** — the same shape as the `no_data` flag he proposed himself, for native services. **Verified against reality 2026-09-01:** already built. `core/src/native.rs` carries a `stateless: bool` and validates it both ways — an empty `data_dirs` without the flag is refused, and the flag beside declared directories is refused too, so the two cannot silently disagree. Four tests exercise both sides. Kenny rated it Onmisbaar in form R11 without either of us checking; the measurement says there is nothing left to build. | done 2026-09-01 |
| T41 | Propose snapshot-before-update as a third protection layer in the realisation plan — F40 | — **Kenny, forms R12 and V6 (2026-09-01): later, and recorded as an explicit question to the failure-behaviour lens** rather than as a task that happens to pass by — 'remember it' is exactly what went wrong with four other things tonight. | open — vraag aan de expertenronde |
| T42 | Repair the syncthing route: 10.10.10.10 → the container — F34, B4 | — **Verified against reality 2026-09-01:** same as T37 — the route points at the container. | done 2026-09-01 |
| T43 | Correct `presets/syncthing/` off vmid 110, which can never be used | — **Obsolete, measured 2026-09-01:** `presets/syncthing/preset.yml` carries no vmid at all — presets never did. There is nothing to correct off 110. | dropped 2026-09-01 |
| T52 | **Done 3.9.0.** Make the deploy restart an app whose mounted config file changed. Today `docker compose up -d` leaves a container alone when only a bind-mounted file differs, so an edit to any config in the fleet can silently not take effect — F80 | — **Verified against reality 2026-09-01:** the row already said "Done 3.9.0"; only the status column had never followed. | done 2026-09-01 |
| T56 | **Done 3.11.0.** Proven the honest way: the hand-made `docker login` was REMOVED from CT 116, a pull was confirmed to fail with `unauthorized`, and the deploy then signed in and pulled with no manual step at all | Teach the deploy about private registries: a stack that names an image behind a login should carry that credential the way it carries its `.env`, so a container rebuilt from scratch does not need a hand-run `docker login` nobody documented **Verified against reality 2026-09-01:** the row already said "Done 3.11.0"; only the status column had never followed. | done 2026-09-01 |
| T60 | **Phase 2 mandatory item 4 — data & config location** (added to the dev procedure 2026-08-31, standing rule 28; Kenny named this project as the reason). Every project not yet at Phase 10 inherits a new mandatory item in its queued mini-rounds on the day it is added. Written up in `QUEUED_MINI_ROUNDS.md` with what this project already has: `/var/lib/homelab` has its knob (`HOMELAB_STATE_DIR`), the `/appdata` prefix does not — it is enforced in `manifest.rs`. F125 is the same item in miniature | procedure change | queued — becomes a form, nothing built |
| T59 | Put a pull-through registry cache on the LAN so an image pull is not on the critical path of a container replacement (F108). Every image would then come off the local network on the second and later pull, and ghcr's rate limiting would stop being able to stretch a ninety-second replacement into eleven minutes. It is a new service, so it is Kenny's decision, not a repair | F108 **Verified against reality 2026-09-01:** four cache containers run on CT 117, and the fallback was drilled on 2026-09-01. | done 2026-09-01 |
| T58 | The `lru` advisory (low) is held by `ratatui 0.29`; closing it means a major TUI-library upgrade | Done the same day after all: Dependabot offered 0.30.2, which brings `lru 0.18.3`. The upgrade compiled with no code changes, 188 tests green, and the TUI was verified running rather than assumed — `scripts/tui-smoke.py` drove it in a pty and it drew 81 KB with the whole tab bar intact. **All seven advisories are now closed** | done 2026-08-31 |
| T57 | Restrict CT 116 in OPNsense so the one container strangers can reach cannot reach the rest of the house. Kenny accepted the exposure deliberately (K5) and asked for this to be planned separately rather than block the site | — **Kenny, form X7 (2026-09-02): he will give an API key, and asked for exactly what to create and which rights are needed.** Written up in `docs/deployment/OPNSENSE_API.md`: the two privileges are separable (Kea DHCP for H2, Firewall Rules + Apply for T57), the apply/reconfigure right is called out as not optional because a rule saved and never applied protects nothing, and the file states what those two pages cannot do — an API user with only them cannot lock Kenny out of his own router. **The API key exists and works, 2026-09-02.** Kenny created user `homelab-api` with a scrambled password, no group, no certificate, no OTP — so the account cannot log in interactively at all and the key is its only way in. Three privileges and no more: `Services: DHCP: Kea(v4)`, `Services: DHCP: Kea Ctrl Agent`, `Firewall: Rules [new]`. **A correction Claude owed him:** the earlier write-up named a `Firewall: Apply` privilege, which does not exist in this OPNsense — it was written from memory, and the real list Kenny pasted showed it. Verified against the live router: Kea reports four subnets and twelve existing reservations, and `firewall/filter/searchRule` answers 200 with 41 rules, so both H2 and T57 can reach what they need. The credential is stored twice because Kenny deleted his own copy — `/var/lib/homelab/secrets/opnsense.cred` (root-only, one line `key:secret`, and therefore inside the H10 host-meta backup) and latch at `opnsense/api/.env`. `host.toml` now carries `opnsense_url` and `opnsense_cred_file`; the daemon restarted healthy and the exact command shape the code uses answers 200 from the host itself. What remains is the rule itself. **Rule applied and measured 2026-09-02** (Kenny, forms Z11/Z12/Z13: all four subnets, all three exceptions, now). Four rules on `opt1` (Trusted): three passes at sequence 10–12 for DNS (10.10.5.1:53), Loki (10.10.10.4:3100) and the registry cache (10.10.10.17:5000-5003), then a logged block at 20 for `10.10.10.16 → 10.10.0.0/16`. Baseline taken before, same six readings after: site 200, DNS resolves, Loki 200, cache 200, internet reachable, Proxmox host now blocked. **And one reading that did NOT change: Jellyfin on 10.10.10.6 still answers.** That is F183, and it is a limit of where the rule sits rather than a mistake in it.| partly done — F183 open 2026-09-02 |
| T55 | Enable kyu-runner's second route, `kyu.events` → HA, which is the runner's own designed purpose (P8): the hub's dead-lettered messages and archived subscriptions become warnings in the house. It needs an HA automation on webhook `hub_kyu_events` FIRST — HA answers 200 to an unknown webhook id, so a route enabled before its automation acknowledges warnings into the void | kyu-runner is deployed and healthy **Kenny, form R15 (2026-09-01): Onmisbaar.** The Home Assistant automation must exist first — HA answers 200 to a webhook it does not know, so a route enabled before its automation acknowledges warnings into the void. **Done 2026-09-02, in the order the config file demanded.** The Home Assistant automation was built FIRST (`automation.hub_kyu_events_webhook`), then smoke-tested with a real POST carrying a `subscription.flagged` payload, and only then was the route enabled — HA answers 200 to a webhook id it does not know, so proving the automation is the whole point of the order. The trace confirms the right branch fired and that `idle_ms: 900000` rendered as "15 minuten". Five event kinds are handled with deliberately different severities: dead-lettered and archived are warnings because something was lost or stopped; expired, flagged and unarchived are information about normal lifecycle. No default branch. The webhook id is opaque (`hub-kyu-events-2c3c201e`, Kenny form X2) rather than the guessable `hub_kyu_events` the register named — it is a capability token, and the two existing webhooks already work that way; this row is corrected to reality rather than the reverse. kyu-runner now reports 3 routes and says `kyu.events` is not born yet, which is correct: kyu creates a topic on first publish and AR16 replays it from the beginning. **Remaining measurement:** the first real hub event is the end-to-end proof, and it has not happened yet — manufacturing a dead letter on a live hub is not worth it. Recorded as M-T55. **Follow-up shipped the same night.** Kenny asked how the "15 minuten" in the smoke test was produced; explaining it surfaced a real defect in what had just been built. The hub sends `idle_ms`, and the automation divided by 60000 — correct, but the hub's own flag threshold is a week, which renders as "10080 minuten": the unit right, the scale useless. Now rendered in a unit that fits the size, with singular handled. Verified live against all four scales: 60000 ms gives "1 minuut / 1 minute", 900000 gives "15 minuten", 10800000 gives "3 uur / 3 hours", 604800000 gives "7 dagen / 7 days". | done 2026-09-02 |
| T54 | Re-point the syncthing preset off vmid 110 (was T43) — the stale `stacks/syncthing` manifest it referred to is deleted, so the preset is the last thing naming a container that can never exist | — **Obsolete, measured 2026-09-01:** same as T43 — the preset holds no vmid, so there is nothing pointing at 110. | dropped 2026-09-01 |
| T53 | Apply the log tag to CT 104, 105 and 106 — done 2026-08-31 with Jellyfin idle and nothing downloading; the gateway's containers were recreated too and the edge verified afterwards (four public hostnames, all 302 through Cloudflare Access). Loki went from 0 to 19 container names. The three unmanaged promtail configs were captured into `captured/` first, so this repair cannot expire the way F82's did | Apply the log tag to CT 104, 105 and 106: their containers must be recreated for the daemon's tag option to take effect, and all three were deferred — 104 is the edge, 105 would interrupt downloads, and Jellyfin was mid-stream on 106 **Verified against reality 2026-09-01:** the row already said "done 2026-08-31"; only the status column had never followed. | done 2026-09-01 |
| T51 | Homepage lists every service by hand in `services.yaml`. The orchestrator already knows every stack, its apps and its routes; rendering the page from the manifests the way T1 renders Prometheus targets would mean a new stack cannot be missing from the front page | — **Kenny, form R14 (2026-09-01): Onmisbaar.** Measured the same evening: every Homepage config file holds zero service entries and `services.yaml` is 0 bytes, so the front page renders and lists nothing. **Done 2026-09-01.** The front page is rendered from the route fragments this orchestrator already writes for the whole fleet, in the same family as T1's Prometheus targets and T2's Grafana dashboard. Fleet-wide rather than per stack, because Homepage keeps one file: the step reads every fragment in the gateway's route directory, not only the one it just wrote. It lists the HOSTNAMES rather than internal addresses — the front page is the front door — and a stack with no route is skipped rather than rendered as an empty heading, because an empty heading reads as broken when it means 'no front door'. Best-effort, like its two siblings. Test: `a_route_fragment_yields_its_hostnames_and_its_backends`, which also pins that a backend address may never reach the page. | done 2026-09-01 |
| T49 | Render an Uptime Kuma monitor set from the manifest at deploy time, the way T1 renders the Prometheus targets and T2 the Grafana dashboard. Today a new stack arrives unwatched until someone re-runs the seed script by hand | The seed script exists and is idempotent, so this is a port rather than a design **Confirmed still open 2026-09-01:** nothing in `core/src/ops/` mentions Uptime Kuma; no monitor is rendered. **Kenny, forms R13 and V2 (2026-09-01), correcting Claude.** Claude wrote that Uptime Kuma has no way to add monitors from outside; Kenny asked how the existing 36 got there. They were seeded by `captured/gateway/uptime-kuma/seed-monitors.py` using the `uptime_kuma_api` library, deliberately idempotent. So it is automatable and always was. Kenny chose to use the script rather than reimplement its protocol in Rust — a protocol its authors do not offer as a public interface is exactly what breaks silently on an update. **Done 2026-09-02.** Split the way D87/D90 decided. The MECHANICAL half — one ping monitor per managed container — is rendered by the orchestrator at deploy time into `/appdata/uptime/kuma-seeder-config/host-monitors.json`, fleet-wide, the same shape as the T1 scrape targets and the T51 front page. The APPLICATION half stays hand-written in `seed.py`, because `/health` versus `/ping` versus `/up?strict=1` is knowledge no manifest holds. The seeder is an app in `stacks/uptime/` (Kenny, form W1), loops hourly so a stack created after a deploy does not wait, and reports stale monitors without ever deleting one (H2b). Its Python library lives in a docker volume, not on /appdata, so a reproducible dependency never enters a restic repository. Test: `the_generated_file_is_json_the_seeder_can_read`, `an_empty_fleet_still_renders_a_file_the_seeder_can_parse`, `a_stack_name_with_a_quote_in_it_cannot_break_the_file`, `every_stack_manifest_agrees_with_the_directories_beside_it` | done 2026-09-02 |
| T50 | Give promtail a real container label fleet-wide, so the three Loki dashboards (F72) can work at all. Two candidate routes: retry `docker_sd_configs` on CT 108 where an experiment is cheap (F69), or set docker's `tag` log-opt in the guards and read `attrs.tag` in a promtail pipeline stage — the second needs every container recreated to take effect | — **Verified against reality 2026-09-01:** 11 promtail configs carry `container_name: tag`; Loki holds the label fleet-wide (F79). | done 2026-09-01 |
| T47 | Re-run the paperless import verification after Kenny has looked at the archive: the 25 documents came back with full-text search but without the tags, correspondents and document dates he assigned in March, which existed only in the lost database | — **Kenny, form X8 (2026-09-02): "Er moet hier niks meer mee gebeuren, ik handel het verder af."** Out of this project's scope by his decision; the archive check and whatever follows from it are his. | closed by Kenny 2026-09-02 |
| T48 | The old `/HDD2TB/paperless_media` and `/HDD2TB/paperless_consume` datasets are now source material with no consumer. Keep them until Kenny confirms the re-import is good, then they can go | — **Kenny, form X8 (2026-09-02): he handles it further himself.** The two datasets stay until he says otherwise, and nothing here will remove them. | closed by Kenny 2026-09-02 |
| T46 | **Release the host binary.** M3's real templates, M4's live half and every M2 fix only take effect once the host runs the new code. First thing that waits on Kenny rather than on work | 42 commits ready, 20 suites green | waiting on Kenny |
| T44 | Send kyu-runner two config corrections: `healthz_listen` 8081 → 8082 (cadvisor owns 8081 fleet-wide) and `webhook_url` to an IP instead of `homeassistant.lan` (C3) | — **Confirmed still open 2026-09-01:** kyu-runner answers nothing on :8082 (`000`). **Correction, same day:** the first check probed `127.0.0.1:8082` and got nothing. kyu-runner binds `10.10.10.9:8082`, which answers 200. Done, and the 'confirmed open' above was my own wrong address. | done 2026-09-01 |
| T45 | Migrate the existing stack-named restic histories to their per-app repositories with `restic copy`, BEFORE those stacks are next deployed — `metrics-config` → `prometheus-config`, `synctest-config` → `syncthing-config` — D25 | Counts verified on both sides: 1 → 1 and 7 → 7. The source repositories are left in place as history, nothing was deleted | done 2026-08-31 |
| T2 | Bring `stacks/metrics/prometheus/prometheus.yml` level with the live one (almanac job, node job ×11, cadvisor job ×6) | The repo file is now the live one and better: the hand-kept node and cadvisor lists are replaced by file discovery, the `mailbox` job is renamed to `kyu`, and the dead `scratch-mailbox` entry is gone | done 2026-08-31 |
| T3 | Add Alertmanager + its four rules + the `rules/` mount to the metrics stack | T1 **Verified against reality 2026-09-01:** Alertmanager runs on CT 113 with 5 rules loaded. | done 2026-09-01 |
| T4 | Add node_exporter and the SMART textfile collector as managed artifacts | T1 **Confirmed still open 2026-09-01:** node_exporter answers on :9100 on the PVE host, but there are 0 SMART textfiles — the collector half is genuinely open. **Correction, same day:** the first check looked in `/var/lib/node_exporter/` and found nothing. The collector writes to `/var/lib/prometheus/node-exporter/` — `smartmon.prom` 33 KB, `nvme.prom`, `apt.prom`, all rewritten at 22:12. Done, and the 'confirmed open' above was my own wrong path. | done 2026-09-01 |
| T5 | Add cadvisor to every docker host, not only the metrics stack | T1 **Verified against reality 2026-09-01:** cadvisor on 10 of 10 managed containers, after F164. | done 2026-09-01 |
| T6 | Add the Prometheus datasource and three dashboards to Grafana provisioning in the repo | T1 **Verified against reality 2026-09-01:** 2 datasources and 7 dashboards committed under `stacks/gateway/grafana/provisioning/`. | done 2026-09-01 |
| T7 | Adopt the Recyclarr preset (vault mini-round MR-2) | T1 **Kenny, form R1 (2026-09-01): Onmisbaar** — before the expert round. T25 is the same task under a second number and is dropped into this one. **Done 2026-09-02.** Built from the decision set R1-R11, which was already frozen in the vault (`Homelab Planned Services` §5 and `Homelab Pending Mini-Rounds` MR-2) — Claude had parked this as needing Kenny's input and Kenny corrected that. `presets/recyclarr/` carries the compose file, the config and an env example. Two absences are deliberate and are held by a test: no `quality_definition` block (the size caps R2/R3/R11 are measured and already set live, and Recyclarr can only scale the whole TRaSH table by one ratio), and no raw `trash_id` hashes (an earlier draft of the file had four written from memory; they are resolved at deploy time with `recyclarr list custom-formats`). Deploying the preset is a separate act — it changes live profiles. Test: `the_recyclarr_preset_keeps_its_two_deliberate_absences`, `every_shipped_preset_scaffolds_a_valid_stack` | done 2026-09-02 |
| T8 | Write the HTTPSwitchboard preset and decide its container (S2) and config location (S3) | T1 **Confirmed still open 2026-09-01:** `presets/http-switchboard/` does not exist. **Kenny, form R2 (2026-09-01): Niet doen.** HTTPSwitchboard already runs as a native service on CT 109, is described in `stacks/kyu/service.yml`, and has carried its own checks since 2026-09-01. A preset would be a second description of the same thing. | dropped 2026-09-01 |
| T9 | Deploy kyu-runner: HA automation first, then route, then smoke test | T8, T3 **Verified against reality 2026-09-01:** `kyu-runner` is active on CT 109. | done 2026-09-01 |
| T10 | Golden LXC image with docker, unattended-upgrades, node_exporter, cadvisor, promtail | T1 **Kenny, forms R3 and V1 (2026-09-01): not building it.** His own question settled it: a preset cannot be promoted into a golden image because a preset is mostly placeholders — stack name, vmid, ip — filled in at scaffold time. What CAN be pre-loaded is the docker image, and the pull-through cache on 10.10.10.17 already does that, for existing containers as well. A golden image only ever reaches what is cloned after it, which is the F164 weakness in another form. | dropped 2026-09-01 |
| T11 | Extend C7 so the orchestrator can create a container and install a Rust binary into it | T1 **Kenny, form R9 (2026-09-01): Onmisbaar**, and he asked for more than the task says: a container that can carry BOTH Rust binaries and docker containers, rather than the current split between native-only and docker-only. **Done 2026-09-02.** The container half already worked — `homelab deploy` creates a `native_only` stack and skips the docker bootstrap — so what was missing was the install. `homelab install-native stacks/<name>[/<unit>] [tag]` downloads the release asset with Kenny's authenticated `gh`, verifies it against the release's SHA256SUMS on the DESKTOP (so the Proxmox host never needs a GitHub credential and never sees an unverified binary), and ships it over the same TLS line H7 uses. On the container it stages beside the target, installs the unit file, reloads systemd, then moves into place and activates — so a transfer that dies half way leaves the running service on its own binary. A re-install rolls back to the preserved binary; a FIRST install says plainly that there is nothing to roll back to instead of claiming a rollback that cannot exist. It ends by recording the same state adoption records, so a service built this way and one taken over by hand are indistinguishable afterwards. Release sources are declared per service, measured: almanac and http-switchboard use their unit name, kyu-runner names its target-triple asset, and kyu has none — F168. Test: `an_install_stages_beside_the_target_before_it_replaces_anything`, `a_first_install_that_fails_says_there_is_nothing_to_roll_back_to`, `a_reinstall_that_fails_returns_to_the_binary_that_was_running`, `an_install_refuses_a_unit_file_that_runs_a_different_binary`, `an_install_without_a_unit_file_is_refused`, `a_release_source_that_is_half_declared_is_refused` | done 2026-09-02 |
| T12 | Full backup to the attached HDDs plus a completed Google Drive run, restore drilled | everything above **Kenny, forms R5 and V5 (2026-09-01): Onmisbaar, but the LAST test, once everything else is addressed.** Three parts, all of them: (1) a fresh backup of every stack with file and byte counts noted, none may report zero files; (2) a real restore onto a throwaway container — destroy, redeploy, let the data come back from Google Drive, count; (3) regenerate the DR runbook and FOLLOW it as if the host were gone, until the first step that does not hold. **Part 1 done, part 3 started and already earning its keep (2026-09-02).** Part 1: the whole fleet backed up — 18,892 files, 14.8 GB, eleven of thirteen stacks clean; the two failures were real (F170, F172) and both are now fixed or decided. Part 3, following the runbook as if the host were gone, stopped at the FIRST claim in Layer 0: it names two data disks and there are four, and CT 103's 13.2 TB lives on three of them — `HDD12TB/subvol-103-disk-0` 8.40 T, `HDD18TB/subvol-103-disk-0` 4.78 T and `HDD4TB/subvol-103-disk-0` 45.3 G. Following it after a host loss would have re-attached two pools and missed two. Looking closer found F177. Layer 0's text still has to be rewritten, and part 2 (the real restore onto a throwaway container) has not run. **Part 2 DONE 2026-09-02** (Kenny, form Z8: after the host update, so the proof holds for the host that runs from now on). A throwaway stack on vmid 118 (rule B1), created and destroyed in the same sitting. 300 files and 1,038,392 bytes written with a known fingerprint; backed up (restic reported exactly 300 files, 1,038,392 bytes); container destroyed; `/appdata` then **wiped by hand**, because a destroy keeps it deliberately and a restore that finds the data still sitting there proves nothing; redeployed. E3 saw the empty directory, found the snapshot and restored 304 files/dirs in 7 seconds from Google Drive. **Byte-perfect:** 300 files, 1,038,392 bytes, and the same fingerprint `cddf2843995ec092e1094754ddf6952c` — a sha256 over every file's sha256, so identical means identical. nginx then served a restored file over HTTP from inside the container, which proves the ownership survived too, not only the bytes. Every trace removed afterwards: container, `/appdata/drill`, the restic repository and the stack directory. **Part 3 remains open** — Layer 0 is corrected, the rest of the runbook has not been walked. **Part 3 finished 2026-09-02, and it earned its keep four times.** Layers 1 and 2 hold as written — the daemon commands work, the compose files are where it says, the intent repo has history. Layer 3 works literally, and the repository list is honest: every name the document gives exists on Drive. What it found: the wrong disk count (fixed), F177 the replica on a live path, F180 the host config in no backup, and F181 the missing host-restore layer. Three of those four could only have been found by FOLLOWING the document rather than reading it. | done 2026-09-02 |
| T13 | Clean up CT 107 | T12 **Obsolete, measured 2026-09-01:** CT 107 was not cleaned up but REUSED — it is `107-app-uptime` since D68. | dropped 2026-09-01 |
| T14 | Clean up CT 190 and 191 after coordinating with pipeline-v2 | T12 **Verified against reality 2026-09-01:** CT 190 and 191 no longer exist. | done 2026-09-01 |
| T15 | Rename the stale worktree branch `hungry-elbakyan-85a663` under `.claude/worktrees/` to something descriptive, or remove it | — **Obsolete, measured 2026-09-01:** there is no `.claude/worktrees/` directory in this checkout any more. | dropped 2026-09-01 |
| T62 | **Expert review of every setting in every managed LXC**, run in a SEPARATE session so it is not the session that just defended those choices; its report comes back into the rollout conversation, which decides what to change (Kenny, 2026-09-01) | media + gateway both live | open |
| T67 | **A round on software worth building ourselves**, at whatever moment fits (Kenny, 2026-09-01), **and the standing option it implies: when a need falls outside this project's scope, starting a separate project that meets it is a legitimate answer rather than a detour.** Both belong in the retrospective. His own example: an HTTP checker configurable per service, so "is this service healthy" is a declared expectation rather than a hand-written curl. The evidence that it is needed came the same day — the ad-hoc verification script used for the gateway lied three times: it counted `403` as a pass while the bouncer was blocking every hostname, reported Grafana as 0 dashboards when there were 20 (broken quoting), and measured Cloudflare Access rather than the services behind it. Overlaps J1/J2 (checks per service, measured as a before/after pair); the round decides what belongs in the orchestrator and what is its own project | T64 | open |
| T70 | **A mini-round on what else syncthing could do for Kenny** (his request, 2026-09-01), held immediately BEFORE the retrospective. The hub now exists, is configured and is proven to work for the Obsidian vault; the question is what else deserves an always-on peer — phone photos, a scan drop folder, project directories, something between machines that currently moves by hand. Deliberately after the rollout and before the retro, so the answer can still land in this project's own conclusions | open |
| T71 | **The native-service family in the TUI.** Four of the five C7 verbs — adopt, backup-native, update-native and now install-native (T11) — are reachable only from the command line. Each was excused individually as "a later round", which is three separate deferrals of the same round; the fourth is written here instead so the gap has a number rather than a comment. The rule it violates is Kenny's (form B4b, 2026-08-31): the TUI must be able to use every feature of this project | The reachability test already holds the question open — a new `Command` variant fails the suite until it is either wired in or listed with a reason. Test: `every_host_operation_is_reachable_or_deliberately_not` **Done 2026-09-02, the same day it was written.** All four verbs are reachable and the CLI_ONLY excuse list no longer names any of them. `B` and `U` on a native stack send BackupNative and UpdateNative instead of their compose counterparts — the same key, the same intent, the right RPC; the sabotage run showed what the old path would have done, namely back up CT 109's three /appdata mounts and none of the state that lives inside the container. `A` adopts every service the stack declares, one command per unit. `I` installs the binaries, and because `gh` is a network fetch the event loop may not block on, the staging happens off-thread in the same shape H7's release update already uses, feeding progress into the focus window. Services with no release source are skipped with a line rather than attempted, which is what kyu needs today (F168). Restore stays command-line for native stacks and now says so out loud instead of doing nothing. Test: `a_native_stack_gets_the_native_operation_from_the_same_key`, `every_native_service_in_the_repository_is_found_with_its_unit_file`, `every_host_operation_is_reachable_or_deliberately_not` | done 2026-09-02 |
| T76 | **A report at the retrospective on how many rounds this project took, plus whatever else the numbers say** (Kenny, 2026-09-02). He also flagged it as a candidate to become a permanent step in the procedure, to be decided at the retro itself rather than now. **The part that cannot wait:** some of it is reconstructable afterwards and some is not. Git history, the register's own dated rows, findings per session and tests-over-time all survive; how many forms were sent, how many items Kenny bounced, and how many decisions arrived as prose instead of a form are in the transcripts, and transcripts are what a compaction eats. Claude should capture those counts as the project runs rather than promise to dig them up later | Kenny, 2026-09-02 **Started 2026-09-02, and started rather than promised on purpose.** `docs/deployment/RETRO_DATA.md` now carries this session's numbers, taken from git and the register rather than from memory: 63 commits, +8378/−1616 lines, 311 tests, 24 findings raised of which 18 were fixed the same session, and 8 forms holding 49 items. It also records the two forms Kenny bounced and the six corrections Claude had to make to its own work — none of which exists anywhere but the transcript, which is exactly what a compaction eats. Updated at the end of each session; the reading of it belongs to the retrospective, so the file is deliberately data and not analysis.| running — updated per session |
| T77 | **Switch the kyu backup to the self-made copy** (D94). Blocked on F179 — the kyu project fixes the hardcoded path in its helper. When their release lands: verify a fresh `kyu.backup-<epoch-ms>.db` exists (newest by glob, not a fixed name — the script keeps 7 and prunes by COUNT, deliberately, so a week of failed runs cannot quietly remove the last good copy), then change the native backup to archive that file instead of the live store, and drop the stop-for-the-copy. **Restore note that must travel with it:** such a copy is a COMPLETE database, not a part. Put it back as `kyu.db` and delete any `kyu.db-wal` and `kyu.db-shm` beside it — they belong to the old file and confuse SQLite about what it is looking at. That is the reverse of the moving rule, where those three must travel together | D94, F179 | open — blocked on F179 |
| T75 | **Parallel nightly backups** (Kenny, form Y1: Onmisbaar; form Y4: three at a time). **Done 2026-09-02.** The measurement that produced it: a full round took ~38 minutes for thirteen stacks, of which only ~6 minutes was actually writing data — the other half hour was 36 small questions to Google Drive, each waiting on a round-trip rather than on bandwidth. That kind of waiting overlaps almost perfectly, which is why this helps more than a bandwidth argument suggests, and why Claude's first framing ("Drive will throttle") was the wrong worry. The global op lock is taken ONCE for the whole backup phase rather than dropped, so backups still cannot interleave with a deploy or a destroy — only with each other, which is the conservative half of the win. Updates stay strictly serial: replacing running containers is a different risk from reading their data. Backups now run before any update, which also orders the night sensibly on its own. The limit is configurable, and a configured `0` is clamped to 1 — it reads as "no limit" to a person and means "run none" to the stream, so a typo meant to go faster would have produced a night with no backups whose only trace is `last_backup` standing still. Sabotage-verified. Test: `y1_a_configured_zero_never_means_no_backups`, `y1_the_default_is_the_number_kenny_chose` | Y1, Y4, measured M-T75 | done 2026-09-02 |
| T73 | **Parallel deploys — dropped by Kenny, 2026-09-02 (form Y2).** He asked whether several containers could be rolled out at once, since so much time goes to waiting on restic. Measured first: the host serialises every mutating operation behind one lock (`state.op_lock`, `host/src/main.rs`), and a deploy touches six shared things — state.json, the intent repo, the scrape list, the dashboard dir, the front page, the monitor list — plus one shared staging path (T74). Doable, but the slow part of a deploy is the image pull, which the registry cache on CT 117 already fixed. Kenny: "we schrappen de parallelle uitrollen, dat is niet meer nodig." Recorded rather than deleted, so the measurement is not lost if the question returns | Y2 | dropped by Kenny 2026-09-02 |
| T74 | **The push staging path is shared across the whole daemon.** Every file bound for a container is written to `/var/lib/homelab/push-staging` first — one fixed path, no per-operation suffix. Harmless today because the global op lock guarantees one operation at a time, and parallel deploys are dropped (T73), so nothing is about to spring it. It is written down because the failure is silent: two concurrent pushes would overwrite each other's staging file and land one stack's compose in another stack's container, with both copies reporting success. `push_content_staged` already takes the path as a parameter, so the fix is passing a unique one | Put to Kenny as form item Y3 **Done 2026-09-02** (Kenny, form Y3: Gewenst). Recorded as F169. Three existing tests were coupled to the old shared name and now match on what they were actually about — the destination path, or the staged content — rather than on the host-side temporary name. | done 2026-09-02 |
| T72 | **Make the kyu hub publish a release binary** (F168). Its release carries no assets, so `homelab install-native` cannot finish a rebuild of CT 109 for the one service the others depend on. The work is in the kyu repository, not this one: a release workflow that attaches the binary and a SHA256SUMS, the same shape kyu-runner, http-switchboard and almanac already have. Until it lands, a rebuilt CT 109 needs the hub binary placed by hand | F168 **Kenny, form X5 (2026-09-02): Onmisbaar.** The work is in the kyu repository, not this one, so it runs in a session opened there; a handover carrying the measured facts is what this project owes it. **Done 2026-09-02**, in the kyu project, same night it was raised. See F168. | done 2026-09-02 |
| T69 | **The TUI's deploy window should be able to ask, not only show** (Kenny, form H1: "twee knoppen die het ofwel toelaten ofwel stoppen"). It already opens a modal with the whole live stream of the operation and scrolls; it has no way to pause and put a question to the operator. The case that raised it: a service check reporting a deliberate drop — routes 29 → 28 after form B2c removed a route — where the honest answer is "allow" rather than a failed deploy. The work is not the buttons: the host runs an operation to completion on its own, so pausing mid-deploy and sending a question back over the line changes how client and host talk **Done 2026-09-02.** A step can now stop and put a question to whoever is watching, and the case Kenny named is wired to it: a service check that fell — routes 29 → 28 after a route was removed on purpose — asks instead of failing outright. `a` allows, `s` stops, and each key says what it DOES rather than only its label (D82). The question is drawn over the live feed, because that is where the operator is already reading. **The load-bearing half is the unattended case:** the same operations run at 04:00 with no client, so the asker checks whether anyone is subscribed before it waits at all, times out after a configurable bound, and answers `Unattended` — deliberately not the same value as `Stop`, because "Kenny stopped this" and "nobody was there" are different stories in a transcript. Only an explicit yes continues. **Six sabotage attempts were needed to get one honest test**, and each failure was a different way for a test to prove nothing: it tested the enum and not the deploy; it lived in a file whose harness stopped at `validate`; it asserted on a log line the sabotage did not need to produce; it looked in `report.steps`, where a FAILED step never appears; and the reading never actually fell because the container did not exist so the baseline was empty. Test: `t69_a_regressed_check_fails_the_deploy_when_nobody_is_watching`, `a_question_nobody_answers_never_reads_as_permission`, `a_waiting_step_is_answerable_from_the_window_the_operator_is_reading`| done 2026-09-02 |
| T68 | **Eleven of fourteen stacks have no service checks.** Only gateway (6), media (4) and downloader (1) ship a `checks.yml`; almanac, home, kp-soft, kyu, metrics, paperwork, productivity, registry, syncthing and uptime have none, so a deploy on those measures nothing before and after itself. The model exists and is proven (D70-D72, and it found two faults on the gateway the day it was written) — what is missing is one file per service, and each has to be measured against the live service rather than imagined | **Done 2026-09-01:** all fourteen stacks now ship a `checks.yml`. Writing them found F163 (syncthing synchronising nothing) and an empty Homepage, and the layer rule refused two drafts of mine — supersync's first version had no application-layer reading at all. | done 2026-09-01 
| T65 | **A non-interactive way to create a stack** — `homelab new` with the same preset catalogue the TUI wizard uses, so a stack is never hand-written again (F136) | — **Confirmed still open 2026-09-01:** the client has no `new` verb. **Done 2026-09-01:** `homelab new <name> --preset <p> --vmid <n>` with optional `--ram --cores --disk --swap` and a repeatable `--no-data <path>`, so every feature the wizard has is reachable without it. The test that matters is not that it produces files but that it produces the SAME files: it scaffolds twice through the two paths and compares every byte, because two paths that drift apart would be worse than one path. | done 2026-09-01 |
| T64 | **Who measures what, and who notifies through which channel** — a full inventory and a redesign, at the END of the rollout. Cannot happen earlier: it runs largely through kyu, and Home Assistant's side needs revising too, so every service has to be up first. Prompted by finding that Uptime Kuma watches 36 things with zero notification channels (Kenny, 2026-09-01) | every service running | open |
| T63 | **A panel of expert-review sessions** after the rollout, **whose findings are input to this project's retrospective** rather than a separate pile of reports (Kenny, 2026-09-01) — a review that lands nowhere is a review nobody acts on. Kenny named seven lenses so far — the system itself, security, usability, devops, Rust, how logging is arranged, and Grafana with everything around it — and asked for suggestions of further ones. **The list is approved by him before any session is started.** Each is given its context by the rollout session, reviews independently, and reports back. **The rollout session goes on hold from the moment the first one starts** and resumes only when all five have reported (Kenny, 2026-09-01) | the rollout finished **Two more lenses, Kenny 2026-09-02.** (8) **Extensibility** — which software, existing or self-built, would enrich what this homelab can do, and exactly what would be needed to adopt each one. (9) **Compatibility of our own software** — how Kenny's other self-built programs actually work, ending in HARD guidelines for what a program must offer to be adopted here without bespoke work: a standardised self-update with a fallback is his example, and the point is that new software is written to the norm instead of the orchestrator growing another special case. That second one has a concrete trigger already: F172, the kyu hub's database copied while it is being written to, which every native service will have in some form. **Three more lenses, Kenny form Z9 (2026-09-02): all three adopted**, bringing the list to twelve. (10) **Runs, reports success, wired to nothing** — the dominant fault shape of this whole project: cadvisor at 1 of 10, a front page of zero bytes, a route that would have acknowledged warnings into the void, a dashboard removal running on the wrong machine, `pct push` reporting 5,000,000 bytes onto a full disk. A lens that hunts nothing but mechanisms that succeed without doing anything. (11) **Data: what exists, where, how long, what its loss costs** — one night produced five instances (F173 the cache backed up for nothing, F174 two repositories under dead names, the duplicated key in no backup, F172, F178's 670 GB of photographs). **Kenny added a named question to this lens on 2026-09-02 (form L3): where the `.env` files live.** F199 is its trigger — a stack's secrets sit at `/opt/<stack>/<app>/.env` inside the container, in no backup at all, and on the morning the latch keys vanished that made thirteen files the only readable copy of every secret in the fleet, in a directory this suite destroys and rebuilds as a matter of routine. The lens is to judge the PLACEMENT, not just the backup: is a container-local path the right home for a secret at all. (12) **What this system costs Kenny in attention** — how many notifications it sends, how many are actionable, how many are furniture: a monitor red for eight hours unnoticed (F157), restic warning nightly about a missing cache nobody read (F171), and two failed-deploy notifications at half past two from checks that could not work (F176). **When the sessions are created** (Kenny, 2026-09-02): tell each one that it may use the software-development procedure's own form mechanism to ask Kenny for clarification — a reviewer with a question should put it as one interactive form like every other choice in this ecosystem, not as prose. That belongs in the context each lens is given at creation, not discovered afterwards. **Lens 13, Kenny 2026-09-02 (form W, opmerkingen):** *"een uitgebreide analyse van hoe swap uitzetten werkt, wat de risico's zijn en wat er gebeurt als mijn diensten zouden uitvallen"*. Not a repeat of the memory lens: this one asks what the machine does at the moment memory runs out with no swap to fall back on — which process the kernel picks, what a container losing that fight looks like from outside, and whether the services would come back on their own. It has a live decision behind it (W1: swap set to 0 on thirteen stacks, 2026-09-02) rather than being theoretical.| open |

## Inherited open items (from before this project)

These were put to Kenny in earlier forms and never closed. They are carried
here so they cannot evaporate; each becomes a Phase 2 feature question or a
mini-round rather than a re-asked form.

| # | Item | Status |
|---|---|---|
| B1 | U1-U3 · who owns updates for the adopted native services (almanac observes its own reverts; kyu has no update mechanism and no binary release asset) | open |
| B2 | R1-R5 · the C7 milestone report was never signed off | open |
| B3 | V1-V5 · v1-tree cleanup (six dead components, `stacks-backup/`, `install-host-service.sh`, branch `v2-merge`) | open |
| B4 | Grafana service-account token (MB5) needed for the datasource, dashboards and the `kyu_sweeper_age_ms` alert | waiting on Kenny |
| B5 | D5 mirror remote + deploy key | waiting on Kenny |
| B6 | H2 OPNsense API credentials | waiting on Kenny |
| B7 | Broken-release rollback drill for C7 — needs a deliberately broken kyu release | parked |
